import type { Question } from '../../types/question';

export const securityQuestions: Question[] = [
  {
    id: 'sec-001',
    stem: 'A company wants to centrally manage WAF rules across 200 AWS accounts in an Organization. Rules must be enforced — individual account teams cannot remove or modify them. Which service and configuration achieves this?',
    type: 'single', difficulty: 2, topicSlug: 'security', examDomain: 'organizational-complexity',
    options: [
      { id: 'a', text: 'Deploy WAF rules in each account using CloudFormation StackSets', correct: false, explanation: 'StackSets can deploy WAF rules but account teams can still modify or delete deployed resources.' },
      { id: 'b', text: 'Use AWS Firewall Manager to create Organization-level WAF policies', correct: true, explanation: 'Firewall Manager enforces WAF policies across all accounts in an Organization. It automatically applies rules to new accounts and resources, and account teams cannot remove the managed rule groups.' },
      { id: 'c', text: 'Create an SCP that restricts modifications to WAF WebACLs', correct: false, explanation: 'An SCP can restrict WAF modifications but cannot create or enforce WAF rules itself.' },
      { id: 'd', text: 'Use AWS Security Hub to detect accounts without WAF rules and send alerts', correct: false, explanation: 'Security Hub is detective (alerts after the fact), not preventive.' },
    ],
    explanation: { overall: 'AWS Firewall Manager is designed for centralized security management across AWS Organizations. It enforces WAF WebACL policies, Shield Advanced subscriptions, Security Group policies, and Network Firewall policies across all accounts. Policies are automatically applied to new accounts/resources.', examTip: 'Firewall Manager = central security policy enforcement across Organizations. Requires AWS Organizations with All Features enabled and Security Hub enabled in the delegated admin account.' },
    tags: ['waf', 'firewall-manager', 'organizations', 'centralized-security'],
  },
  {
    id: 'sec-002',
    stem: 'A company discovers that their AWS credentials were accidentally exposed in a public GitHub repository. What are the FIRST TWO immediate steps they should take?',
    type: 'multiple', difficulty: 2, topicSlug: 'security', examDomain: 'continuous-improvement',
    options: [
      { id: 'a', text: 'Immediately deactivate the compromised access keys in IAM', correct: true, explanation: 'Deactivating (not deleting — preserve for forensics) immediately stops unauthorized API calls using those credentials.' },
      { id: 'b', text: 'Review CloudTrail logs to identify unauthorized API calls made with the exposed credentials', correct: true, explanation: 'CloudTrail shows which API calls were made with the compromised credentials, enabling assessment of the blast radius and identifying any resources created/modified.' },
      { id: 'c', text: 'Delete all IAM users and recreate them with new credentials', correct: false, explanation: 'Deleting all users is disruptive and unnecessary. Only the compromised credentials need to be addressed.' },
      { id: 'd', text: 'Change the AWS account root user password', correct: false, explanation: 'Root password change is not directly related to compromised IAM access keys. Address the actual compromised credentials first.' },
    ],
    explanation: { overall: 'Incident response for compromised credentials: (1) Immediate containment: deactivate credentials. (2) Investigation: CloudTrail logs to assess what was done. (3) Remediation: delete any rogue resources, reverse unauthorized changes. (4) Recovery: issue new credentials. (5) Prevention: detect with GuardDuty, use Secrets Manager.', examTip: 'Compromised credentials response order: Contain (deactivate) → Assess (CloudTrail) → Eradicate (delete rogue resources) → Recover (new credentials) → Prevent (GuardDuty, no long-lived keys). Do not delete credentials before forensic investigation.' },
    tags: ['incident-response', 'cloudtrail', 'iam', 'compromised-credentials'],
  },
  {
    id: 'sec-003',
    stem: 'A company wants to detect when EC2 instances are communicating with known malicious IP addresses, cryptocurrency mining domains, or command-and-control servers. They want automated detection without managing threat intelligence feeds. Which service provides this?',
    type: 'single', difficulty: 1, topicSlug: 'security', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'AWS Config with custom rules for network traffic analysis', correct: false, explanation: 'AWS Config monitors resource configuration changes, not network traffic or active threat intelligence.' },
      { id: 'b', text: 'Amazon GuardDuty', correct: true, explanation: 'GuardDuty analyzes VPC Flow Logs, CloudTrail, Route 53 DNS logs, and EKS/ECR logs using AWS threat intelligence and ML. Detects: unusual EC2 behavior, communications with malicious IPs, cryptocurrency mining, compromised credentials. Fully managed.' },
      { id: 'c', text: 'AWS Security Hub with CIS AWS Foundations Benchmark', correct: false, explanation: 'Security Hub aggregates security findings and checks compliance benchmarks. GuardDuty findings are fed into Security Hub, but Security Hub itself does not perform threat detection.' },
      { id: 'd', text: 'VPC Flow Logs with CloudWatch Logs Insights queries', correct: false, explanation: 'VPC Flow Logs record network traffic. Custom CloudWatch queries could detect known malicious IPs, but maintaining threat intelligence feeds requires significant manual effort vs GuardDuty\'s managed approach.' },
    ],
    explanation: { overall: 'GuardDuty: regional threat detection service. Sources: VPC Flow Logs, CloudTrail (management + data events), Route 53 resolver logs, EKS audit logs, ECR image scans, Lambda network activity, RDS login activity. Threat intelligence: AWS-maintained + CrowdStrike + Proofpoint. 30-day free trial, then per-event pricing.', examTip: 'GuardDuty = threat detection (anomalies, malicious activity). Security Hub = compliance aggregation. Macie = S3 data classification. Inspector = vulnerability scanning. Detective = investigation. These four form the core security services to know for SA Pro.' },
    tags: ['guardduty', 'threat-detection', 'security', 'managed-service'],
  },
  {
    id: 'sec-004',
    stem: 'A company stores sensitive PII data in S3 across 500 buckets and 10 TB of data. They need to identify which buckets contain PII and classify sensitive data automatically. Which service does this?',
    type: 'single', difficulty: 1, topicSlug: 'security', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Amazon GuardDuty with S3 protection enabled', correct: false, explanation: 'GuardDuty S3 protection detects suspicious access patterns (e.g., unusual API calls). It does not classify data content as PII.' },
      { id: 'b', text: 'Amazon Macie', correct: true, explanation: 'Macie uses ML to automatically discover, classify, and protect sensitive data in S3. Identifies PII (SSNs, credit cards, passport numbers, etc.). Generates findings for sensitive data in buckets that are publicly accessible or shared.' },
      { id: 'c', text: 'AWS Config with an S3 object content rule', correct: false, explanation: 'AWS Config monitors resource configurations, not object content within S3 buckets.' },
      { id: 'd', text: 'Amazon Comprehend with a batch S3 job', correct: false, explanation: 'Comprehend can detect PII in text but is a general NLP service requiring application code. Macie provides automated, managed S3-specific PII discovery.' },
    ],
    explanation: { overall: 'Amazon Macie: automated sensitive data discovery for S3. Managed data identifiers: 75+ types of sensitive data (PII, financial, credentials). Custom data identifiers: regex-based for company-specific patterns. Findings sent to Security Hub and EventBridge. Pricing: per S3 bucket scanned + data processed.', examTip: 'Macie = S3 sensitive data classification. Findings: SensitiveData/Credentials, SensitiveData/PersonalInformation, Policy/bucketPubliclyAccessible. Enable Macie organization-wide via delegated admin. Critical for GDPR, CCPA, HIPAA compliance.' },
    tags: ['macie', 'pii', 's3', 'data-classification', 'compliance'],
  },
  {
    id: 'sec-005',
    stem: 'A company uses AWS Certificate Manager (ACM) certificates on their Application Load Balancers. The certificates are about to expire. The security team needs to ensure certificates are renewed automatically. Which is true about ACM certificate renewal?',
    type: 'single', difficulty: 1, topicSlug: 'security', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'ACM public certificates renew automatically if the domain validation can be completed', correct: true, explanation: 'ACM automatically renews public certificates 60 days before expiration using the original validation method. DNS validation is fully automatic. Email validation requires human action on each renewal.' },
      { id: 'b', text: 'ACM certificates must be manually renewed by submitting a new CSR', correct: false, explanation: 'ACM manages the entire certificate lifecycle including renewal. No CSR submission needed.' },
      { id: 'c', text: 'ACM certificates expire and must be replaced before expiry, causing downtime', correct: false, explanation: 'ACM handles renewal automatically for DNS-validated certificates. No manual intervention and no downtime.' },
      { id: 'd', text: 'ACM certificates are renewed only if the load balancer is actively receiving traffic', correct: false, explanation: 'ACM renewal is independent of load balancer traffic. It only requires DNS validation to be in place (CNAME record).' },
    ],
    explanation: { overall: 'ACM auto-renewal: DNS validation = fully automatic (CNAME record checked). Email validation = requires human action (click approval email). Best practice: always use DNS validation for automated renewal. ACM cannot export private keys of managed certificates. For on-premises: import certificates into ACM.', examTip: 'ACM DNS validation = automatic renewal (zero maintenance). Email validation = manual renewal required. Use DNS validation for production certificates. ACM certificates can only be used with integrated AWS services (ALB, CloudFront, API Gateway, etc.), not EC2 directly.' },
    tags: ['acm', 'certificates', 'auto-renewal', 'dns-validation'],
  },
  {
    id: 'sec-006',
    stem: 'A company needs to encrypt data in DynamoDB at the field level (not just table-level encryption). Specific fields (credit card numbers) must be encrypted with a customer-managed key that is different from the table encryption key. Which approach achieves this?',
    type: 'single', difficulty: 3, topicSlug: 'security', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Enable DynamoDB encryption at rest with a CMK — this encrypts individual fields', correct: false, explanation: 'DynamoDB table-level encryption encrypts the entire table (all data at rest), not specific fields with different keys.' },
      { id: 'b', text: 'Use client-side encryption: encrypt specific fields using KMS before writing to DynamoDB', correct: true, explanation: 'Client-side encryption with KMS: the application calls KMS to encrypt credit card values before storing in DynamoDB. Only the application (with KMS permissions) can decrypt. Different CMKs per field or data type are supported.' },
      { id: 'c', text: 'Use DynamoDB Accelerator (DAX) with field-level encryption', correct: false, explanation: 'DAX is an in-memory cache for DynamoDB, not a field-level encryption mechanism.' },
      { id: 'd', text: 'Create a separate DynamoDB table for sensitive fields with a different CMK', correct: false, explanation: 'Separate tables for sensitive fields is a design approach but still uses table-level encryption, not field-level with different keys per attribute.' },
    ],
    explanation: { overall: 'Field-level encryption: application-level encryption using KMS before writing to DynamoDB. The DynamoDB item stores the encrypted blob. Only the application with KMS decrypt permission can read the plaintext. AWS DynamoDB Encryption Client (from AWS SDK) makes this easier with annotations.', examTip: 'DynamoDB encryption at rest = protects data from AWS-side access. Field-level encryption = protects data from anyone with DynamoDB access (other apps, DBAs). Use field-level for highest-sensitivity fields like PAN (credit cards), SSNs. Implement with AWS Encryption SDK or DynamoDB Encryption Client.' },
    tags: ['dynamodb', 'field-level-encryption', 'kms', 'client-side-encryption'],
  },
  {
    id: 'sec-007',
    stem: 'A company runs multi-tier applications in AWS. They need to perform automated vulnerability scanning on EC2 instances and container images in ECR to detect software vulnerabilities. Which service provides this?',
    type: 'single', difficulty: 1, topicSlug: 'security', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Amazon GuardDuty with EC2 malware protection', correct: false, explanation: 'GuardDuty detects threats (active malicious activity) but does not perform vulnerability scanning against CVE databases.' },
      { id: 'b', text: 'Amazon Inspector', correct: true, explanation: 'Inspector automatically discovers and scans EC2 instances and ECR container images for software vulnerabilities (CVEs). Continuously rescans when new CVEs are published. Provides risk scores, affected packages, and remediation recommendations.' },
      { id: 'c', text: 'AWS Shield Advanced with vulnerability assessment', correct: false, explanation: 'Shield Advanced protects against DDoS attacks, not software vulnerabilities.' },
      { id: 'd', text: 'AWS Security Hub with vulnerability standards', correct: false, explanation: 'Security Hub aggregates findings from Inspector and others, but does not perform vulnerability scanning itself.' },
    ],
    explanation: { overall: 'Amazon Inspector v2: automated vulnerability management. EC2: agent-based scanning (SSM agent required). ECR: CI/CD pipeline integration, scan images on push. Lambda: code package scanning. Findings include CVE ID, severity (CVSS), affected component, remediation recommendation. Integrates with Security Hub and EventBridge.', examTip: 'Inspector = vulnerability scanning (CVEs, unintended network exposure). GuardDuty = threat detection (active attacks, malicious behavior). Macie = data classification (PII). These three cover: vulnerabilities, active threats, and sensitive data respectively.' },
    tags: ['inspector', 'vulnerability-scanning', 'ec2', 'ecr', 'cve'],
  },
  {
    id: 'sec-008',
    stem: 'A company uses AWS KMS Customer Managed Keys (CMKs) to encrypt sensitive data. The security team wants to ensure that even if an employee deletes the CMK, the data can be recovered. Which KMS feature provides this protection?',
    type: 'single', difficulty: 2, topicSlug: 'security', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'KMS key aliases prevent accidental key deletion', correct: false, explanation: 'Key aliases are references to keys. Deleting the alias does not delete the key; deleting the key is still possible.' },
      { id: 'b', text: 'KMS key deletion schedule: keys are not immediately deleted; there is a 7-30 day waiting period', correct: true, explanation: 'KMS CMK deletion has a mandatory 7-30 day waiting period. During this time, the deletion can be cancelled. The key cannot be used during the pending deletion period. This provides time to detect and reverse accidental deletions.' },
      { id: 'c', text: 'Enable KMS key rotation to create new key versions that persist even if the current version is deleted', correct: false, explanation: 'Key rotation creates new key material for future encryption. Previous key versions are retained for decryption of existing data, but this is not protection against intentional key deletion.' },
      { id: 'd', text: 'Use multi-region keys so deletion in one region does not affect the replica key', correct: false, explanation: 'Multi-region key deletion: deleting the primary key schedules all replicas for deletion too. It does not independently protect against deletion.' },
    ],
    explanation: { overall: 'KMS key deletion protection: minimum 7-day waiting period (configurable up to 30 days). During pending deletion: key cannot encrypt/decrypt. Can be cancelled at any time before the deletion date. Deletion cannot be reversed after the waiting period. Best practices: IAM policies restricting kms:ScheduleKeyDeletion, CloudTrail alarm on schedule deletion API call.', examTip: 'KMS deletion: scheduled (7-30 days) → pending deletion state → permanent deletion. Cancel: kms:CancelKeyDeletion. Protect with IAM + SCP denying ScheduleKeyDeletion for critical keys. Consider key policies requiring MFA for deletion.' },
    tags: ['kms', 'key-deletion', 'data-protection', 'key-management'],
  },
  {
    id: 'sec-009',
    stem: 'A company wants to provide temporary AWS console access to contractors without creating IAM users. Contractors authenticate via their corporate directory (Active Directory). Which solution enables this?',
    type: 'single', difficulty: 2, topicSlug: 'security', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Create IAM users for each contractor and enforce MFA', correct: false, explanation: 'Creating IAM users for contractors requires management overhead (create, manage, delete). Corporate directory integration is more scalable.' },
      { id: 'b', text: 'AWS IAM Identity Center (SSO) with Active Directory as identity source', correct: true, explanation: 'IAM Identity Center integrates with on-premises AD (via AWS Directory Service or AD connector). Contractors authenticate with AD credentials. Identity Center federates access to AWS accounts with permission sets (IAM roles). Console SSO and CLI SSO supported.' },
      { id: 'c', text: 'Use AWS Cognito User Pools with SAML federation to Active Directory', correct: false, explanation: 'Cognito is for application-level user authentication, not for AWS console/API access federation.' },
      { id: 'd', text: 'Configure SAML 2.0 federation directly between Active Directory and IAM', correct: false, explanation: 'Direct SAML federation between AD and IAM works but is harder to manage than IAM Identity Center, which provides a centralized portal for multiple accounts and centralized permission management.' },
    ],
    explanation: { overall: 'IAM Identity Center: SSO portal for AWS accounts and applications. Identity sources: Identity Center directory, AWS Directory Service (Managed AD), external SAML IdP (Okta, Azure AD). Permission sets = IAM roles assigned to accounts. Multi-account SSO with one login. Preferred over individual SAML federations per account.', examTip: 'IAM Identity Center = multi-account SSO. Replaces the older AWS SSO. Sources: managed directory, on-prem AD, SAML IdP. Permission Sets = IAM roles. Preferred for enterprise multi-account AWS environments.' },
    tags: ['iam-identity-center', 'sso', 'active-directory', 'federation'],
  },
  {
    id: 'sec-010',
    stem: 'A company runs a web application behind CloudFront. They want to protect the ALB origin so that it only accepts traffic from CloudFront and not direct internet access. Which combination achieves this?',
    type: 'multiple', difficulty: 2, topicSlug: 'security', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Configure ALB security group to allow inbound only from CloudFront managed prefix list', correct: true, explanation: 'AWS provides a managed prefix list for CloudFront edge node IP ranges. ALB security group rule: allow HTTPS inbound from prefix list com.amazonaws.global.cloudfront.origin-facing.' },
      { id: 'b', text: 'Use CloudFront custom header + ALB WAF rule to allow only requests with the custom header', correct: true, explanation: 'CloudFront injects a custom secret header (X-Custom-Header: secret-value). ALB WAF rule blocks requests without this header. The secret header is unknown to direct internet users.' },
      { id: 'c', text: 'Enable CloudFront Origin Shield to protect the ALB from direct access', correct: false, explanation: 'Origin Shield reduces origin load by adding a caching layer, but does not restrict ALB from direct internet access.' },
      { id: 'd', text: 'Configure ALB to accept HTTPS only from AWS Certificate Manager certificates', correct: false, explanation: 'ACM certificate on ALB provides TLS termination. It does not restrict which clients (CloudFront vs internet) can connect.' },
    ],
    explanation: { overall: 'Protecting ALB origin from direct access: (1) Security Group + CloudFront IP prefix list (network-level). (2) Custom header + WAF (application-level, survives IP changes). Best practice: use BOTH for defense in depth. CloudFront also provides HTTPS, WAF, DDoS protection at edge.', examTip: 'CloudFront → ALB protection strategies: (1) SG + prefix list: network blocking, must update if prefix list changes (AWS manages it). (2) Custom header: application-level, rotate periodically. Use Secrets Manager + EventBridge to auto-rotate the custom header value.' },
    tags: ['cloudfront', 'alb', 'origin-protection', 'security-group', 'waf'],
  },
  {
    id: 'sec-011',
    stem: 'An application uses AWS Secrets Manager to store database credentials. The application retrieves secrets at startup. A security audit requires that credentials are automatically rotated every 30 days. How should rotation be configured?',
    type: 'single', difficulty: 2, topicSlug: 'security', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Enable automatic rotation with a Lambda rotation function; for RDS, use the built-in rotation template', correct: true, explanation: 'Secrets Manager automatic rotation uses a Lambda function that updates both the secret value and the actual database password atomically. AWS provides rotation templates for RDS, Redshift, and DocumentDB. Configure rotation interval (30 days).' },
      { id: 'b', text: 'Set a CloudWatch Event rule to trigger rotation every 30 days and update the secret', correct: false, explanation: 'Manual CloudWatch + Lambda for rotation misses the atomic update capability. Secrets Manager\'s built-in rotation handles the two-phase update (new credential, test, then commit).' },
      { id: 'c', text: 'Configure IAM to force password reset for the database user every 30 days', correct: false, explanation: 'IAM password policies apply to IAM user passwords, not database credentials stored in Secrets Manager.' },
      { id: 'd', text: 'Use Parameter Store SecureString instead, which has built-in rotation', correct: false, explanation: 'AWS Systems Manager Parameter Store does not have built-in automatic rotation. Secrets Manager is the service with rotation capability.' },
    ],
    explanation: { overall: 'Secrets Manager rotation: Lambda function performs a 4-step rotation (createSecret → setSecret → testSecret → finishSecret). For RDS: supports single-user rotation (test with same credentials) and alternating-user rotation (create new user, test, delete old). Rotation lambda needs access to both the database and Secrets Manager.', examTip: 'Secrets Manager: automatic rotation via Lambda. Built-in templates for RDS MySQL/PostgreSQL/MariaDB, Redshift, DocumentDB. For custom databases: write your own rotation Lambda. Lambda needs VPC access if DB is in private subnet + Secrets Manager VPC Endpoint or NAT Gateway.' },
    tags: ['secrets-manager', 'rotation', 'rds', 'lambda', 'security'],
  },
  {
    id: 'sec-012',
    stem: 'A company is building a multi-account AWS Organization. They want to centralize security findings from GuardDuty, Inspector, Macie, and Security Hub across all 100 accounts into a single dashboard. Which approach achieves this?',
    type: 'single', difficulty: 2, topicSlug: 'security', examDomain: 'organizational-complexity',
    options: [
      { id: 'a', text: 'Enable Security Hub in each account and manually export findings to a central S3 bucket', correct: false, explanation: 'Manual export per account is operationally intensive. Organization-level security services support centralized management natively.' },
      { id: 'b', text: 'Designate a Security Hub delegated administrator account; auto-enable Security Hub in all member accounts via Organizations', correct: true, explanation: 'Security Hub with Organizations: designate admin account, auto-enable in new accounts, aggregate findings from all member accounts (cross-region and cross-account). GuardDuty, Inspector, Macie also support delegated admin for central management.' },
      { id: 'c', text: 'Use CloudTrail organization trail to centralize all security events', correct: false, explanation: 'Organization CloudTrail centrally logs API calls (audit trail), not security findings from GuardDuty/Inspector/Macie.' },
      { id: 'd', text: 'Create an EventBridge event bus in each account forwarding to a central account', correct: false, explanation: 'EventBridge cross-account event forwarding can aggregate findings but is more complex. Security Hub Organizations provides native cross-account aggregation.' },
    ],
    explanation: { overall: 'Security Hub Organizations: one delegated admin account, all member accounts automatically enroll. Findings automatically aggregated in admin account. Cross-region aggregation also supported (designate an aggregation region). Supports AWS Config, GuardDuty, Inspector, Macie, Firewall Manager, IAM Access Analyzer findings.', examTip: 'Security services with Organizations delegated admin: GuardDuty, Inspector, Macie, Security Hub, Firewall Manager, IAM Access Analyzer. Designate one account (security account) as delegated admin for all. Centralized view of security posture across Organization.' },
    tags: ['security-hub', 'organizations', 'delegated-admin', 'centralized-security'],
  },
  {
    id: 'sec-013',
    stem: 'A company wants to implement network segmentation to prevent lateral movement between EC2 workloads in the same VPC. Even if an EC2 instance is compromised, it should not be able to communicate with other EC2 instances. Which security control prevents this?',
    type: 'single', difficulty: 2, topicSlug: 'security', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Place each workload in a separate VPC and use TGW for controlled inter-VPC traffic', correct: false, explanation: 'Separate VPCs provide strong isolation but are operationally complex for many workloads. Security Groups can provide isolation within a single VPC.' },
      { id: 'b', text: 'Restrict Security Group rules to not allow inbound from other EC2 security groups in the same VPC', correct: true, explanation: 'Security Groups with no inbound rule allowing other SGs = EC2 instances cannot communicate with each other. Each workload SG only allows inbound from specific sources (ALB, specific service). No SG-to-SG rules = zero east-west traffic.' },
      { id: 'c', text: 'Enable VPC Flow Logs to monitor and block east-west traffic', correct: false, explanation: 'VPC Flow Logs are detective (logging), not preventive. They cannot block traffic.' },
      { id: 'd', text: 'Use Network ACLs to block all east-west traffic in the subnet', correct: false, explanation: 'NACLs block by CIDR. If all workloads are in the same subnet/CIDR, you cannot distinguish between them with NACLs.' },
    ],
    explanation: { overall: 'Zero-trust micro-segmentation with Security Groups: each EC2 workload has its own SG with no inbound rules allowing other workload SGs. Communication is only through approved paths (ALB → web tier SG). Lateral movement is blocked at the network level. This is the AWS micro-segmentation pattern within a VPC.', examTip: 'Security Group micro-segmentation: remove all default "allow all from same SG" rules. Define explicit inbound rules per workload. Combine with VPC Flow Logs for east-west traffic visibility. GuardDuty detects unusual network patterns even with SG controls.' },
    tags: ['security-groups', 'micro-segmentation', 'lateral-movement', 'zero-trust'],
  },
  {
    id: 'sec-014',
    stem: 'A company must meet compliance requirements to maintain an audit trail of all changes to AWS infrastructure. The trail must be tamper-proof — even privileged AWS users cannot modify or delete the logs. Which CloudTrail configuration achieves this?',
    type: 'single', difficulty: 2, topicSlug: 'security', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Enable CloudTrail and store logs in S3 with restrictive bucket policies', correct: false, explanation: 'Bucket policies restrict access but can be modified by account admins. S3 Object Lock provides immutability that bucket policies cannot.' },
      { id: 'b', text: 'Enable CloudTrail with log file validation and store logs in S3 with Object Lock Compliance mode', correct: true, explanation: 'Log file validation: CloudTrail creates a hash digest file for every log. Any tampering is detectable. S3 Object Lock Compliance mode: prevents deletion even by root. Together: immutable, tamper-evident audit trail.' },
      { id: 'c', text: 'Enable CloudTrail with server-side encryption using KMS CMK', correct: false, explanation: 'KMS encryption protects log confidentiality but does not prevent deletion or modification of log files.' },
      { id: 'd', text: 'Use AWS Config to record all configuration changes instead of CloudTrail', correct: false, explanation: 'AWS Config records resource configuration state changes but not API call details. CloudTrail records the API calls (who did what, when).' },
    ],
    explanation: { overall: 'CloudTrail tamper-proof audit trail: (1) Enable in all regions, (2) Enable log file integrity validation (SHA-256 hash), (3) Send to S3 with Object Lock Compliance mode + KMS encryption, (4) SNS alert on log delivery issues, (5) SCP denying CloudTrail disable actions. Organization CloudTrail for all accounts.', examTip: 'CloudTrail best practices: enable in ALL regions (multi-region trail). Log file validation: detects log tampering (not prevention — use S3 Object Lock for prevention). Object Lock Compliance = immutable logs. Send to separate security account for extra isolation.' },
    tags: ['cloudtrail', 'audit-trail', 'object-lock', 'log-integrity', 'compliance'],
  },
  {
    id: 'sec-015',
    stem: 'A company uses AWS WAF on their Application Load Balancer. They want to prevent SQL injection and XSS attacks, rate-limit requests from individual IP addresses, and use managed rule groups from AWS. Which WAF configuration achieves all three?',
    type: 'single', difficulty: 2, topicSlug: 'security', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Add AWS Managed Rule Groups (Core Rule Set, SQL injection), a rate-based rule limiting 2000 req/5min per IP, and custom XSS rules', correct: true, explanation: 'AWS Managed Rules (AWSManagedRulesCommonRuleSet): blocks common vulnerabilities including XSS. AWSManagedRulesSQLiRuleSet: SQL injection protection. Rate-based rule: configurable threshold per 5-minute window per IP. All three in one WebACL.' },
      { id: 'b', text: 'Enable AWS Shield Advanced on the ALB to block SQL injection and XSS', correct: false, explanation: 'Shield Advanced protects against DDoS attacks (volumetric, protocol layer). It does not provide application-layer protection against SQL injection or XSS.' },
      { id: 'c', text: 'Create custom WAF rules using regex pattern sets for each vulnerability type', correct: false, explanation: 'Custom regex rules require maintaining and updating patterns manually. AWS Managed Rules are maintained by AWS Security Research team and updated automatically.' },
      { id: 'd', text: 'Configure Security Groups on the ALB to block SQL injection patterns', correct: false, explanation: 'Security Groups operate at IP/port level and cannot inspect HTTP request content for SQL injection or XSS patterns.' },
    ],
    explanation: { overall: 'WAF WebACL components: (1) AWS Managed Rule Groups (pre-built, maintained by AWS): Core Rule Set (CRS), SQL injection, Known Bad Inputs, Bot Control, IP Reputation. (2) Rate-based rules: count requests per IP/country in 5-minute window. (3) Custom rules: regex, IP sets, geo-match. Combine for comprehensive protection.', examTip: 'WAF managed rules: AWSManagedRulesCommonRuleSet (CRS) = XSS, path traversal, etc. AWSManagedRulesSQLiRuleSet = SQL injection. AWSManagedRulesKnownBadInputsRuleSet = known attack patterns. Bot Control (paid). All automatically updated. Rate-based rules = DDoS mitigation at L7.' },
    tags: ['waf', 'sql-injection', 'xss', 'rate-limiting', 'managed-rules'],
  },
  {
    id: 'sec-016',
    stem: 'A company must encrypt all data in transit between microservices running in ECS. Even traffic within the VPC between containers must be encrypted. What approach provides transparent encryption without application changes?',
    type: 'single', difficulty: 3, topicSlug: 'security', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Configure TLS in each application container using ACM certificates', correct: false, explanation: 'Application-level TLS works but requires code changes to each service and certificate management per service.' },
      { id: 'b', text: 'Use AWS App Mesh with mutual TLS (mTLS) and ACM Private CA', correct: true, explanation: 'App Mesh is a service mesh that injects Envoy sidecar proxies. mTLS with ACM Private CA: certificates issued automatically to each Envoy proxy. Traffic between services is encrypted without application code changes. Envoy handles TLS termination.' },
      { id: 'c', text: 'Enable VPC encryption at rest using KMS to encrypt all network traffic', correct: false, explanation: 'KMS encrypts data at rest, not data in transit. Network traffic encryption requires TLS or VPC-level encryption features (not available for general VPC traffic).' },
      { id: 'd', text: 'Use a Network Load Balancer with TLS termination between each service', correct: false, explanation: 'NLB TLS termination would require an NLB per service-to-service pair, which is complex and does not scale.' },
    ],
    explanation: { overall: 'Service mesh with mTLS: App Mesh or AWS App Mesh injects Envoy proxy sidecars into each container. Proxies handle mTLS automatically: certificate issuance (via ACM Private CA or SPIFFE/SVID), certificate rotation, mTLS handshake. Application sees plaintext; proxy handles encryption/decryption. Zero application code changes.', examTip: 'Service mesh (App Mesh): service discovery, traffic management, observability, security (mTLS). Envoy sidecar = transparent proxy. mTLS = mutual authentication between services (both client and server present certificates). ACM Private CA = private certificate authority for internal certs.' },
    tags: ['app-mesh', 'mtls', 'encryption-in-transit', 'service-mesh', 'ecs'],
  },
  {
    id: 'sec-017',
    stem: 'A company uses Amazon S3 to store sensitive data. They want to ensure that S3 data is only accessible through specific VPC endpoints, preventing exfiltration via public internet. Which S3 bucket policy condition restricts access to only a specific VPC?',
    type: 'single', difficulty: 2, topicSlug: 'security', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Condition: StringEquals: "aws:SourceVpc": "vpc-12345"', correct: true, explanation: 'The aws:SourceVpc condition key restricts access to requests originating from a specific VPC (via Gateway or Interface Endpoint). Only calls that pass through the specified VPC\'s endpoint can access the bucket.' },
      { id: 'b', text: 'Condition: StringEquals: "aws:SourceIp": "VPC CIDR range"', correct: false, explanation: 'aws:SourceIp matches the request\'s source IP. For requests from within a VPC through a Gateway Endpoint, the source IP is the EC2 instance IP — not reliable for VPC restriction. Use aws:SourceVpce or aws:SourceVpc instead.' },
      { id: 'c', text: 'Condition: StringEquals: "aws:VpcSourceIp": "VPC CIDR"', correct: false, explanation: 'aws:VpcSourceIp is not a valid IAM condition key. Use aws:SourceVpc or aws:SourceVpce.' },
      { id: 'd', text: 'Condition: Bool: "aws:SecureTransport": "true"', correct: false, explanation: 'aws:SecureTransport restricts to HTTPS only, not to specific VPC access. This is a separate security control (encryption in transit, not network boundary).' },
    ],
    explanation: { overall: 'S3 VPC restriction: aws:SourceVpc restricts to requests from a specific VPC. aws:SourceVpce restricts to a specific VPC Endpoint. Best practice for data exfiltration prevention: bucket policy DenyNotFrom-VPC: {Effect: Deny, Condition: StringNotEquals aws:SourceVpc = vpc-xxx}. Even if credentials are stolen, data cannot be exfiltrated from outside the VPC.', examTip: 'S3 exfiltration prevention: (1) aws:SourceVpc/aws:SourceVpce in bucket policy. (2) VPC Endpoint policy restricting which S3 buckets the endpoint can access. (3) Combined: both bucket AND endpoint policies for defense in depth.' },
    tags: ['s3', 'vpc-endpoint', 'data-exfiltration', 'bucket-policy', 'condition-keys'],
  },
  {
    id: 'sec-018',
    stem: 'A company recently passed a SOC 2 Type II audit. They need to continuously demonstrate compliance as their AWS infrastructure evolves. Which AWS service provides automated compliance checking against AWS security best practices with pre-built frameworks like CIS, PCI DSS, and SOC 2?',
    type: 'single', difficulty: 1, topicSlug: 'security', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'AWS Trusted Advisor', correct: false, explanation: 'Trusted Advisor provides best practice checks across cost, performance, security, fault tolerance, and service limits but does not provide compliance framework-specific reports (CIS, PCI, SOC 2).' },
      { id: 'b', text: 'AWS Security Hub', correct: true, explanation: 'Security Hub provides automated security best practice checks using compliance standards: CIS AWS Foundations Benchmark, PCI DSS, AWS Foundational Security Best Practices, NIST 800-53. Continuous monitoring and compliance score.' },
      { id: 'c', text: 'AWS Audit Manager', correct: false, explanation: 'Audit Manager automates audit evidence collection for compliance reporting. It complements Security Hub by collecting evidence, but Security Hub does the continuous automated security checks.' },
      { id: 'd', text: 'AWS Config with conformance packs', correct: false, explanation: 'Config conformance packs provide compliance rules. Security Hub aggregates Config findings along with findings from other services into a unified compliance dashboard.' },
    ],
    explanation: { overall: 'Security Hub compliance: automated checks against 200+ controls across compliance standards. Integrated with Config rules. Aggregated score per standard. Findings exportable for evidence. Audit Manager: evidence collection for audit workflows, integrates Security Hub findings.', examTip: 'Security Hub: automated continuous compliance checks + security finding aggregation. Audit Manager: structured audit evidence collection (for auditors). Config: resource configuration tracking. Use Security Hub for ongoing compliance monitoring; Audit Manager for formal audit preparation.' },
    tags: ['security-hub', 'compliance', 'cis', 'pci-dss', 'continuous-monitoring'],
  },
  {
    id: 'sec-019',
    stem: 'A company needs to implement zero-trust network access for EC2 instances in a private VPC. Engineers must be able to SSH and RDP into instances without opening port 22 or 3389 inbound or maintaining bastion hosts. Which solution achieves this?',
    type: 'single', difficulty: 2, topicSlug: 'security', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'AWS Systems Manager Session Manager with IAM authentication', correct: true, explanation: 'Session Manager: browser-based or CLI shell access to EC2 without inbound ports (22/3389) or bastion hosts. Authentication via IAM. Session logs to CloudTrail and CloudWatch. Requires SSM agent + IAM role on EC2.' },
      { id: 'b', text: 'Deploy a bastion host in a public subnet with MFA-protected SSH access', correct: false, explanation: 'Bastion hosts require managing EC2 instances, SSH keys, and open port 22. This is operational overhead and increases attack surface.' },
      { id: 'c', text: 'Use EC2 Instance Connect for browser-based SSH access', correct: false, explanation: 'EC2 Instance Connect provides browser-based SSH but requires port 22 to be accessible from EC2 Instance Connect service IP ranges.' },
      { id: 'd', text: 'Configure AWS Client VPN for engineers to connect to the private VPC', correct: false, explanation: 'Client VPN provides network access but still requires port 22/3389 to be open on the instance\'s security group for the VPN CIDR.' },
    ],
    explanation: { overall: 'SSM Session Manager: no inbound ports, no bastion host, no SSH keys. Works via SSM agent calling SSM API outbound. Access control via IAM policies (ssm:StartSession). Session logging: commands and output to CloudWatch Logs and/or S3. Supports SSH/SCP tunneling over Session Manager for compatibility.', examTip: 'Session Manager = zero-trust EC2 access. Requires: SSM agent, IAM instance profile with SSM permissions, SSM VPC Endpoint (if private subnet). No ports open. All access audited. Use Session Manager document policies to restrict which instances users can access.' },
    tags: ['ssm', 'session-manager', 'zero-trust', 'bastion-replacement', 'iam'],
  },
  {
    id: 'sec-020',
    stem: 'A company stores database encryption keys in AWS Secrets Manager. Their compliance team requires that any access to the encryption keys must require approval from two different authorized people (dual control). Which AWS feature enables this "two-person integrity" requirement?',
    type: 'single', difficulty: 3, topicSlug: 'security', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'KMS key policy requiring MFA for key usage', correct: false, explanation: 'MFA on KMS ensures the key user authenticated with MFA, but does not require a second person to approve.' },
      { id: 'b', text: 'AWS IAM Conditions requiring two separate users to sign the API request', correct: false, explanation: 'IAM does not support dual-user signing of API requests. Each API call is made by a single IAM principal.' },
      { id: 'c', text: 'AWS Systems Manager Change Manager with approval workflows requiring multiple approvers before accessing secrets', correct: true, explanation: 'Change Manager implements approval workflows. For sensitive operations, require 2 approvers before executing a Change Template that grants temporary access or runs automation that retrieves the secret.' },
      { id: 'd', text: 'Require two separate IAM roles to both be assumed before accessing Secrets Manager', correct: false, explanation: 'You can assume one role at a time. Role chaining is possible but does not require a second person to participate.' },
    ],
    explanation: { overall: 'Two-person integrity in AWS: use Step Functions/Change Manager approval workflows. Pattern: request access → approval required from N approvers → approved → temporary IAM permission granted or automation runs. For KMS: custom solution with workflow tool (Jira, ServiceNow via EventBridge) → Lambda grants temporary key access.', examTip: 'Dual control in AWS typically requires process/workflow solutions (Change Manager, Step Functions with human approval states, or integration with ITSM tools). No native AWS service enforces "two physical persons" at the IAM API level — this is a process control.' },
    tags: ['dual-control', 'change-manager', 'approval-workflow', 'compliance', 'secrets-manager'],
  },
];
