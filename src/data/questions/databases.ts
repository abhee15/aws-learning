import type { Question } from '../../types/question';

export const databasesQuestions: Question[] = [
  {
    id: 'db-001',
    stem: 'A company needs a globally distributed database that can accept writes in multiple AWS regions simultaneously with minimal replication lag. The application can handle eventual consistency. Which AWS database service meets this requirement?',
    type: 'single', difficulty: 2, topicSlug: 'databases', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Amazon Aurora Global Database', correct: false, explanation: 'Aurora Global Database has one primary write region and up to 5 read-only secondary regions. Writes only go to the primary region.' },
      { id: 'b', text: 'Amazon DynamoDB Global Tables', correct: true, explanation: 'DynamoDB Global Tables is a multi-active (multi-master) replication solution. All replica tables can accept writes. Conflicts resolved with last-writer-wins. Replication lag typically under 1 second.' },
      { id: 'c', text: 'Amazon RDS Multi-AZ with cross-region read replicas', correct: false, explanation: 'RDS read replicas in other regions are read-only — they cannot accept writes.' },
      { id: 'd', text: 'Amazon Aurora Multi-Master', correct: false, explanation: 'Aurora Multi-Master allows multiple write nodes within a single region, not across regions.' },
    ],
    explanation: { overall: 'DynamoDB Global Tables provides multi-active replication across AWS regions. Every replica can accept reads and writes. Conflict resolution uses last-writer-wins. Sub-second replication lag. Only AWS-managed DB with native multi-region writes.', examTip: 'DynamoDB Global Tables = multi-region, multi-active (writes in any region). Aurora Global DB = multi-region, single writer (reads in other regions). This distinction is frequently tested.' },
    tags: ['dynamodb', 'global-tables', 'multi-region', 'multi-active'],
  },
  {
    id: 'db-002',
    stem: 'A serverless application uses Lambda to process requests. The Lambda function connects to an Amazon RDS PostgreSQL database. Under peak load, connections are being exhausted and requests are failing. The application scales to 2000 concurrent Lambda invocations. Which solution resolves this?',
    type: 'single', difficulty: 2, topicSlug: 'databases', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Increase the RDS instance size to handle more connections', correct: false, explanation: 'Larger instances allow more connections but 2000 concurrent connections would still overwhelm any database.' },
      { id: 'b', text: 'Use Amazon RDS Proxy to pool and multiplex connections from Lambda to RDS', correct: true, explanation: 'RDS Proxy maintains a pool of database connections and multiplexes thousands of Lambda connections to a smaller pool. Reduces connection overhead and handles failover transparently.' },
      { id: 'c', text: 'Configure Lambda reserved concurrency to limit to 100 concurrent invocations', correct: false, explanation: 'Limiting concurrency solves the connection problem but throttles the application during peak load.' },
      { id: 'd', text: 'Switch to DynamoDB to eliminate connection limits', correct: false, explanation: 'DynamoDB does not have connection limits but requires significant application refactoring.' },
    ],
    explanation: { overall: 'RDS Proxy is specifically designed for serverless applications that create many short-lived database connections. It pools connections from thousands of Lambda invocations into a smaller persistent connection pool. Also provides faster failover and stores credentials in Secrets Manager.', examTip: 'Lambda + RDS = connection exhaustion risk. Solution = RDS Proxy. RDS Proxy requires Secrets Manager for credentials. Supports MySQL and PostgreSQL.' },
    tags: ['rds-proxy', 'lambda', 'connection-pooling', 'serverless'],
  },
  {
    id: 'db-003',
    stem: 'A company runs a social media application with unpredictable traffic patterns ranging from 100 to 1,000,000 requests per second. The database needs to scale automatically and charge only for actual usage. Which AWS database is best suited?',
    type: 'single', difficulty: 1, topicSlug: 'databases', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Amazon RDS Aurora Serverless v2', correct: false, explanation: 'Aurora Serverless v2 scales automatically but has a minimum capacity unit (ACU), so there is always a baseline cost even at near-zero load.' },
      { id: 'b', text: 'Amazon DynamoDB with On-Demand capacity mode', correct: true, explanation: 'DynamoDB On-Demand scales instantly from 0 to millions of requests per second. Charges per request ($1.25 per million write units, $0.25 per million read units). No minimum capacity.' },
      { id: 'c', text: 'Amazon ElastiCache for Redis', correct: false, explanation: 'ElastiCache Redis is an in-memory cache/session store, not a primary database. It requires pre-provisioned nodes.' },
      { id: 'd', text: 'Amazon Neptune', correct: false, explanation: 'Neptune is a graph database for relationship-heavy data. Not a general-purpose database for social media workloads.' },
    ],
    explanation: { overall: 'DynamoDB On-Demand: pay-per-request pricing with no capacity planning. Automatically handles any traffic level. Ideal for unpredictable, spiky workloads. Provisioned mode (with Auto Scaling) is better for predictable workloads where sustained throughput is needed.', examTip: 'DynamoDB On-Demand = spiky/unpredictable traffic, higher per-request cost. DynamoDB Provisioned + Auto Scaling = predictable traffic, lower cost. Aurora Serverless v2 = spiky but relational, minimum cost baseline.' },
    tags: ['dynamodb', 'on-demand', 'auto-scaling', 'serverless'],
  },
  {
    id: 'db-004',
    stem: 'A company uses Amazon RDS MySQL. They need to scale read capacity for a reporting workload that performs complex queries. The reporting queries should not impact the production OLTP workload. Which solution achieves this?',
    type: 'single', difficulty: 1, topicSlug: 'databases', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Enable RDS Multi-AZ and route reporting queries to the standby', correct: false, explanation: 'RDS Multi-AZ standby does not accept reads — it is a synchronous hot standby for failover only.' },
      { id: 'b', text: 'Create RDS Read Replicas and route reporting traffic to the replica endpoint', correct: true, explanation: 'Read Replicas are asynchronously replicated copies of the primary. Reporting queries can use the replica endpoint, offloading read traffic from the primary OLTP instance. Up to 15 read replicas for Aurora, 5 for RDS MySQL.' },
      { id: 'c', text: 'Enable RDS Performance Insights to optimize query performance', correct: false, explanation: 'Performance Insights identifies slow queries but does not offload read traffic from the primary.' },
      { id: 'd', text: 'Use RDS Proxy to distribute read and write traffic', correct: false, explanation: 'RDS Proxy manages connection pooling but does not route reads to replicas automatically. Use custom connection strings or Route 53 for read routing.' },
    ],
    explanation: { overall: 'Read Replicas: asynchronous replication from primary. Accepts reads (SELECT only). Separate endpoint for connection routing. Reports connect to replica endpoint; OLTP connects to primary. Replication lag is usually under a second for lightly loaded primaries.', examTip: 'Multi-AZ = HA failover (synchronous replication, standby cannot read). Read Replica = read scaling (asynchronous, readable). Aurora has a cluster endpoint (auto-read/write routing) and reader endpoint (load balanced reads). RDS MySQL/PostgreSQL: manual endpoint management.' },
    tags: ['rds', 'read-replica', 'read-scaling', 'oltp-olap'],
  },
  {
    id: 'db-005',
    stem: 'A startup builds an application that needs to handle millions of user requests per day with consistent single-digit millisecond latency. Data access patterns are well-known in advance: get user by ID, list orders by user, and update order status. Which database and key design is optimal?',
    type: 'single', difficulty: 2, topicSlug: 'databases', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Amazon RDS Aurora MySQL with optimized indexes', correct: false, explanation: 'Aurora provides excellent performance but connection management and query planning add overhead. DynamoDB provides more predictable single-digit ms latency at any scale.' },
      { id: 'b', text: 'DynamoDB with a composite primary key (UserID as partition key, OrderID as sort key), with GSI for status queries', correct: true, explanation: 'DynamoDB partition key = UserID enables GetItem by user ID. Sort key = OrderID enables efficient range queries (list orders by user). GSI on status enables efficient status filtering. Single-digit ms at any scale.' },
      { id: 'c', text: 'Amazon ElastiCache Redis as the primary database', correct: false, explanation: 'Redis is an in-memory store. Data is lost on restart without persistence. Not suitable as a primary database for transactional data.' },
      { id: 'd', text: 'Amazon DocumentDB for flexible JSON storage', correct: false, explanation: 'DocumentDB provides MongoDB-compatible JSON storage but lacks the serverless scaling and predictable performance of DynamoDB for known access patterns.' },
    ],
    explanation: { overall: 'DynamoDB with composite key: (UserID, OrderID) supports: GetItem by UserID+OrderID, Query by UserID (all orders), Query with SortKey filter (orders by date range). GSI allows querying by non-key attributes (status). Single-table design: store multiple entity types in one table using prefixes.', examTip: 'DynamoDB design: identify access patterns FIRST, then design keys. Partition key = entity, Sort key = attribute to range-query on. GSIs for alternative access patterns. Avoid hot partition keys (high-cardinality partition keys spread load).' },
    tags: ['dynamodb', 'partition-key', 'gsi', 'data-modeling'],
  },
  {
    id: 'db-006',
    stem: 'A company needs to migrate an on-premises Oracle database to AWS. They want to move to a fully managed database with minimal code changes. The Oracle database uses stored procedures and Oracle-specific features. Which migration path minimizes re-engineering?',
    type: 'single', difficulty: 2, topicSlug: 'databases', examDomain: 'migration-modernization',
    options: [
      { id: 'a', text: 'Migrate directly to Amazon Aurora PostgreSQL using AWS Schema Conversion Tool', correct: false, explanation: 'SCT converts Oracle to PostgreSQL but stored procedures and Oracle-specific SQL need significant conversion. Not "minimal code changes."' },
      { id: 'b', text: 'Migrate to Amazon RDS for Oracle using AWS Database Migration Service', correct: true, explanation: 'RDS Oracle is a fully managed Oracle compatible database. DMS migrates data with minimal downtime. Oracle-specific features (stored procedures, packages) work unchanged. Pay per instance without managing hardware.' },
      { id: 'c', text: 'Lift and shift Oracle to EC2 instances', correct: false, explanation: 'EC2 Oracle provides maximum compatibility but is not fully managed — requires OS/database patching, backup management, etc.' },
      { id: 'd', text: 'Use AWS Glue to extract and load Oracle data into Amazon Redshift', correct: false, explanation: 'Redshift is a data warehouse for analytics, not an OLTP replacement for Oracle.' },
    ],
    explanation: { overall: 'RDS Oracle: managed Oracle with BYOL or License Included. Supports all Oracle features. DMS handles ongoing replication during migration with minimal downtime. Full Managed: automated backups, Multi-AZ, patching. For Oracle to open-source: use SCT to assess and convert (higher effort but lower ongoing license cost).', examTip: 'Oracle migration paths: RDS Oracle (managed, same Oracle code, BYOL), EC2 Oracle (unmanaged, full Oracle features), Aurora PostgreSQL via SCT (lower cost, more refactoring). SA Pro often asks which minimizes refactoring vs which minimizes cost.' },
    tags: ['oracle', 'rds', 'dms', 'database-migration'],
  },
  {
    id: 'db-007',
    stem: 'A company stores transaction data in DynamoDB. They need to run complex analytical queries joining multiple attributes and computing aggregates. DynamoDB Scan operations are too slow and expensive. Which architecture enables analytics without impacting DynamoDB?',
    type: 'single', difficulty: 2, topicSlug: 'databases', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Use DynamoDB PartiQL for SQL-like queries', correct: false, explanation: 'DynamoDB PartiQL provides SQL syntax but still uses DynamoDB table scans. Complex joins and aggregations are not supported efficiently in DynamoDB.' },
      { id: 'b', text: 'DynamoDB Streams → Lambda → S3 → Athena for analytics', correct: true, explanation: 'DynamoDB Streams captures change events. Lambda processes and writes to S3 (Parquet). Athena queries S3 with SQL, enabling complex analytics without impacting DynamoDB. ETL job can also do initial full export.' },
      { id: 'c', text: 'Use DynamoDB Global Secondary Indexes for analytical queries', correct: false, explanation: 'GSIs support single-table item access patterns but do not support complex joins and aggregations across items.' },
      { id: 'd', text: 'Enable DynamoDB Multi-AZ to add read replicas for analytics', correct: false, explanation: 'DynamoDB does not use traditional read replicas. DAX provides caching; Global Tables provides multi-region. Neither adds analytical query capability.' },
    ],
    explanation: { overall: 'Analytics on DynamoDB: use DynamoDB Streams to capture changes → Lambda processes events → write to S3 data lake → Athena for SQL analytics. For initial load: DynamoDB export to S3 (native feature, no impact on performance). This pattern separates OLTP (DynamoDB) from analytics (S3/Athena).', examTip: 'DynamoDB + Analytics: use DynamoDB S3 Export (built-in, no RCU cost) for initial data load to S3. Use DynamoDB Streams for ongoing changes. Then Athena or Redshift for analytics. Do not scan DynamoDB for analytics.' },
    tags: ['dynamodb', 'streams', 'athena', 'analytics', 's3'],
  },
  {
    id: 'db-008',
    stem: 'An RDS Aurora MySQL cluster has a primary instance and 2 Aurora Replicas. A failure occurs in the primary instance. What happens automatically?',
    type: 'single', difficulty: 1, topicSlug: 'databases', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Aurora automatically promotes one of the replicas to primary, typically in under 30 seconds', correct: true, explanation: 'Aurora Multi-AZ failover is automatic. The Aurora replica with the least replication lag is promoted to primary. DNS for the cluster endpoint is updated. Typically completes in 30 seconds or less.' },
      { id: 'b', text: 'AWS manually intervenes to restore the primary from the latest automated backup', correct: false, explanation: 'Aurora failover is fully automatic. No manual intervention needed. Restoring from backup would be much slower (minutes to hours).' },
      { id: 'c', text: 'Aurora replays the binary log to bring the replica up to date before promoting', correct: false, explanation: 'Aurora uses a shared distributed storage layer — replicas already have the same data as the primary. No binary log replay needed, hence the fast failover.' },
      { id: 'd', text: 'The application must reconnect to a new endpoint after Aurora provisions a new primary', correct: false, explanation: 'Applications connect to the cluster endpoint. DNS is updated during failover. Reconnection is needed, but no endpoint change is required in the application configuration.' },
    ],
    explanation: { overall: 'Aurora failover: Aurora Replicas are already synchronized via the shared distributed storage. During primary failure, Aurora promotes the healthiest replica in ~30 seconds (much faster than RDS Multi-AZ ~60-120s). The cluster endpoint DNS is updated. Applications using the cluster endpoint reconnect automatically.', examTip: 'Aurora failover < 30 seconds. RDS Multi-AZ failover 1-2 minutes. Aurora uses shared storage (no data loss). RDS Multi-AZ uses synchronous replication to standby (near-zero RPO). Aurora replicas = both read scaling AND HA failover candidates.' },
    tags: ['aurora', 'failover', 'high-availability', 'multi-az'],
  },
  {
    id: 'db-009',
    stem: 'A company needs to perform a complex database migration from SQL Server to Aurora PostgreSQL with minimal downtime. The database has 500 GB and the migration window is 2 hours. Which migration approach is appropriate?',
    type: 'single', difficulty: 3, topicSlug: 'databases', examDomain: 'migration-modernization',
    options: [
      { id: 'a', text: 'AWS SCT to convert schema + AWS DMS for full load + ongoing CDC replication', correct: true, explanation: 'SCT converts SQL Server schema to PostgreSQL. DMS performs initial full load (can run over days). Then CDC (Change Data Capture) replicates ongoing changes with minimal lag. During the 2-hour migration window, only the final CDC cutover is needed, not the full 500 GB load.' },
      { id: 'b', text: 'Take a SQL Server backup, restore to Aurora PostgreSQL via database import', correct: false, explanation: 'SQL Server backup format is incompatible with Aurora PostgreSQL. Schema and data must be converted and migrated with SCT/DMS.' },
      { id: 'c', text: 'Export to CSV via SSMS, then use AWS Glue to load into Aurora PostgreSQL', correct: false, explanation: 'CSV export of 500 GB and Glue ETL does not provide the CDC replication needed for near-zero downtime migration.' },
      { id: 'd', text: 'Snapshot the SQL Server EC2 instance and import to Aurora', correct: false, explanation: 'EC2 snapshots are disk images and cannot be directly imported into Aurora PostgreSQL. Also does not address schema conversion.' },
    ],
    explanation: { overall: 'Heterogeneous migration (SQL Server → Aurora PostgreSQL): Phase 1 - SCT: convert schema, stored procedures, and application code (manual effort needed). Phase 2 - DMS full load: migrate all data (days for 500 GB). Phase 3 - DMS CDC: capture ongoing changes. Phase 4 - Cutover: stop writes, flush CDC, switch connection strings.', examTip: 'DMS supports homogeneous (MySQL→MySQL) and heterogeneous (SQL Server→PostgreSQL) migrations. For heterogeneous, SCT is required first. DMS CDC requires: source DB in full recovery mode (SQL Server) or RDS with replication enabled.' },
    tags: ['dms', 'sct', 'migration', 'sql-server', 'aurora'],
  },
  {
    id: 'db-010',
    stem: 'A company uses DynamoDB with provisioned capacity. During peak events (flash sales), the table receives 10x normal traffic for 5 minutes. The provisioned capacity is insufficient for the spike, causing throttling. What is the most cost-effective solution?',
    type: 'single', difficulty: 2, topicSlug: 'databases', examDomain: 'cost-optimization',
    options: [
      { id: 'a', text: 'Switch to DynamoDB On-Demand capacity mode', correct: false, explanation: 'On-Demand handles spikes but is 6x more expensive than provisioned at sustained throughput. Cost-effective for very spiky, rare traffic but not optimal for mostly-steady traffic with occasional spikes.' },
      { id: 'b', text: 'Use DynamoDB Auto Scaling with burst capacity and a pre-warmed DAX cluster', correct: true, explanation: 'DynamoDB Auto Scaling adjusts provisioned capacity based on CloudWatch metrics. DAX (DynamoDB Accelerator) provides microsecond latency for cached reads, absorbing read spikes without hitting DynamoDB. DynamoDB burst capacity absorbs short spikes automatically.' },
      { id: 'c', text: 'Increase provisioned capacity permanently to handle peak traffic', correct: false, explanation: 'Permanently provisioning for peak wastes cost during normal traffic (90% of the time). Dynamic approaches are more cost-effective.' },
      { id: 'd', text: 'Add a CloudFront distribution in front of DynamoDB', correct: false, explanation: 'CloudFront cannot directly sit in front of DynamoDB — it works with HTTP/HTTPS origins like ALB, API Gateway, or custom servers.' },
    ],
    explanation: { overall: 'DynamoDB burst capacity: each partition retains up to 5 minutes of unused capacity that can be consumed during short spikes. Auto Scaling: adjusts provisioned capacity based on CloudWatch metrics (target 70% utilization). DAX: cache for read-heavy workloads, reduces RCU consumption. Combination handles flash sales cost-effectively.', examTip: 'DynamoDB throttling solutions: (1) Auto Scaling for sustained spikes, (2) Burst capacity for short spikes (<5 min), (3) DAX for read caching, (4) On-Demand for unpredictable traffic. For flash sales: DAX + Auto Scaling + burst capacity is most cost-effective.' },
    tags: ['dynamodb', 'auto-scaling', 'dax', 'throttling', 'burst-capacity'],
  },
  {
    id: 'db-011',
    stem: 'A company uses Amazon RDS PostgreSQL with automated backups. A database administrator accidentally drops a critical table. What is the fastest way to recover the dropped table?',
    type: 'single', difficulty: 2, topicSlug: 'databases', examDomain: 'continuous-improvement',
    options: [
      { id: 'a', text: 'Restore from the most recent automated snapshot (point-in-time)', correct: false, explanation: 'Restoring a full RDS snapshot creates a new DB instance and takes time proportional to database size. Also loses any data written after the last snapshot.' },
      { id: 'b', text: 'Use RDS Point-in-Time Recovery (PITR) to restore to 5 minutes before the drop to a new instance', correct: true, explanation: 'PITR restores the database to any second within the backup retention period. Restore to a new instance at T-5min, then export just the dropped table from the new instance and import into the production instance.' },
      { id: 'c', text: 'Use the Read Replica to recover the table since replicas are behind by a few seconds', correct: false, explanation: 'Read replicas replicate all operations including the DROP TABLE. The replica also dropped the table.' },
      { id: 'd', text: 'Restore from the daily automated backup and replay application logs', correct: false, explanation: 'Daily backup + log replay is complex and slower than PITR. PITR already uses the transaction logs for second-level precision.' },
    ],
    explanation: { overall: 'RDS PITR: restores to a new DB instance (does not overwrite the original). Continuous backup to S3 (transaction logs) enables second-level precision. Process: restore to new instance at time just before drop → export the specific table → import into production. This minimizes data loss and production downtime.', examTip: 'RDS PITR restores to a NEW instance (original is unchanged). You must then extract only what was lost and import it back. PITR window = backup retention period (default 7 days, max 35 days). Aurora PITR is especially fast due to storage architecture.' },
    tags: ['rds', 'pitr', 'backup-recovery', 'disaster-recovery'],
  },
  {
    id: 'db-012',
    stem: 'A company stores IoT time-series sensor data — 10,000 sensors each writing a data point every second. They need fast queries for recent data (last 24 hours) and the ability to aggregate data over months. Which AWS database is optimized for this use case?',
    type: 'single', difficulty: 2, topicSlug: 'databases', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Amazon DynamoDB with a time-based sort key', correct: false, explanation: 'DynamoDB can handle time-series but is not purpose-built for it. High write throughput and complex time-range aggregations add complexity without time-series-specific optimizations.' },
      { id: 'b', text: 'Amazon Timestream', correct: true, explanation: 'Timestream is purpose-built for time-series data. Automatically manages data tiering (recent data in memory, older in magnetic storage). Built-in time-series SQL functions (time_series, interpolate, bin, etc.). Scales to trillions of events per day.' },
      { id: 'c', text: 'Amazon Redshift with a timestamp column', correct: false, explanation: 'Redshift is a data warehouse optimized for analytical queries, not ingestion of high-frequency IoT writes. High write throughput would require micro-batch loading.' },
      { id: 'd', text: 'Amazon RDS PostgreSQL with TimescaleDB extension', correct: false, explanation: 'TimescaleDB on PostgreSQL is valid for time-series but requires managing the RDS instance and extension. Timestream is fully managed with native time-series capabilities.' },
    ],
    explanation: { overall: 'Amazon Timestream: serverless time-series database. Automatic tiered storage (hot: in-memory, warm: SSD, cold: S3). Built-in time-series analytics functions. Serverless: no cluster management. Integrates with IoT Core, Kinesis, Grafana. 1000x faster than RDS for time-series queries.', examTip: 'Timestream use cases: IoT telemetry, application metrics, clickstreams, financial data. Key features: automatic data tiering, time-series SQL functions (time_series, interpolate, bin by time), retention policies per tier, serverless.' },
    tags: ['timestream', 'iot', 'time-series', 'managed-database'],
  },
  {
    id: 'db-013',
    stem: 'A social network application needs to find connections between users (friends-of-friends), detect communities, and recommend connections. The data is highly relational with complex many-to-many relationships. Which database type is optimal?',
    type: 'single', difficulty: 2, topicSlug: 'databases', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Amazon RDS with JOIN queries for relationship traversal', correct: false, explanation: 'Recursive JOIN queries for finding friends-of-friends are exponentially expensive in relational databases as the network grows.' },
      { id: 'b', text: 'Amazon Neptune', correct: true, explanation: 'Neptune is a managed graph database supporting Gremlin (property graph) and SPARQL (RDF). Graph traversal queries (find all friends within 3 degrees, recommend by mutual friends) are native and efficient in graph databases.' },
      { id: 'c', text: 'Amazon DynamoDB with relationship tables', correct: false, explanation: 'DynamoDB can store relationships but does not support efficient graph traversal queries. Finding N-degree connections requires application-level recursion.' },
      { id: 'd', text: 'Amazon ElastiCache for quick relationship lookups', correct: false, explanation: 'ElastiCache is a cache, not a graph database. It cannot model or traverse complex relationship structures.' },
    ],
    explanation: { overall: 'Graph databases model entities as vertices and relationships as edges. Amazon Neptune supports: Gremlin (Apache TinkerPop, property graph), SPARQL (RDF/SPARQL 1.1), openCypher. Use cases: social networks, fraud detection, knowledge graphs, recommendation engines. Highly connected data traversal is efficient.', examTip: 'AWS database selection: RDS/Aurora = relational OLTP, DynamoDB = key-value/document/OLTP scale, Redshift = OLAP/DW, Neptune = graphs, Timestream = time-series, DocumentDB = MongoDB-compatible, ElastiCache = caching, MemoryDB = Redis persistent.' },
    tags: ['neptune', 'graph-database', 'social-network', 'relationships'],
  },
  {
    id: 'db-014',
    stem: 'A company wants to cache frequently accessed DynamoDB queries to reduce latency and cost. The cache must be query-level (not just item-level) and support millisecond response times. Which service provides this?',
    type: 'single', difficulty: 2, topicSlug: 'databases', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Amazon ElastiCache Redis in front of DynamoDB with application-managed caching logic', correct: false, explanation: 'ElastiCache Redis can cache at any granularity but requires application code to manage cache population, invalidation, and miss handling.' },
      { id: 'b', text: 'Amazon DAX (DynamoDB Accelerator)', correct: true, explanation: 'DAX is a fully managed, in-memory cache specifically for DynamoDB. Item-level and query-level caching. Microsecond latency. Fully DynamoDB API-compatible — no application code changes. Sits in front of DynamoDB transparently.' },
      { id: 'c', text: 'Amazon CloudFront caching DynamoDB responses', correct: false, explanation: 'CloudFront is an HTTP CDN and cannot directly cache DynamoDB API calls (which use the AWS SDK, not HTTP GET requests).' },
      { id: 'd', text: 'DynamoDB read replicas for read-heavy workloads', correct: false, explanation: 'DynamoDB does not have traditional read replicas (Global Tables are multi-active, not read-only replicas). DAX is the in-memory caching solution.' },
    ],
    explanation: { overall: 'DAX: write-through in-memory cache for DynamoDB. Item cache (specific items) and query cache (results of Query/Scan). SDK-compatible: swap DynamoDB client with DAX client with minimal code changes. Reduces read latency from single-digit ms to microseconds. Reduces DynamoDB RCU consumption.', examTip: 'DAX = DynamoDB-specific cache, SDK-compatible, write-through. ElastiCache = general-purpose cache, supports multiple data structures, pub/sub, Lua scripting. Use DAX when you want transparent DynamoDB caching; use ElastiCache for application-level caching with more control.' },
    tags: ['dax', 'dynamodb', 'caching', 'performance'],
  },
  {
    id: 'db-015',
    stem: 'A company needs a fully managed document database compatible with existing MongoDB drivers and tools. They want automatic backups, Multi-AZ, and scaling without managing infrastructure. Which AWS service should they use?',
    type: 'single', difficulty: 1, topicSlug: 'databases', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Amazon DynamoDB', correct: false, explanation: 'DynamoDB is a key-value/document database but does not use MongoDB API, drivers, or tools. Applications would need modifications.' },
      { id: 'b', text: 'Amazon DocumentDB (with MongoDB compatibility)', correct: true, explanation: 'DocumentDB is MongoDB-compatible. Existing MongoDB drivers, ODMs (Mongoose), and tools work without changes. Fully managed: automated backups, Multi-AZ, storage auto-scaling up to 128 TB, read replicas.' },
      { id: 'c', text: 'MongoDB Atlas on AWS', correct: false, explanation: 'MongoDB Atlas runs on AWS but is not an AWS-managed service. It uses a different billing model and management console.' },
      { id: 'd', text: 'Amazon Aurora with JSON columns', correct: false, explanation: 'Aurora supports JSON columns but requires SQL queries and is not MongoDB-compatible for document-oriented applications.' },
    ],
    explanation: { overall: 'DocumentDB with MongoDB compatibility: implements a subset of MongoDB 4.0 API. Supports common MongoDB operations, aggregation pipeline, and indexes. Emulation layer: not a direct fork of MongoDB. Some advanced MongoDB features may not be supported. Storage auto-scales to 128 TB in 10 GB increments.', examTip: 'DocumentDB compatibility: MongoDB 3.6, 4.0, 5.0 compatible API. Does not run MongoDB binary. Check compatibility matrix for unsupported features (e.g., some aggregation pipeline stages). Best for new apps or apps with standard MongoDB usage patterns.' },
    tags: ['documentdb', 'mongodb', 'document-database', 'managed-database'],
  },
  {
    id: 'db-016',
    stem: 'A company uses Amazon Aurora MySQL and needs to ensure that the database is highly available with an RPO of 0 data loss. The current Single-AZ setup lost data during a recent hardware failure. What configuration prevents data loss?',
    type: 'single', difficulty: 2, topicSlug: 'databases', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Enable Aurora Multi-AZ with at least one Aurora Replica in a different AZ', correct: true, explanation: 'Aurora Multi-AZ: storage is automatically replicated 6 ways across 3 AZs. A replica in another AZ provides both read scaling and rapid failover. Aurora\'s shared distributed storage means replicas are always current — zero data loss on failover.' },
      { id: 'b', text: 'Enable automated backups with a 1-hour backup window', correct: false, explanation: 'Backups provide point-in-time recovery but have a backup window gap — RPO = time since last backup. Does not achieve RPO of 0.' },
      { id: 'c', text: 'Enable Aurora Global Database for cross-region replication', correct: false, explanation: 'Aurora Global Database provides cross-region DR with RPO < 1 second (async replication). For single-region zero data loss, Multi-AZ within the same region is sufficient and simpler.' },
      { id: 'd', text: 'Enable RDS Multi-AZ deployment (not Aurora Multi-AZ)', correct: false, explanation: 'Aurora is the deployment type. Aurora uses a shared distributed storage (6 copies across 3 AZs), different from RDS Multi-AZ synchronous replication.' },
    ],
    explanation: { overall: 'Aurora Multi-AZ: storage layer automatically writes 6 copies across 3 AZs. Loss of 2 copies does not affect writes; loss of 3 copies does not affect reads. Aurora Replica in another AZ: fast failover (<30s), zero data loss (shared storage). This is essentially built into Aurora by default.', examTip: 'Aurora storage: always 6 copies across 3 AZs (built-in, always on). Aurora Replicas: add for read scaling AND faster failover. Aurora = built-in HA storage. Add a replica for sub-30s failover. No need for separate Multi-AZ configuration.' },
    tags: ['aurora', 'multi-az', 'rpo', 'high-availability'],
  },
  {
    id: 'db-017',
    stem: 'A company has a Redis-compatible ElastiCache cluster storing session data. A node failure causes session data loss. Which ElastiCache feature prevents data loss on node failure?',
    type: 'single', difficulty: 2, topicSlug: 'databases', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Enable ElastiCache Redis Multi-AZ with automatic failover', correct: false, explanation: 'Multi-AZ failover uses a replica that takes over when the primary fails. Data on the failed primary that was not replicated is lost. Does not provide RPO=0 for in-flight writes.' },
      { id: 'b', text: 'Enable Redis Cluster Mode with sharding across multiple nodes', correct: false, explanation: 'Cluster mode distributes data across shards but does not prevent data loss on a shard primary failure.' },
      { id: 'c', text: 'Enable Redis AOF (Append-Only File) persistence and use Amazon MemoryDB for Redis for mission-critical sessions', correct: true, explanation: 'AOF persistence logs every write to disk, enabling recovery. For mission-critical sessions with strict durability, Amazon MemoryDB for Redis stores data durably in a Multi-AZ transaction log — designed for durable in-memory data.' },
      { id: 'd', text: 'Use ElastiCache Redis snapshot (RDB) every 5 minutes', correct: false, explanation: 'RDB snapshots have an RPO equal to the snapshot interval (5 minutes of potential data loss). Not zero data loss.' },
    ],
    explanation: { overall: 'ElastiCache Redis persistence options: RDB (snapshots at intervals, higher RPO), AOF (every-write logging, near-zero RPO but performance impact). For zero-RPO durable Redis: use Amazon MemoryDB for Redis — built on Multi-AZ transaction log, data is immediately durable. ElastiCache is optimized for caching (where data loss is acceptable); MemoryDB for durable primary database use.', examTip: 'ElastiCache = caching, ephemeral, high performance. MemoryDB for Redis = durable, Redis-compatible PRIMARY database (Zero RPO). If the question asks about durable Redis or primary data store, choose MemoryDB. If caching with acceptable data loss, choose ElastiCache.' },
    tags: ['elasticache', 'redis', 'memorydb', 'durability', 'persistence'],
  },
  {
    id: 'db-018',
    stem: 'A company needs to run OLAP queries on a 10 TB dataset with complex joins and aggregations. The dataset is updated nightly. Query performance must support concurrent analytics users. Which AWS database service is designed for this workload?',
    type: 'single', difficulty: 1, topicSlug: 'databases', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Amazon Aurora MySQL for OLAP queries', correct: false, explanation: 'Aurora is optimized for OLTP workloads. Complex OLAP queries on a 10 TB dataset would be slow and consume significant IOPS.' },
      { id: 'b', text: 'Amazon Redshift', correct: true, explanation: 'Redshift is a columnar data warehouse designed for OLAP. Massively parallel processing (MPP), columnar storage, compression, and result caching deliver fast analytical query performance at any scale.' },
      { id: 'c', text: 'Amazon DynamoDB with Global Secondary Indexes', correct: false, explanation: 'DynamoDB is designed for OLTP with known access patterns. Complex multi-table joins and aggregations are not efficient in DynamoDB.' },
      { id: 'd', text: 'Amazon RDS PostgreSQL with materialized views', correct: false, explanation: 'PostgreSQL with materialized views can handle some OLAP but does not scale to petabyte-level analytics with many concurrent users efficiently.' },
    ],
    explanation: { overall: 'Redshift: columnar storage (reads only queried columns), MPP (parallel query across compute nodes), zone maps (skip irrelevant disk blocks), result caching (repeat queries instant). RA3 nodes: compute/storage separation with managed storage in S3. Redshift Serverless: no cluster management.', examTip: 'OLTP = RDS/Aurora/DynamoDB (high concurrency, row operations, indexes). OLAP = Redshift (bulk scans, aggregations, joins across large datasets). The workload type determines the right database choice.' },
    tags: ['redshift', 'olap', 'data-warehouse', 'analytics'],
  },
  {
    id: 'db-019',
    stem: 'A company uses Amazon RDS for MySQL. They want to implement blue-green deployments for schema changes without any downtime. The current blue environment receives all production traffic. Which RDS feature enables zero-downtime schema changes?',
    type: 'single', difficulty: 3, topicSlug: 'databases', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Create a read replica, apply schema change, promote to primary during maintenance window', correct: false, explanation: 'Promoting a read replica requires stopping replication, which creates a downtime window. Not zero-downtime.' },
      { id: 'b', text: 'RDS Blue/Green Deployments feature', correct: true, explanation: 'RDS Blue/Green Deployments creates a staging (green) environment as a copy of production (blue) with continuous logical replication. Apply schema changes to green. After testing, switch over — typically 1 minute or less with minimal data loss.' },
      { id: 'c', text: 'Use AWS Schema Conversion Tool to migrate the schema changes', correct: false, explanation: 'SCT converts schemas between different database engines. It does not provide blue-green deployment capabilities for same-engine schema changes.' },
      { id: 'd', text: 'Enable RDS Multi-AZ and perform schema changes on the standby', correct: false, explanation: 'Multi-AZ standby is synchronous replica for failover, not for applying changes independently. Changes applied to primary are immediately replicated to standby.' },
    ],
    explanation: { overall: 'RDS Blue/Green Deployments: creates an identical staging environment (green) with logical replication from production (blue). Deploy changes to green (schema migrations, engine upgrades). After validation, trigger switchover — RDS handles DNS cutover with sub-minute downtime. Supports MySQL, PostgreSQL, MariaDB.', examTip: 'RDS Blue/Green: logical replication keeps green in sync with blue. Switchover < 1 minute. Can also rollback to blue. Ideal for schema migrations, minor version upgrades, instance class changes. Launched 2022 — common SA Pro topic.' },
    tags: ['rds', 'blue-green', 'zero-downtime', 'schema-migration'],
  },
  {
    id: 'db-020',
    stem: 'A company runs AWS Glue ETL jobs that read from RDS and write to S3. The Glue job runs hourly and takes 45 minutes to complete. They want to reduce cost. Currently using Standard workers (G.1X). Which optimization would most reduce cost?',
    type: 'single', difficulty: 2, topicSlug: 'databases', examDomain: 'cost-optimization',
    options: [
      { id: 'a', text: 'Switch to AWS Glue Auto Scaling to use only the DPUs needed', correct: true, explanation: 'Glue Auto Scaling dynamically adjusts the number of workers based on actual data processing needs. For jobs where data volume varies, this can reduce DPU usage and cost significantly by not paying for unused workers.' },
      { id: 'b', text: 'Switch to G.2X workers for faster processing and lower total DPU-hours', correct: false, explanation: 'G.2X workers have twice the compute and memory per DPU but are not necessarily faster in all scenarios. Total cost depends on the job\'s actual bottleneck.' },
      { id: 'c', text: 'Use AWS Glue Flex execution class for non-urgent ETL jobs', correct: true, explanation: 'Glue Flex execution class uses spare capacity at 34% lower cost. Suitable for non-time-sensitive ETL jobs (batch reports, data lake loads). Jobs may start with a delay and have variable runtime.' },
      { id: 'd', text: 'Increase the number of workers to complete faster, reducing billable hours', correct: false, explanation: 'Glue pricing is per DPU-hour. More workers completing faster does not reduce total DPU-hours if the job is compute-bound. It would only help if the bottleneck is data parallelism.' },
    ],
    explanation: { overall: 'Glue cost optimizations: (1) Auto Scaling: right-size workers dynamically. (2) Flex class: 34% cheaper for non-urgent jobs, uses spare capacity. (3) G.1X vs G.2X: choose based on memory needs (G.2X for skewed data or memory-intensive transformations). (4) Partition pushdown: reduce data scanned from source.', examTip: 'Glue pricing: DPU-hours (number of workers × hours). Auto Scaling + Flex class = best cost reduction combo. G.1X = 4 vCPU, 16 GB. G.2X = 8 vCPU, 32 GB. Flex = non-critical batch, 34% cheaper but no SLA on start time.' },
    tags: ['glue', 'etl', 'cost-optimization', 'auto-scaling', 'flex'],
  },
];
