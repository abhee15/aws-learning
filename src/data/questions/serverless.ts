import type { Question } from '../../types/question';

export const serverlessQuestions: Question[] = [
  {
    id: 'sv-001',
    stem: 'A financial application uses Lambda functions behind API Gateway. During market open (9:30 AM ET), the first requests experience high latency due to cold starts, causing poor user experience. Which solution eliminates cold starts for this predictable peak?',
    type: 'single', difficulty: 2, topicSlug: 'serverless', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Configure Lambda Reserved Concurrency to 500 before market open', correct: false, explanation: 'Reserved Concurrency guarantees capacity allocation but does not pre-warm execution environments — cold starts still occur.' },
      { id: 'b', text: 'Use Lambda Provisioned Concurrency, activated via Application Auto Scaling schedule', correct: true, explanation: 'Provisioned Concurrency pre-initializes execution environments, eliminating cold starts. Scheduled Auto Scaling can scale up before 9:30 AM and scale back down after the peak.' },
      { id: 'c', text: 'Switch to container-based Lambda with a larger memory allocation', correct: false, explanation: 'Container-based Lambda images have longer cold start times, not shorter.' },
      { id: 'd', text: 'Enable X-Ray tracing to identify and optimize the cold start initialization code', correct: false, explanation: 'X-Ray can diagnose cold start duration but does not eliminate them.' },
    ],
    explanation: { overall: 'Lambda Provisioned Concurrency keeps execution environments initialized and warm. Combined with Application Auto Scaling scheduled actions, you can pre-warm before predictable peaks and scale down after.', examTip: 'Reserved Concurrency = reservation (no cold start elimination). Provisioned Concurrency = pre-warmed (cold start elimination, additional cost). Use scheduled scaling for predictable peaks.' },
    tags: ['lambda', 'cold-start', 'provisioned-concurrency', 'api-gateway'],
  },
  {
    id: 'sv-002',
    stem: 'A company builds a microservices architecture. Service A needs to call Service B synchronously. Service B processes 10,000 requests/second at peak. Lambda is used for both services. Service B experiences throttling. How should this be designed?',
    type: 'single', difficulty: 3, topicSlug: 'serverless', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Increase Service B Lambda reserved concurrency to 10,000', correct: false, explanation: 'Reserved concurrency of 10,000 reduces available concurrency for other functions. Lambda default account limit is 1,000 concurrent executions (adjustable). Reserving 10,000 may exceed account limits.' },
      { id: 'b', text: 'Add SQS queue between Service A and Service B to decouple and buffer requests; make Service B asynchronous', correct: true, explanation: 'If the synchronous requirement can be relaxed, SQS buffers 10,000/s requests. Service B Lambdas process at their own rate from the queue, avoiding throttling. Request concurrency is controlled by SQS batch size × Lambda reserved concurrency.' },
      { id: 'c', text: 'Use Lambda@Edge to distribute Service B globally and reduce per-region load', correct: false, explanation: 'Lambda@Edge runs at CloudFront edge locations for HTTP requests. It does not directly solve Lambda concurrency throttling for service-to-service calls.' },
      { id: 'd', text: 'Request a Service Quota increase for Lambda concurrent executions in the AWS account', correct: true, explanation: 'Lambda account-level concurrency limit (default 1,000, can be increased to tens of thousands) needs to be increased to support 10,000 concurrent executions. This is a necessary step along with proper architecture.' },
    ],
    explanation: { overall: 'For high-throughput Lambda workloads: (1) Request concurrency quota increase. (2) Decouple with SQS for non-synchronous calls. (3) Use reserved concurrency to allocate available concurrency to critical functions. (4) Monitor with CloudWatch Throttles metric.', examTip: 'Lambda throttling types: account-level throttle (total concurrency), function-level throttle (reserved concurrency), burst throttle (initial scaling limit per region). For 10k+ concurrent, request limit increase AND design for throttle handling (exponential backoff).' },
    tags: ['lambda', 'throttling', 'concurrency', 'sqs'],
  },
  {
    id: 'sv-003',
    stem: 'An API Gateway REST API is experiencing latency issues. The API calls an RDS database for most requests. 80% of requests are for the same few popular data records that rarely change. Which optimization reduces latency and database load?',
    type: 'single', difficulty: 2, topicSlug: 'serverless', examDomain: 'continuous-improvement',
    options: [
      { id: 'a', text: 'Enable API Gateway caching with a suitable TTL for GET responses', correct: true, explanation: 'API Gateway has a built-in response cache. GET requests with the same path+query parameters are cached for the TTL duration. Popular records are served from cache without Lambda or RDS invocation.' },
      { id: 'b', text: 'Switch from REST API to HTTP API to reduce latency', correct: false, explanation: 'HTTP API is cheaper and faster than REST API but does not have a response cache. The RDS bottleneck would remain.' },
      { id: 'c', text: 'Increase Lambda memory to maximum (10 GB) to reduce execution time', correct: false, explanation: 'Lambda memory affects CPU proportionally but RDS query time is the bottleneck, not Lambda compute.' },
      { id: 'd', text: 'Add a CloudFront distribution in front of API Gateway', correct: false, explanation: 'CloudFront + API Gateway caches at edge for public APIs. API Gateway caching is simpler for APIs not requiring edge distribution.' },
    ],
    explanation: { overall: 'API Gateway caching: TTL 0-3600 seconds (default 300s). Cache capacity 0.5 GB to 237 GB. Reduce or eliminate Lambda invocations for cached responses. Cache key: method + path + configured headers/query strings. Invalidation: Cache-Control: max-age=0 header with proper permissions.', examTip: 'API Gateway caching requires REST API (not HTTP API). Enable per-stage. Cache key includes path, headers, and query strings you configure. Per-client cache: use custom key (e.g., user ID header). Cached responses bypass Lambda entirely.' },
    tags: ['api-gateway', 'caching', 'lambda', 'performance'],
  },
  {
    id: 'sv-004',
    stem: 'A company uses API Gateway with Lambda integration. They want different behavior based on the caller: internal services use one Lambda version, external consumers use another. Both share the same API endpoint. Which API Gateway feature enables this?',
    type: 'single', difficulty: 2, topicSlug: 'serverless', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'API Gateway stages (dev/staging/prod) with different Lambda aliases per stage', correct: true, explanation: 'API Gateway stages can integrate with different Lambda aliases or versions. Internal callers use stage=internal (Lambda alias=internal), external callers use stage=external. Stage variables pass the alias name to the integration URI.' },
      { id: 'b', text: 'API Gateway usage plans to restrict external users to a subset of endpoints', correct: false, explanation: 'Usage plans control rate limits and quotas, not which Lambda version is invoked.' },
      { id: 'c', text: 'Two separate API Gateways with different Lambda integrations', correct: false, explanation: 'Two APIs would work but is operationally complex. Stage variables with Lambda aliases is more elegant.' },
      { id: 'd', text: 'API Gateway resource policies to route requests based on caller IP', correct: false, explanation: 'Resource policies control access (allow/deny) not routing to different Lambda versions.' },
    ],
    explanation: { overall: 'API Gateway stage variables: dynamic values configurable per stage (e.g., ${stageVariables.lambdaAlias}). Lambda integration ARN: arn:aws:lambda:...:function:myFunction:${stageVariables.lambdaAlias}. Internal stage: lambdaAlias=internal. External stage: lambdaAlias=prod. Lambda traffic shifting: use Lambda aliases with weighted routing for canary deployments.', examTip: 'Stage variables: environment-specific config without code changes. Use in Lambda integration ARNs, HTTP endpoints, mapping templates. Combined with Lambda aliases: enables stage-specific Lambda versions through a single API Gateway.' },
    tags: ['api-gateway', 'stages', 'lambda-aliases', 'stage-variables'],
  },
  {
    id: 'sv-005',
    stem: 'A company builds an event-driven architecture. A Lambda function processes events from multiple sources (S3, SQS, DynamoDB Streams). They need a centralized place to define event routing rules, filter events, and fan out to multiple targets. Which service is best suited?',
    type: 'single', difficulty: 2, topicSlug: 'serverless', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Amazon SNS with subscription filter policies', correct: false, explanation: 'SNS supports filter policies for pub/sub but is limited to attribute-based filtering and does not support complex event pattern matching or content-based routing.' },
      { id: 'b', text: 'Amazon EventBridge', correct: true, explanation: 'EventBridge is the event bus service with powerful content-based routing rules. Rules filter events by source, type, and content using JSON pattern matching. Routes to multiple targets (Lambda, SQS, SNS, Step Functions, etc.).' },
      { id: 'c', text: 'AWS Step Functions', correct: false, explanation: 'Step Functions orchestrates workflows with state machines. It does not provide event routing and fan-out for multi-source event streams.' },
      { id: 'd', text: 'Amazon Kinesis Data Streams', correct: false, explanation: 'Kinesis handles streaming data but routes all data to consumers without content-based filtering or fan-out to multiple different service types.' },
    ],
    explanation: { overall: 'EventBridge: event routing with content-based filtering (JSON pattern rules), fan-out to 5 targets per rule. Sources: AWS services, custom apps, SaaS. Schema registry for event discovery. Event Archives for replay. Pipes for direct point-to-point source-to-target connections.', examTip: 'EventBridge vs SNS: EventBridge = content-based routing, event archives, schema registry, SaaS integration. SNS = pub/sub, higher throughput, simpler. EventBridge is the modern event bus; SNS for simple notification fan-out.' },
    tags: ['eventbridge', 'event-driven', 'routing', 'fan-out'],
  },
  {
    id: 'sv-006',
    stem: 'A company builds a multi-step order processing workflow: validate → payment → fulfillment → notification. If payment fails, the order must be cancelled. The workflow must track state across multiple Lambda invocations. Which service orchestrates this?',
    type: 'single', difficulty: 2, topicSlug: 'serverless', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'SQS with separate queues per step and Lambda consumers', correct: false, explanation: 'SQS queues can chain steps but do not provide state management, branching logic, error handling, or workflow visualization.' },
      { id: 'b', text: 'AWS Step Functions Standard workflow', correct: true, explanation: 'Step Functions Standard: state machine with state persistence, error handling (Retry/Catch), branching (Choice state), parallel execution, and visual workflow editor. Each step invokes a Lambda. State persists for up to 1 year.' },
      { id: 'c', text: 'AWS Lambda with a DynamoDB table to track workflow state', correct: false, explanation: 'Custom state management in DynamoDB is possible but requires significant application code. Step Functions provides this out-of-the-box with error handling, retries, and observability.' },
      { id: 'd', text: 'Amazon MQ for message-based workflow orchestration', correct: false, explanation: 'Amazon MQ is a managed message broker (ActiveMQ/RabbitMQ). It is not a workflow orchestration service.' },
    ],
    explanation: { overall: 'Step Functions Standard: for long-running workflows (up to 1 year), audit history, human approval steps. Express: for high-throughput short-duration workflows (5 min max, 100k events/sec). Error handling: Retry (exponential backoff), Catch (go to error handler state). Branching: Choice state with conditions.', examTip: 'Step Functions Standard vs Express: Standard = long-running, audit trail, exactly-once, up to 1 year. Express = short-duration, high-volume, at-least-once. For order processing with compensation logic (saga pattern), use Standard with Catch/Retry.' },
    tags: ['step-functions', 'workflow', 'orchestration', 'saga'],
  },
  {
    id: 'sv-007',
    stem: 'A company deploys Lambda functions via API Gateway. Security requires that Lambda function code must not be accessible even if the AWS account is compromised. They want the function code stored encrypted with a key they control. Which approach provides this?',
    type: 'single', difficulty: 2, topicSlug: 'serverless', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Deploy Lambda from a private ECR registry', correct: false, explanation: 'ECR private repositories are access-controlled but encryption is with AWS-managed or customer-managed KMS keys. The function code itself in Lambda is encrypted at rest.' },
      { id: 'b', text: 'Enable Lambda function code signing and encrypt the deployment package with a KMS CMK', correct: true, explanation: 'Lambda functions can be encrypted at rest using KMS customer-managed keys. The CMK provides control over key access. Code signing with AWS Signer ensures only signed, unmodified code can be deployed.' },
      { id: 'c', text: 'Store function code in S3 with SSE-KMS encryption and deploy from there', correct: false, explanation: 'S3 stores the deployment package encrypted, but Lambda\'s own runtime environment uses AWS-managed encryption by default unless a CMK is specified.' },
      { id: 'd', text: 'Use Lambda SnapStart to encrypt the execution environment', correct: false, explanation: 'Lambda SnapStart reduces cold starts for Java functions by caching the initialized execution environment. It is not a security encryption feature.' },
    ],
    explanation: { overall: 'Lambda encryption at rest: by default, Lambda encrypts function code and environment variables with AWS-managed keys. To use CMK: specify the KMS key when creating/updating the function. Environment variable encryption: separate KMS key can be specified. Code signing: ensures deployed code matches what was signed by trusted authority.', examTip: 'Lambda security: (1) Function code encrypted at rest (CMK optional). (2) Environment variables encrypted at rest (CMK optional for additional encryption). (3) Code signing: prevents unsigned code deployment. (4) VPC: isolate Lambda in private network.' },
    tags: ['lambda', 'encryption', 'kms', 'code-signing', 'security'],
  },
  {
    id: 'sv-008',
    stem: 'A company uses Lambda functions to process DynamoDB Streams. During high-write periods, the DynamoDB Stream has millions of records per hour. Lambda is throttled and records in the stream are about to expire (24-hour retention). What should they do?',
    type: 'single', difficulty: 3, topicSlug: 'serverless', examDomain: 'continuous-improvement',
    options: [
      { id: 'a', text: 'Increase the DynamoDB Stream retention period to 7 days', correct: false, explanation: 'DynamoDB Streams have a fixed 24-hour retention period that cannot be extended.' },
      { id: 'b', text: 'Increase the number of shards in the DynamoDB Stream', correct: false, explanation: 'DynamoDB Stream shard count is determined by the number of DynamoDB table partitions. It cannot be independently scaled.' },
      { id: 'c', text: 'Increase Lambda reserved concurrency and batch size for the DynamoDB Streams trigger; use parallelization factor', correct: true, explanation: 'Lambda parallel processing: each shard can have up to 10 concurrent Lambda invocations (parallelization factor). Increase batch size (up to 10,000 records) to process more records per invocation. Request Lambda concurrency increase if needed.' },
      { id: 'd', text: 'Switch from DynamoDB Streams to Kinesis Data Streams for Lambda event source', correct: true, explanation: 'Kinesis Data Streams (used with DynamoDB Kinesis streaming) has a 7-day retention period, fan-out to multiple consumers, and Enhanced Fan-Out for higher throughput. More flexible than DynamoDB native streams.' },
    ],
    explanation: { overall: 'DynamoDB Streams Lambda processing: (1) Parallelization factor (1-10): multiple concurrent Lambda batches per shard. (2) Batch size (1-10,000): more records per invocation. (3) Bisect on error: split failing batches. (4) For longer retention: enable DynamoDB Kinesis Streaming (7-day retention, fan-out).', examTip: 'DynamoDB Streams: 24-hour retention, 2 consumers. DynamoDB Kinesis Streaming: 7-day retention, multiple consumers (EFO), no function code change needed. Parallelization factor doubles throughput per shard up to 10x.' },
    tags: ['dynamodb-streams', 'lambda', 'kinesis', 'parallelization'],
  },
  {
    id: 'sv-009',
    stem: 'A company builds an API with AWS Lambda and API Gateway. They want to protect the API from unauthorized access using JWT tokens issued by Amazon Cognito. Which API Gateway feature should they use?',
    type: 'single', difficulty: 2, topicSlug: 'serverless', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'API Gateway Resource Policy to whitelist Cognito user pools', correct: false, explanation: 'Resource policies control access by IP or account. They do not validate JWT tokens.' },
      { id: 'b', text: 'API Gateway JWT Authorizer (HTTP API) or Cognito User Pool Authorizer (REST API)', correct: true, explanation: 'HTTP API JWT Authorizer: natively validates JWT tokens (including Cognito-issued JWTs) without Lambda. REST API: Cognito User Pool Authorizer is a built-in authorizer that validates Cognito JWTs. Both reject requests with invalid/expired tokens.' },
      { id: 'c', text: 'AWS WAF on API Gateway to block requests without Authorization header', correct: false, explanation: 'WAF can check for header presence but cannot validate JWT token signatures or expiry. Authorizers are purpose-built for this.' },
      { id: 'd', text: 'Lambda Authorizer that calls Cognito to verify the token', correct: false, explanation: 'A Lambda Authorizer could verify Cognito JWTs but adds latency and cost. The native Cognito authorizer is simpler, faster, and free.' },
    ],
    explanation: { overall: 'API Gateway authorization options: (1) Cognito User Pool Authorizer (REST API): validates Cognito JWTs natively, no Lambda needed. (2) JWT Authorizer (HTTP API): validates JWTs from any OIDC/OAuth2 provider including Cognito. (3) Lambda Authorizer: custom auth logic (API keys, OAuth opaque tokens, LDAP). (4) IAM: AWS SigV4 signed requests.', examTip: 'Cognito + API Gateway: REST API uses Cognito User Pool Authorizer. HTTP API uses JWT Authorizer. HTTP API is cheaper and lower latency. Use REST API for advanced features (request/response transformation, API caching). JWT Authorizer: no extra cost, built into API Gateway.' },
    tags: ['api-gateway', 'cognito', 'jwt', 'authorization'],
  },
  {
    id: 'sv-010',
    stem: 'A company runs Lambda functions that process messages from SQS. Some messages cause Lambda to fail (poison pill messages). The function retries 3 times and still fails. What is the best way to handle failed messages?',
    type: 'single', difficulty: 2, topicSlug: 'serverless', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Set SQS visibility timeout to 0 to immediately discard failed messages', correct: false, explanation: 'Zero visibility timeout makes messages immediately visible to other consumers. This causes aggressive retrying by other Lambda instances, not discarding.' },
      { id: 'b', text: 'Configure a Dead Letter Queue (DLQ) on the SQS queue; Lambda moves messages to DLQ after maxReceiveCount retries', correct: true, explanation: 'SQS DLQ: after a message is received maxReceiveCount times without successful processing, SQS moves it to the DLQ. Lambda does not need to explicitly send to DLQ. DLQ messages can be inspected, reprocessed, or discarded.' },
      { id: 'c', text: 'Configure Lambda DLQ for the SQS event source mapping', correct: false, explanation: 'Lambda DLQ (not to be confused with SQS DLQ) captures async invocation failures (S3, SNS, EventBridge), not SQS poll-based failures. SQS DLQ handles message-level failures.' },
      { id: 'd', text: 'Increase Lambda timeout to give more processing time for complex messages', correct: false, explanation: 'Timeout increase only helps if processing takes longer than the timeout. Poison pill messages fail for logic reasons, not timeout.' },
    ],
    explanation: { overall: 'SQS DLQ: configured on the source queue with a redrive policy (maxReceiveCount). After N failed receives, SQS moves the message to DLQ. Monitor DLQ with CloudWatch alarm for ApproximateNumberOfMessagesVisible. Process DLQ messages separately (inspect, fix, requeue or discard).', examTip: 'SQS DLQ handles failed message processing. Lambda DLQ handles failed async invocations. Lambda SQS Event Source Mapping has additional: bisect-on-error (split batch) and maximum retry attempts settings. Always configure DLQ in production.' },
    tags: ['sqs', 'dlq', 'lambda', 'error-handling', 'poison-pill'],
  },
  {
    id: 'sv-011',
    stem: 'A media company processes video uploads using Lambda with container images. The Lambda function needs 15 GB of memory and an execution timeout of 15 minutes to process large videos. They deploy 50 new videos per day with 100 MB average size. What limits should they be aware of?',
    type: 'multiple', difficulty: 2, topicSlug: 'serverless', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Lambda maximum execution timeout is 15 minutes', correct: true, explanation: 'Lambda maximum timeout is 900 seconds (15 minutes). Video processing must complete within this window.' },
      { id: 'b', text: 'Lambda maximum memory is 10 GB (10,240 MB)', correct: true, explanation: 'Lambda maximum memory allocation is 10,240 MB (10 GB), not 15 GB. 15 GB would exceed Lambda limits.' },
      { id: 'c', text: 'Lambda container images can be up to 10 GB in size', correct: true, explanation: 'Lambda container images can be up to 10 GB, enabling large dependency packages (ML libraries, media processing tools).' },
      { id: 'd', text: 'Lambda /tmp ephemeral storage is limited to 512 MB by default', correct: false, explanation: '/tmp storage is configurable up to 10 GB (default 512 MB, adjustable). For 100 MB video processing, even default 512 MB is sufficient.' },
    ],
    explanation: { overall: 'Lambda limits: memory 128 MB - 10,240 MB. Timeout: 1s - 900s (15 min). Deployment package: 50 MB (zipped), 250 MB (unzipped), 10 GB (container). /tmp: 512 MB - 10 GB. CPU scales with memory (1 vCPU at 1,769 MB). For heavy video processing >15 min, consider AWS Elemental MediaConvert or ECS Fargate.', examTip: 'Lambda limits for SA Pro: Max memory = 10,240 MB. Max timeout = 15 min. Max payload = 6 MB sync / 256 KB async. Container = 10 GB. /tmp = up to 10 GB. Concurrency = 1,000 default (per region, per account).' },
    tags: ['lambda', 'limits', 'container', 'memory', 'timeout'],
  },
  {
    id: 'sv-012',
    stem: 'A company wants to expose an existing on-premises REST API through AWS API Gateway without re-hosting it. API Gateway should proxy requests to the on-premises endpoint. The on-premises datacenter is connected via Direct Connect. Which API Gateway integration type achieves this?',
    type: 'single', difficulty: 2, topicSlug: 'serverless', examDomain: 'migration-modernization',
    options: [
      { id: 'a', text: 'Lambda integration: Lambda calls the on-premises API', correct: false, explanation: 'Lambda can call on-premises APIs via VPC (with DX), but adds an extra hop and Lambda cold start latency. HTTP integration is more direct.' },
      { id: 'b', text: 'HTTP integration (HTTP_PROXY or HTTP) with the on-premises endpoint URL via VPC Link', correct: true, explanation: 'VPC Link connects API Gateway to resources in a VPC (and via DX to on-premises). HTTP_PROXY passes the request directly to the on-premises endpoint. No transformation needed.' },
      { id: 'c', text: 'AWS integration with on-premises service', correct: false, explanation: 'AWS integration type is for calling AWS services (S3, DynamoDB, etc.), not on-premises HTTP endpoints.' },
      { id: 'd', text: 'Mock integration to simulate the on-premises API response', correct: false, explanation: 'Mock integration returns a hardcoded response without calling any backend — for testing only.' },
    ],
    explanation: { overall: 'VPC Link: connects REST API Gateway to NLB in a VPC. NLB routes to on-premises via DX or VPN. HTTP_PROXY: API Gateway passes request/response without transformation. HTTP: API Gateway can transform request/response. For proxying on-premises, HTTP_PROXY via VPC Link is the simplest.', examTip: 'API Gateway integration types: HTTP/HTTP_PROXY (external HTTP endpoints), Lambda/Lambda_PROXY, AWS (AWS services), Mock. VPC Link: required for private NLB/ALB (in VPC) as backend. HTTP_PROXY = pass-through (no transformation). HTTP = with optional request/response mapping.' },
    tags: ['api-gateway', 'vpc-link', 'on-premises', 'hybrid', 'direct-connect'],
  },
  {
    id: 'sv-013',
    stem: 'A company uses AWS Lambda and needs to access secrets from AWS Secrets Manager. The Lambda function is invoked thousands of times per second. They want to avoid hitting Secrets Manager API rate limits. What is the best practice?',
    type: 'single', difficulty: 2, topicSlug: 'serverless', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Call Secrets Manager on every Lambda invocation', correct: false, explanation: 'Calling Secrets Manager on every invocation hits API rate limits and adds 20-100ms latency per call.' },
      { id: 'b', text: 'Use Lambda environment variables to store secret values', correct: false, explanation: 'Lambda environment variables are in plaintext in the Lambda configuration (encrypted at rest but not best practice for secrets). Secrets cannot be rotated without redeploying Lambda.' },
      { id: 'c', text: 'Use the AWS Secrets Manager Lambda extension to cache secrets in the Lambda execution environment', correct: true, explanation: 'The AWS Parameters and Secrets Lambda Extension caches secrets within the Lambda execution environment. Secrets are retrieved via a local HTTP endpoint (no AWS SDK call needed). Cache TTL is configurable. Minimizes Secrets Manager API calls.' },
      { id: 'd', text: 'Store secrets in S3 and read at Lambda cold start', correct: false, explanation: 'S3 is not a secrets management service. S3 objects can be read by anyone with S3 permissions. Secrets Manager provides rotation, versioning, and fine-grained IAM control.' },
    ],
    explanation: { overall: 'AWS Parameters and Secrets Lambda Extension: caches SSM Parameter Store and Secrets Manager values locally in the Lambda execution environment. Access via HTTP GET to localhost:2773/secretsmanager/get... Cache TTL configurable. Reduces API calls by 99%+ for warm Lambda invocations.', examTip: 'Lambda extensions: run in the same execution environment as the function. Parameters and Secrets extension: local HTTP cache. X-Ray extension: async telemetry flush. Extensions add to init time but reduce function latency for subsequent calls.' },
    tags: ['secrets-manager', 'lambda', 'extension', 'caching', 'security'],
  },
  {
    id: 'sv-014',
    stem: 'A company is building a real-time auction platform. When a user bids, the bid must be validated, inventory checked, competing bids notified, and payment authorized. All steps must complete within 3 seconds or the bid fails. Which architecture handles this?',
    type: 'single', difficulty: 3, topicSlug: 'serverless', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Step Functions Standard workflow with each step as a Lambda function', correct: false, explanation: 'Step Functions Standard has overhead per state transition. Complex workflows with many states may not complete within 3 seconds SLA.' },
      { id: 'b', text: 'Single Lambda function orchestrating all steps synchronously', correct: false, explanation: 'A single monolithic Lambda increases timeout risk and makes individual step retries and error handling complex.' },
      { id: 'c', text: 'Step Functions Express workflow with synchronous execution using optimized state machine', correct: true, explanation: 'Step Functions Express (synchronous) supports high-throughput (100k events/sec), up to 5-minute duration, and can orchestrate Lambda steps with sub-second transitions. Synchronous Express returns result inline to the caller.' },
      { id: 'd', text: 'EventBridge Pipes with multiple targets in sequence', correct: false, explanation: 'EventBridge Pipes connects source to target but does not orchestrate multi-step sequential workflows with error handling and conditional logic.' },
    ],
    explanation: { overall: 'Step Functions Express (Synchronous): caller waits for workflow completion (up to 5 min). High throughput (100k executions/sec). Lower cost than Standard. Ideal for synchronous API-backed workflows needing orchestration. At-least-once execution (Standard is exactly-once for long-running).', examTip: 'Step Functions Express Synchronous: the API Gateway → Express workflow → Lambda chain is a common SA Pro pattern for synchronous multi-step processing. Express = high throughput, short duration, at-least-once, no audit history. Standard = long-running, exactly-once, full history.' },
    tags: ['step-functions', 'express', 'synchronous', 'orchestration', 'real-time'],
  },
  {
    id: 'sv-015',
    stem: 'A company uses API Gateway (REST) to expose Lambda functions. They want to protect the API from being called by unauthorized AWS accounts. They want to allow only their organization\'s AWS accounts. Which mechanism achieves this?',
    type: 'single', difficulty: 2, topicSlug: 'serverless', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'API Gateway usage plan with API keys for each account', correct: false, explanation: 'API keys are for identifying clients and metering usage. They are not a security mechanism for restricting to specific AWS accounts.' },
      { id: 'b', text: 'API Gateway resource policy allowing principals from specific AWS accounts', correct: true, explanation: 'Resource policies on API Gateway can specify which AWS accounts (principals) can invoke the API using IAM authentication (aws:PrincipalAccount or aws:PrincipalOrgID condition). Combined with IAM authentication, only allowed accounts can call the API.' },
      { id: 'c', text: 'API Gateway client certificate mutual TLS authentication', correct: false, explanation: 'Mutual TLS validates certificate-based authentication between client and API, not AWS account-level restrictions.' },
      { id: 'd', text: 'AWS WAF rule on API Gateway blocking unauthorized source IPs', correct: false, explanation: 'WAF blocks based on IPs, not AWS account identity. AWS account identity requires IAM auth + resource policy.' },
    ],
    explanation: { overall: 'API Gateway resource policy + IAM authentication: resource policy can use aws:PrincipalOrgID to restrict to Organization accounts, or aws:PrincipalAccount for specific accounts. The calling account must sign requests with SigV4 (IAM auth). Combined: only accounts in the Org can invoke the API.', examTip: 'API Gateway auth hierarchy: IAM auth + resource policy for cross-account. Resource policy alone does not authenticate — it restricts access for IAM-authenticated callers. aws:PrincipalOrgID in resource policy = Organization-level restriction.' },
    tags: ['api-gateway', 'resource-policy', 'organizations', 'iam-auth', 'cross-account'],
  },
  {
    id: 'sv-016',
    stem: 'A company uses Lambda for data processing. The Lambda function writes results to DynamoDB. During high load, DynamoDB throttles PUT requests causing Lambda retries and increased duration. What is the recommended pattern to handle DynamoDB throughput limits?',
    type: 'single', difficulty: 2, topicSlug: 'serverless', examDomain: 'continuous-improvement',
    options: [
      { id: 'a', text: 'Implement exponential backoff with jitter in the Lambda function for DynamoDB writes', correct: true, explanation: 'AWS SDK automatically retries with exponential backoff for throttled DynamoDB requests. Jitter adds randomness to prevent thundering herd. This is already built into AWS SDKs but can be tuned.' },
      { id: 'b', text: 'Use DynamoDB Transactions to batch writes and reduce API calls', correct: false, explanation: 'DynamoDB Transactions atomically write multiple items but use 2x WCUs. They reduce API calls only if multiple writes can be batched in a transaction — they do not reduce WCU consumption overall.' },
      { id: 'c', text: 'Use DynamoDB BatchWriteItem to batch up to 25 writes per API call', correct: true, explanation: 'BatchWriteItem sends up to 25 PutItem/DeleteItem in a single API call. Reduces API overhead and Lambda execution time. If some items are throttled, they are returned as unprocessed items for retry.' },
      { id: 'd', text: 'Enable DynamoDB Auto Scaling to adjust capacity when Lambda writes increase', correct: true, explanation: 'DynamoDB Auto Scaling adjusts WCU/RCU based on CloudWatch metrics. Helps with sustained increased load from Lambda. Target utilization (default 70%) allows burst headroom.' },
    ],
    explanation: { overall: 'Handling DynamoDB throttling from Lambda: (1) BatchWriteItem reduces API call count. (2) Exponential backoff with jitter (built into SDK) handles transient throttling. (3) Auto Scaling handles sustained increased load. (4) DAX or caching reduces read load. Monitor with DynamoDB SuccessfulRequestLatency and ConsumedWriteCapacityUnits.', examTip: 'DynamoDB write efficiency: BatchWriteItem = 25 items per call. TransactWriteItems = 100 items, atomically, 2x WCU. PutItem = 1 item. For high-throughput Lambda → DynamoDB: batch writes + exponential backoff + auto scaling.' },
    tags: ['dynamodb', 'lambda', 'throttling', 'batch-write', 'exponential-backoff'],
  },
  {
    id: 'sv-017',
    stem: 'A company wants to build a WebSocket-based real-time notification system where the server can push messages to connected clients. Multiple Lambda functions need to push to specific connections. Which AWS service provides managed WebSocket support?',
    type: 'single', difficulty: 2, topicSlug: 'serverless', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Application Load Balancer with WebSocket support', correct: false, explanation: 'ALB supports WebSocket connections for long-running backends but does not provide serverless WebSocket management or the ability to push to specific connection IDs.' },
      { id: 'b', text: 'API Gateway WebSocket API with connection management callbacks', correct: true, explanation: 'API Gateway WebSocket API: manages persistent WebSocket connections. $connect/$disconnect/$default routes. Lambda handles events. Any Lambda can push to a connection by calling the API Gateway Management API with the connectionId.' },
      { id: 'c', text: 'AWS AppSync with real-time subscriptions (GraphQL)', correct: false, explanation: 'AppSync provides real-time GraphQL subscriptions which is great for data sync, but WebSocket API in API Gateway is more flexible for custom real-time protocols.' },
      { id: 'd', text: 'Amazon SNS Mobile Push Notifications', correct: false, explanation: 'SNS mobile push sends push notifications to mobile devices (APNs, FCM), not browser WebSocket connections.' },
    ],
    explanation: { overall: 'API Gateway WebSocket API: serverless WebSocket management. Routes: $connect (client connects), $disconnect (client disconnects), custom routes (messages). Lambda handlers for each route. Push to client: Lambda calls wss://execute-api.../connectionId with AmazonApiGatewayManagementApi. Connection IDs stored in DynamoDB for multi-function push.', examTip: 'WebSocket API pattern: $connect → Lambda stores connectionId in DynamoDB. Message → Lambda looks up connectionId, calls Management API to push. $disconnect → Lambda removes from DynamoDB. This serverless WebSocket pattern is a common SA Pro pattern.' },
    tags: ['api-gateway', 'websocket', 'real-time', 'push-notifications'],
  },
  {
    id: 'sv-018',
    stem: 'A company wants to throttle API access to their Lambda-backed API. Premium users can make 1000 requests/minute; free users can make 100 requests/minute. Which API Gateway feature enables per-user rate limiting?',
    type: 'single', difficulty: 2, topicSlug: 'serverless', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'API Gateway stage-level throttling (default throttling)', correct: false, explanation: 'Stage-level throttling applies a single rate limit to all callers on a stage. It cannot differentiate between user tiers.' },
      { id: 'b', text: 'API Gateway Usage Plans with API keys per user tier', correct: true, explanation: 'Usage Plans: define rate limits (requests/second) and burst limits per plan. Associate API keys with plans. Premium users get premium plan keys (1000 RPM); free users get free plan keys (100 RPM). API Gateway enforces limits per key.' },
      { id: 'c', text: 'Lambda Authorizer that tracks request count per user in DynamoDB', correct: false, explanation: 'Custom rate limiting in a Lambda Authorizer is possible but complex and adds latency. Usage Plans provide built-in rate limiting without application code.' },
      { id: 'd', text: 'AWS WAF with rate-based rules per IP address', correct: false, explanation: 'WAF rate-based rules throttle by IP (not by user tier). An IP may have multiple users; a user may have multiple IPs.' },
    ],
    explanation: { overall: 'Usage Plans + API Keys: Usage plan defines throttle (rate limit + burst) and quota (daily/weekly/monthly request count). API keys are issued to users and associated with usage plans. API Gateway enforces limits per key. Can use different plans for free/pro/enterprise tiers.', examTip: 'Usage Plans = billing and rate limiting for API consumers. Not a security mechanism (API keys are not auth tokens). Combine with Cognito or Lambda Authorizer for actual authentication. Usage Plans provide business-level access control.' },
    tags: ['api-gateway', 'usage-plans', 'throttling', 'rate-limiting', 'api-keys'],
  },
  {
    id: 'sv-019',
    stem: 'A company uses Lambda functions to process images uploaded to S3. Lambda downloads the image from S3, processes it, and writes results to S3. Lambda runs in a VPC for security. S3 access is failing. What is the most likely cause and solution?',
    type: 'single', difficulty: 2, topicSlug: 'serverless', examDomain: 'continuous-improvement',
    options: [
      { id: 'a', text: 'Lambda in VPC needs a NAT Gateway to reach S3 over the internet', correct: false, explanation: 'Routing through NAT Gateway to reach S3 is possible but expensive and not optimal. VPC Endpoint is better.' },
      { id: 'b', text: 'Lambda in VPC cannot access S3 by default — create a VPC Gateway Endpoint for S3', correct: true, explanation: 'Lambda in VPC loses its default internet access (no public IP). S3 is a public service endpoint. VPC Gateway Endpoint for S3 adds a route table entry routing S3 traffic privately without internet — free, no data transfer charges.' },
      { id: 'c', text: 'Lambda needs a public IP address to access S3', correct: false, explanation: 'Lambda in VPC does not have a public IP. Assigning public IPs to Lambda in VPC is not possible. Use Gateway Endpoint or NAT Gateway.' },
      { id: 'd', text: 'Lambda function needs additional IAM permissions for S3 access when in VPC', correct: false, explanation: 'IAM permissions are the same regardless of VPC. The issue is network connectivity, not authorization.' },
    ],
    explanation: { overall: 'Lambda in VPC: all traffic goes through the VPC. No internet access unless you add NAT Gateway. S3 is a public endpoint but accessible privately via Gateway Endpoint (free). ECR, SSM, Secrets Manager need Interface Endpoints. For Lambda in VPC: always add Gateway Endpoints for S3 and DynamoDB, Interface Endpoints for other AWS services.', examTip: 'Lambda in VPC loses internet access. Needs NAT GW for internet or VPC Endpoints for AWS services. Gateway Endpoints (S3, DynamoDB): free, route-table based. Interface Endpoints (everything else): hourly cost. Must be in same VPC/subnet as Lambda.' },
    tags: ['lambda', 'vpc', 'vpc-endpoint', 'gateway-endpoint', 's3'],
  },
  {
    id: 'sv-020',
    stem: 'A company wants to run serverless containers that can scale to zero when there is no traffic. Lambda has a 15-minute timeout which is insufficient for their workloads. ECS Fargate with Auto Scaling has a minimum of 1 task running. What alternative AWS service meets these requirements?',
    type: 'single', difficulty: 2, topicSlug: 'serverless', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'AWS Fargate with KEDA (Kubernetes Event-Driven Autoscaling)', correct: false, explanation: 'KEDA is used with EKS, not standalone Fargate. ECS Fargate with service Auto Scaling does support scale-to-zero but requires careful configuration.' },
      { id: 'b', text: 'AWS App Runner', correct: true, explanation: 'App Runner scales from 0 to N instances automatically based on traffic. Runs containerized applications. Handles HTTP/HTTPS workloads. No cluster management. Supports long-running processes unlike Lambda.' },
      { id: 'c', text: 'Lambda with container images and 15-minute timeout', correct: false, explanation: 'Lambda maximum timeout is 15 minutes regardless of whether container images are used.' },
      { id: 'd', text: 'ECS Fargate with scheduled scaling to zero during off-hours', correct: false, explanation: 'Scheduled scaling reduces cost during predictable periods but not for dynamic traffic-based scale-to-zero.' },
    ],
    explanation: { overall: 'App Runner: fully managed container service. No servers to manage. Scales from 0 instances (pause when no traffic) to N. HTTP/HTTPS workloads. Continuous deployment from ECR or GitHub. Use for: web applications, APIs, microservices that need serverless experience with container flexibility.', examTip: 'App Runner vs Lambda: App Runner = container-based, no timeout, auto-scaling from 0, simpler than ECS. Lambda = event-driven, 15-min max, pay-per-invocation, cold starts. App Runner vs ECS: App Runner = simpler, fewer controls. ECS = full control over networking, task placement, sidecar containers.' },
    tags: ['app-runner', 'containers', 'scale-to-zero', 'serverless-containers'],
  },
];
