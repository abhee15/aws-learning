import type { Question } from '../../types/question';

export const messagingQuestions: Question[] = [
  {
    id: 'msg-001',
    stem: 'An application uses SQS to buffer messages before processing by Lambda. During testing, messages are being processed multiple times, causing duplicate records in the database. What is the most likely cause and solution?',
    type: 'single', difficulty: 3, topicSlug: 'messaging', examDomain: 'continuous-improvement',
    options: [
      { id: 'a', text: 'The SQS visibility timeout is shorter than the Lambda function execution time', correct: true, explanation: 'If the visibility timeout expires before Lambda finishes processing, SQS makes the message visible again for reprocessing. The visibility timeout must be >= Lambda timeout.' },
      { id: 'b', text: 'The SQS queue is not configured as FIFO', correct: false, explanation: 'FIFO queues provide exactly-once processing within the deduplication window but are not the primary cause here.' },
      { id: 'c', text: 'Lambda is deployed with insufficient reserved concurrency', correct: false, explanation: 'Insufficient concurrency causes throttling/delays but not duplicate processing.' },
      { id: 'd', text: 'The message retention period is too short', correct: false, explanation: 'Message retention affects how long messages stay in the queue, not duplicate processing.' },
    ],
    explanation: { overall: 'SQS visibility timeout: defines how long a message is hidden after being picked up. If Lambda takes longer, SQS re-enqueues the message. Best practice: set SQS visibility timeout to at least 6x the Lambda function timeout, and implement idempotent processing.', examTip: 'SQS visibility timeout >= Lambda function timeout (ideally 6x). Always implement idempotent consumers for SQS — at-least-once delivery means you WILL occasionally get duplicates.' },
    tags: ['sqs', 'lambda', 'visibility-timeout', 'idempotency'],
  },
  {
    id: 'msg-002',
    stem: 'A company has a fan-out pattern where a single order event must be sent to multiple downstream services: inventory, billing, notification, and analytics. Each service must receive every event. Which architecture achieves reliable fan-out?',
    type: 'single', difficulty: 2, topicSlug: 'messaging', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'One SQS queue with all four services polling from it', correct: false, explanation: 'A single SQS queue delivers each message to only ONE consumer. The first service to read the message gets it. Other services do not receive it.' },
      { id: 'b', text: 'SNS topic with SQS queue subscriptions for each service', correct: true, explanation: 'SNS fan-out: publish once to SNS. SNS delivers to all subscribed SQS queues (one per service). Each service gets its own durable queue, decoupled from other services. Dead letter queues per service for failures.' },
      { id: 'c', text: 'Multiple SQS queues with the publisher sending to each queue separately', correct: false, explanation: 'Publishing to multiple queues creates tight coupling and requires the publisher to know all consumers. Also fails if one queue write fails.' },
      { id: 'd', text: 'Amazon Kinesis Data Stream with 4 consumers using Enhanced Fan-Out', correct: false, explanation: 'Kinesis EFO provides dedicated throughput per consumer but requires all consumers to read from specific shards. For simple fan-out of discrete events, SNS+SQS is simpler.' },
    ],
    explanation: { overall: 'SNS → SQS fan-out: publish once to SNS. SNS delivers to all subscribed SQS queues simultaneously. Benefits: durability (SQS queues), per-consumer rate limiting, DLQ per service, independent service processing. This is the standard fan-out pattern.', examTip: 'Fan-out patterns: SNS+SQS (pub/sub with durable queues), EventBridge (content-based routing rules), Kinesis EFO (streaming fan-out with ordered records). SNS+SQS = simplest fan-out. EventBridge = content-based routing. Kinesis = ordered streaming.' },
    tags: ['sns', 'sqs', 'fan-out', 'pub-sub'],
  },
  {
    id: 'msg-003',
    stem: 'A financial services company processes transactions that must be processed in the exact order they are received. Each transaction ID must be processed exactly once. Which SQS queue type meets these requirements?',
    type: 'single', difficulty: 1, topicSlug: 'messaging', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'SQS Standard Queue', correct: false, explanation: 'SQS Standard Queue: at-least-once delivery (possible duplicates), best-effort ordering (not guaranteed). Does not meet strict ordering or exactly-once requirements.' },
      { id: 'b', text: 'SQS FIFO Queue with message deduplication', correct: true, explanation: 'FIFO: exactly-once processing (within 5-minute deduplication window), strict ordering. MessageDeduplicationId prevents duplicate processing. MessageGroupId controls ordering per group.' },
      { id: 'c', text: 'SQS Standard Queue with application-level deduplication logic', correct: false, explanation: 'Application-level deduplication (checking a database before processing) can work but adds complexity. FIFO provides this natively.' },
      { id: 'd', text: 'Amazon Kinesis Data Streams with sequence numbers', correct: false, explanation: 'Kinesis provides ordered streaming with sequence numbers but is not a queue service and does not provide exactly-once semantics natively.' },
    ],
    explanation: { overall: 'SQS FIFO: ordering within a MessageGroupId. Deduplication: if same MessageDeduplicationId sent within 5 minutes, second message is ignored. Throughput: 300 msg/sec per API action (3,000 with batching). High Throughput mode: 30,000 msg/sec. Supports Lambda and EC2 consumers.', examTip: 'SQS Standard vs FIFO: Standard = higher throughput (unlimited), at-least-once, best-effort order. FIFO = exactly-once (5-min dedup window), strict order within group, 3,000 msg/sec (high-throughput: 30,000). Financial transactions = FIFO.' },
    tags: ['sqs-fifo', 'ordering', 'exactly-once', 'deduplication'],
  },
  {
    id: 'msg-004',
    stem: 'A company uses Amazon SNS to send notifications. They need to send customized messages to different subscriber types: mobile push (iOS), email subscribers, and SQS queue subscribers. The mobile message should be JSON with "aps" key. Email should be plain text. SQS should receive the full JSON. How is this configured?',
    type: 'single', difficulty: 2, topicSlug: 'messaging', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Publish three separate SNS messages with different content for each subscriber type', correct: false, explanation: 'Publishing three separate messages is complex. SNS message structure per protocol eliminates this need.' },
      { id: 'b', text: 'Use SNS message structure per protocol (MessageStructure=json) with different message bodies per protocol', correct: true, explanation: 'SNS MessageStructure=json: publish a JSON body with keys for each protocol (apns, email, sqs, default). SNS delivers the protocol-specific content to each subscriber type automatically.' },
      { id: 'c', text: 'Create three separate SNS topics for each subscriber type', correct: false, explanation: 'Three topics require three separate publish calls. Message structure per protocol achieves this with a single publish.' },
      { id: 'd', text: 'Use SNS filter policies to route to different subscribers', correct: false, explanation: 'Filter policies control which messages subscribers receive based on attributes. They do not provide protocol-specific message content transformation.' },
    ],
    explanation: { overall: 'SNS MessageStructure=json: Message body is a JSON object with protocol keys. Keys: "default", "email", "sqs", "http", "apns", "gcm", "lambda". SNS delivers the matching protocol\'s value to each subscriber. "default" used if no protocol-specific key exists.', examTip: 'SNS protocols: HTTP/HTTPS, Email, Email-JSON, SQS, Lambda, Application (mobile push), SMS, Firehose. Message structure allows per-protocol customization. Platform application (APNS/GCM/FCM) = mobile push with device tokens. SNS topic → platform application → device.' },
    tags: ['sns', 'message-structure', 'mobile-push', 'multi-protocol'],
  },
  {
    id: 'msg-005',
    stem: 'A company\'s order processing system uses SQS. Occasionally, malformed messages cause Lambda to fail repeatedly. After 3 failed processing attempts, the message should be moved to a separate queue for analysis. What is the standard pattern?',
    type: 'single', difficulty: 1, topicSlug: 'messaging', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Configure a Dead Letter Queue (DLQ) on the source SQS queue with maxReceiveCount=3', correct: true, explanation: 'SQS DLQ: configure a redrive policy with maxReceiveCount=3. After the message is received 3 times without deletion, SQS moves it to the DLQ. Lambda does not need any code changes.' },
      { id: 'b', text: 'Add a Lambda DLQ that captures failed invocations', correct: false, explanation: 'Lambda DLQ captures async invocation failures (from SNS, EventBridge). For SQS polling, Lambda invocation failures return the message to SQS — the SQS DLQ handles it.' },
      { id: 'c', text: 'Set SQS message retention to 0 after Lambda fails', correct: false, explanation: 'Message retention is a queue-level setting affecting all messages. It cannot be set per-message based on failures.' },
      { id: 'd', text: 'Use SQS FIFO queue so malformed messages do not block other messages in the queue', correct: false, explanation: 'FIFO queues with MessageGroupId would block the group if one message fails. This worsens the problem.' },
    ],
    explanation: { overall: 'SQS DLQ: separate queue for failed messages. Redrive policy: {deadLetterTargetArn: dlq-arn, maxReceiveCount: 3}. After 3 receives without delete, SQS moves to DLQ. Monitor DLQ with CloudWatch alarm. Process DLQ messages: inspect (log, debug), fix application, requeue via DLQ Redrive (SQS console/API).', examTip: 'SQS DLQ Redrive: can now redrive messages from DLQ back to source queue for reprocessing (no need for custom Lambda to move messages). Monitor: DLQ ApproximateNumberOfMessagesVisible alarm = detect processing issues early.' },
    tags: ['sqs', 'dlq', 'dead-letter-queue', 'error-handling'],
  },
  {
    id: 'msg-006',
    stem: 'A streaming analytics platform ingests 1 million clickstream events per second. The data must be available to 5 different analytics applications simultaneously. Data must be retained for 7 days for replay. Which service fits this use case?',
    type: 'single', difficulty: 2, topicSlug: 'messaging', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Amazon SQS Standard with 5 separate queues per consumer', correct: false, explanation: 'SQS would require publishing to 5 queues separately (via SNS fan-out). SQS visibility timeout makes concurrent consumption complex. Not designed for high-throughput streaming with replay.' },
      { id: 'b', text: 'Amazon Kinesis Data Streams with Enhanced Fan-Out', correct: true, explanation: 'Kinesis: handles millions of events/sec (shard-based scaling). Enhanced Fan-Out: 5 consumers each get dedicated 2 Mbps throughput per shard. 7-day data retention configurable. Replay: re-read from sequence number. Designed for this exact use case.' },
      { id: 'c', text: 'Amazon MSK (Managed Kafka) with 5 consumer groups', correct: true, explanation: 'MSK: Kafka supports multiple consumer groups, each consuming independently from the same topic. Configurable retention. Handles high throughput. More operational than Kinesis but more flexible and compatible with existing Kafka ecosystem.' },
      { id: 'd', text: 'SNS with 5 Lambda subscribers', correct: false, explanation: 'SNS has no replay capability and does not retain messages. Lambda invocation rate limits may throttle at 1 million events/sec.' },
    ],
    explanation: { overall: 'Kinesis vs MSK/Kafka: Kinesis = fully managed, AWS-native, simpler operations. Kinesis limits: 1 MB/sec write per shard, 2 Mbps read per consumer (EFO). MSK = Kafka-compatible, more flexible, existing Kafka tooling works, no hard API limits. Both support replay and multiple consumers.', examTip: 'Kinesis EFO: each registered consumer gets 2 Mbps/shard dedicated throughput (vs 2 Mbps/shard shared for standard consumers). EFO uses HTTP/2 push model. Standard consumers = pull model, share 2 Mbps/shard. For 5+ high-throughput consumers: EFO required.' },
    tags: ['kinesis', 'msk', 'enhanced-fan-out', 'streaming', 'replay'],
  },
  {
    id: 'msg-007',
    stem: 'A company processes financial transactions from 100 branch offices. Each branch sends transactions in batches. Transactions must be processed in order per branch, but different branches can be processed in parallel. Which Kinesis feature supports this?',
    type: 'single', difficulty: 2, topicSlug: 'messaging', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Use partition keys = branch ID; Kinesis routes same-key records to same shard', correct: true, explanation: 'Kinesis partition key: records with the same partition key always go to the same shard, preserving order per branch. Different branches may land on different shards and are processed in parallel. Lambda processes records per shard in order.' },
      { id: 'b', text: 'Use SQS FIFO with MessageGroupId = branch ID', correct: true, explanation: 'SQS FIFO MessageGroupId: messages with same group ID are processed in order and serially. Different group IDs processed in parallel. Perfect for ordered-per-branch parallel processing.' },
      { id: 'c', text: 'Use standard SQS with sequence numbers in the message body', correct: false, explanation: 'Standard SQS provides no ordering guarantee. Application-level sequence numbers require complex reordering logic.' },
      { id: 'd', text: 'Use Kinesis with a single shard to maintain global order', correct: false, explanation: 'Single shard provides global order but eliminates parallelism and is limited to 1 MB/sec write and 2 MB/sec read.' },
    ],
    explanation: { overall: 'Ordered-per-partition parallel processing: Kinesis partition key = branch ID, Lambda parallel per shard. SQS FIFO MessageGroupId = branch ID, parallel per group. Both achieve: per-branch ordering, cross-branch parallelism. Kinesis: higher throughput, streaming. SQS FIFO: simpler, exactly-once, lower throughput (300 API/sec per queue).', examTip: 'Kinesis partition key determines shard placement = ordering guarantee per key. SQS FIFO MessageGroupId = ordering per group, parallel across groups. Use partition key/group ID equal to the entity requiring ordering (customer ID, branch ID, session ID).' },
    tags: ['kinesis', 'sqs-fifo', 'partitioning', 'ordering', 'parallel-processing'],
  },
  {
    id: 'msg-008',
    stem: 'A company uses Amazon EventBridge to route events from multiple SaaS applications to their AWS workloads. The security team requires that all events are archived and can be replayed in case of downstream failures. Which EventBridge features enable this?',
    type: 'single', difficulty: 2, topicSlug: 'messaging', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'EventBridge Event Archives with optional retention period', correct: true, explanation: 'EventBridge Archives: automatically store all events (or filtered by event pattern) on the bus. Retention: indefinite or specific duration. Replay: select archive, time range, and target bus/rules — events are replayed as if they were occurring now.' },
      { id: 'b', text: 'EventBridge Schema Registry', correct: false, explanation: 'Schema Registry discovers and stores event schemas for code generation. It does not archive events for replay.' },
      { id: 'c', text: 'EventBridge Pipes with a DLQ', correct: false, explanation: 'Pipes connect source to target with optional transformation and filtering. DLQ captures failed delivery but does not archive all events for replay.' },
      { id: 'd', text: 'EventBridge Rule targets including S3 for event logging', correct: false, explanation: 'A rule targeting S3 (via Firehose) stores events but is not the built-in archive/replay feature. The archive feature is purpose-built and simpler.' },
    ],
    explanation: { overall: 'EventBridge Archives: enable on an event bus, specify event pattern (or all events), set retention. Replay: create a replay from an archive, specify start/end time, target event bus. Events replay through existing rules on that bus — exactly as they arrived. Useful for: DR testing, backfill after new rule deployment, reprocessing failed events.', examTip: 'EventBridge archive: retention options are 1-90 days or indefinite. Replay: creates events on a target event bus with the "replay-name" header. Rules process replayed events as normal. Note: replayed events are not "live" — they go through existing rules at replay speed.' },
    tags: ['eventbridge', 'archive', 'replay', 'disaster-recovery'],
  },
  {
    id: 'msg-009',
    stem: 'A company runs a messaging system using Amazon MQ (ActiveMQ broker). They need high availability with automatic failover. The broker must survive an AZ failure without message loss. Which deployment option provides this?',
    type: 'single', difficulty: 2, topicSlug: 'messaging', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Single-instance Amazon MQ broker with automated backups', correct: false, explanation: 'Single-instance has no HA. If the AZ or broker fails, it is unavailable until manually restored. Messages in memory are lost.' },
      { id: 'b', text: 'Amazon MQ Active/Standby broker pair across two AZs', correct: true, explanation: 'Active/Standby: primary broker in AZ-A, standby in AZ-B, shared Amazon EFS storage. If primary fails, standby activates with the same data (EFS). Clients reconnect to standby. Near-zero message loss (only in-flight messages at failure moment).' },
      { id: 'c', text: 'Amazon MQ with Multi-AZ Replication', correct: false, explanation: 'Amazon MQ does not have a "Multi-AZ Replication" option like RDS. The correct HA option for MQ is Active/Standby with shared EFS.' },
      { id: 'd', text: 'Two Amazon MQ instances with network replication between them', correct: false, explanation: 'Amazon MQ does not support custom network replication between broker instances. Use the built-in Active/Standby deployment.' },
    ],
    explanation: { overall: 'Amazon MQ HA: Active/Standby with EFS. Both brokers share the same EFS mount (stores message journal and message store). EFS is Multi-AZ, so storage survives AZ failure. On failover: standby reads from shared EFS and activates. Client reconnect URL includes both broker endpoints (failover transport URL).', examTip: 'Amazon MQ: managed ActiveMQ and RabbitMQ. For HA: ActiveMQ uses Active/Standby+EFS. RabbitMQ uses cluster deployment. Use MQ when migrating existing JMS/AMQP applications. For new applications on AWS: prefer SQS/SNS/Kinesis (fully managed, no capacity planning).' },
    tags: ['amazon-mq', 'activemq', 'high-availability', 'active-standby'],
  },
  {
    id: 'msg-010',
    stem: 'A company wants to migrate from RabbitMQ on-premises to AWS. The application uses AMQP protocol and the team does not want to rewrite the application. Which AWS service provides this?',
    type: 'single', difficulty: 1, topicSlug: 'messaging', examDomain: 'migration-modernization',
    options: [
      { id: 'a', text: 'Amazon SQS — it supports AMQP protocol', correct: false, explanation: 'SQS uses the AWS SDK and AWS API (not AMQP). Applications must use the SQS SDK, requiring code changes.' },
      { id: 'b', text: 'Amazon MQ with RabbitMQ broker', correct: true, explanation: 'Amazon MQ is a managed message broker supporting RabbitMQ and ActiveMQ. RabbitMQ on MQ supports AMQP 0-9-1 protocol. Existing applications connect without code changes — just update the broker endpoint URL.' },
      { id: 'c', text: 'Amazon MSK (Managed Streaming for Kafka)', correct: false, explanation: 'MSK is Kafka, not RabbitMQ/AMQP. Requires code changes to use Kafka client protocol instead of AMQP.' },
      { id: 'd', text: 'Amazon EventBridge', correct: false, explanation: 'EventBridge uses event-driven publish/subscribe with AWS API. No AMQP support.' },
    ],
    explanation: { overall: 'Amazon MQ: managed ActiveMQ (OpenWire, MQTT, AMQP, STOMP) or RabbitMQ (AMQP 0-9-1). Migration: change broker endpoint in application config. No code changes needed. Storage: Amazon EFS or EBS. Monitoring: CloudWatch metrics. Not serverless — requires selecting broker instance size.', examTip: 'Amazon MQ use cases: (1) Lift-and-shift from on-premises ActiveMQ/RabbitMQ. (2) Protocol requirements (AMQP, MQTT, STOMP, OpenWire). For new applications: prefer SQS/SNS (no servers to manage, higher scale). Choose MQ when: protocol compatibility matters or migrating legacy systems.' },
    tags: ['amazon-mq', 'rabbitmq', 'amqp', 'migration', 'messaging'],
  },
  {
    id: 'msg-011',
    stem: 'A company uses Kinesis Data Streams. A Lambda consumer is falling behind on processing (iterator age increasing). The stream has 4 shards. Increasing Lambda concurrency alone does not help. What is the root cause and solution?',
    type: 'single', difficulty: 3, topicSlug: 'messaging', examDomain: 'continuous-improvement',
    options: [
      { id: 'a', text: 'Lambda has a limit of one Lambda invocation per Kinesis shard; need more shards', correct: false, explanation: 'Lambda parallelization factor allows up to 10 concurrent invocations per shard. More than 4 concurrent invocations may be needed, but the question says concurrency increase alone did not help.' },
      { id: 'b', text: 'The processing logic in Lambda is slow; optimize or scale out the processing work', correct: false, explanation: 'Slow processing is possible but the question hints at a different root cause.' },
      { id: 'c', text: 'The shard count is insufficient for the incoming data rate; increase shards via Kinesis resharding', correct: true, explanation: 'If each shard receives data faster than it can be consumed, increasing Lambda concurrency does not help (still limited by shard read rate 2 MB/sec). Resharding (splitting shards) increases total stream throughput.' },
      { id: 'd', text: 'Switch to Kinesis Enhanced Fan-Out to get dedicated bandwidth per consumer', correct: true, explanation: 'Standard consumers share 2 Mbps/shard per poll. EFO provides 2 Mbps/shard dedicated per consumer via HTTP/2 push, reducing latency and improving throughput for each consumer independently.' },
    ],
    explanation: { overall: 'Kinesis lag causes: (1) Insufficient shards (data coming in faster than consumed). (2) Slow processing per batch. (3) Shared consumer bandwidth (standard, not EFO). Solutions: (1) Increase shard count (resharding). (2) EFO for dedicated bandwidth. (3) Optimize Lambda. (4) Increase parallelization factor (up to 10x per shard).', examTip: 'Kinesis metrics: GetRecords.IteratorAgeMilliseconds = how far behind consumers are. High iterator age = consumer falling behind. Solutions in order: EFO → parallelization factor → optimize code → increase shards. Resharding: split high-throughput shards.' },
    tags: ['kinesis', 'resharding', 'enhanced-fan-out', 'iterator-age', 'performance'],
  },
  {
    id: 'msg-012',
    stem: 'A company builds a real-time chat application. Messages must be delivered to all online users in a chatroom. Users connect via WebSocket. Which combination of services implements low-latency fan-out to WebSocket clients?',
    type: 'single', difficulty: 3, topicSlug: 'messaging', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'API Gateway WebSocket API + DynamoDB (store connection IDs) + Lambda (push to connections)', correct: true, explanation: 'Standard WebSocket chat pattern: on $connect, store connectionId + roomId in DynamoDB. When a message arrives, Lambda queries DynamoDB for all connectionIds in the room, then calls API Gateway Management API to push to each connection.' },
      { id: 'b', text: 'SNS with mobile push notifications for each user', correct: false, explanation: 'SNS mobile push requires registered device tokens and app installations. Browser WebSocket connections use connectionIds, not device tokens.' },
      { id: 'c', text: 'SQS per user with WebSocket client polling', correct: false, explanation: 'WebSocket is push-based — server pushes to clients. Polling with WebSocket is a misuse of the protocol. Per-user SQS queues are also not scalable.' },
      { id: 'd', text: 'AppSync real-time subscriptions with WebSocket support', correct: false, explanation: 'AppSync subscriptions are GraphQL-based and work well for structured data sync. For raw chat messages, API Gateway WebSocket is more appropriate.' },
    ],
    explanation: { overall: 'WebSocket chat with AWS: (1) Clients connect to API Gateway WebSocket. (2) $connect Lambda stores connectionId + chatRoomId in DynamoDB. (3) Message Lambda queries DynamoDB for room members, calls PostToConnection API for each connectionId. (4) $disconnect Lambda removes from DynamoDB. Fan-out per message: O(users in room).', examTip: 'API Gateway WebSocket: connection lifecycle routes ($connect, $disconnect). Data routes ($default or custom). PostToConnection API: push to specific connectionId. Store connectionIds in DynamoDB. Fan-out: read all connectionIds → call PostToConnection for each.' },
    tags: ['api-gateway', 'websocket', 'chat', 'real-time', 'dynamodb'],
  },
  {
    id: 'msg-013',
    stem: 'A company needs to build an event-driven workflow where a Lambda function is triggered only when BOTH a payment confirmation AND a fraud check complete successfully for the same order. Neither event alone should trigger processing. Which service coordinates this?',
    type: 'single', difficulty: 3, topicSlug: 'messaging', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'SQS with correlation ID: Lambda checks if both events arrived before processing', correct: false, explanation: 'Implementing event correlation logic with SQS visibility timeouts and correlation IDs is complex and unreliable. Purpose-built event correlation is simpler.' },
      { id: 'b', text: 'AWS Step Functions waiting for multiple events using .waitForTaskToken pattern', correct: true, explanation: 'Step Functions with .waitForTaskToken: the state machine pauses and waits for an external signal (task token). Payment confirmation sends one token; fraud check sends another. A Parallel state or EventBridge correlation can coordinate both events before resuming.' },
      { id: 'c', text: 'Amazon EventBridge Content Filtering with multiple event sources', correct: false, explanation: 'EventBridge rules filter individual events. There is no built-in "wait for event A AND event B" correlation capability without additional state management.' },
      { id: 'd', text: 'EventBridge Pipes with filtering to combine multiple events', correct: false, explanation: 'Pipes process one event at a time from a source. They do not correlate or combine events from multiple sources.' },
    ],
    explanation: { overall: 'Event correlation patterns: (1) Step Functions with Parallel + task tokens: parallel branches wait for each event, complete when both arrive. (2) DynamoDB correlation table: Lambda stores partial state, triggers full processing when complete. (3) EventBridge + DynamoDB: store event receipt, trigger on completion. Step Functions provides the cleanest solution.', examTip: 'Wait for multiple events (event correlation): Step Functions with task tokens + parallel states is the most elegant. Alternative: custom correlation with DynamoDB (stateful Lambda). No built-in EventBridge "AND" operator across different event sources.' },
    tags: ['step-functions', 'event-correlation', 'wait-for-task-token', 'parallel'],
  },
  {
    id: 'msg-014',
    stem: 'An e-commerce company uses SNS to send order notifications via SMS and email. They receive complaints about too many notifications. They want to implement per-user notification preferences so users can opt out of SMS but keep email. Which SNS feature enables this?',
    type: 'single', difficulty: 2, topicSlug: 'messaging', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'SNS Message Filter Policies on subscriptions', correct: false, explanation: 'Filter policies filter messages based on message attributes, not per-user preferences for different protocols.' },
      { id: 'b', text: 'Separate SNS topics per notification type with user-managed subscriptions', correct: true, explanation: 'Create separate topics per type (orders-sms, orders-email). Users subscribe/unsubscribe from topics individually. User opts out of SMS: unsubscribe from orders-sms topic. SNS subscription management with opt-out handling per protocol.' },
      { id: 'c', text: 'Amazon Pinpoint for user-managed notification preferences', correct: true, explanation: 'Amazon Pinpoint is purpose-built for marketing communications with per-user preference management, opt-out/opt-in, multi-channel (SMS, email, push, in-app). Manages per-user channel preferences automatically.' },
      { id: 'd', text: 'Lambda function that routes notifications based on user preferences stored in DynamoDB', correct: false, explanation: 'Custom routing logic works but requires building and maintaining preference management. Pinpoint provides this natively.' },
    ],
    explanation: { overall: 'Per-user notification preferences: (1) Pinpoint: managed, multi-channel, preference management, opt-out compliance (TCPA, CAN-SPAM), analytics. (2) Custom: DynamoDB preferences + Lambda routing logic. For marketing/transactional messaging at scale with compliance requirements, Pinpoint is the preferred solution.', examTip: 'Amazon Pinpoint: user engagement platform. Channels: email, SMS, push, in-app. Features: A/B testing, journey builder, user segments, analytics, opt-out management. For applications with notification preference management: Pinpoint > custom SNS/SES solution.' },
    tags: ['sns', 'pinpoint', 'notification-preferences', 'opt-out', 'multi-channel'],
  },
  {
    id: 'msg-015',
    stem: 'A company sends 10 million transactional emails per day (order confirmations, receipts). They need high deliverability, bounce management, and DKIM/DMARC support. Which AWS service handles this at scale?',
    type: 'single', difficulty: 1, topicSlug: 'messaging', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Amazon SNS with email subscriptions', correct: false, explanation: 'SNS email is for notification subscriptions (subscribers confirm via email). Not designed for transactional emails at scale with bounce management.' },
      { id: 'b', text: 'Amazon SES (Simple Email Service)', correct: true, explanation: 'SES: high-volume transactional email, marketing email. Features: SMTP interface, SDK API, DKIM/DMARC/SPF support, bounce and complaint notifications (SNS), dedicated IP addresses, email templates, Virtual Deliverability Manager.' },
      { id: 'c', text: 'Amazon Pinpoint email channel', correct: false, explanation: 'Pinpoint uses SES under the hood for email. SES is the right service for high-volume transactional email directly.' },
      { id: 'd', text: 'Amazon WorkMail', correct: false, explanation: 'WorkMail is a managed email and calendar service for organizations (Microsoft Exchange compatible). Not for programmatic transactional email sending.' },
    ],
    explanation: { overall: 'Amazon SES: high-volume email. SMTP endpoint: smtp.ses.amazonaws.com. API: SendEmail, SendRawEmail. Bounce handling: configure SNS notifications for bounces/complaints, automatically suppress bounced addresses. Dedicated IPs: better deliverability control. SES in VPC: send from private subnets via VPC endpoint.', examTip: 'SES use cases: transactional email (order confirmations, password resets), marketing email (newsletters). SES pricing: $0.10/1,000 emails (from EC2/Lambda). SMTP and API available. SPF/DKIM/DMARC: configure DNS records for domain. Sandbox mode → request production access for high volume.' },
    tags: ['ses', 'email', 'transactional', 'deliverability', 'dkim'],
  },
  {
    id: 'msg-016',
    stem: 'A company uses Kinesis Data Firehose to deliver logs from EC2 instances to S3. They need to filter out DEBUG-level log entries before they reach S3 to reduce storage costs. Which Firehose feature enables this?',
    type: 'single', difficulty: 2, topicSlug: 'messaging', examDomain: 'cost-optimization',
    options: [
      { id: 'a', text: 'Kinesis Data Firehose Lambda transformation to filter records', correct: true, explanation: 'Firehose Lambda transformation: attach a Lambda function to the delivery stream. Lambda receives batches of records, filters/transforms them, and returns. Records can be: Dropped, Ok (pass through), ProcessingFailed (sent to S3 error prefix). Filter DEBUG entries by returning Dropped status.' },
      { id: 'b', text: 'Kinesis Data Firehose Dynamic Partitioning to separate DEBUG logs', correct: false, explanation: 'Dynamic Partitioning organizes records into S3 prefixes based on data values (e.g., /year/month/day/logLevel). It separates records but does not drop them — DEBUG logs still stored in a separate path.' },
      { id: 'c', text: 'S3 lifecycle policy to delete DEBUG log files after 1 day', correct: false, explanation: 'Lifecycle policy would delete DEBUG logs but they are still ingested and stored (costing money for 1 day). Lambda transformation prevents them from ever reaching S3.' },
      { id: 'd', text: 'CloudWatch Logs filter to remove DEBUG entries before sending to Firehose', correct: false, explanation: 'CloudWatch Logs metric filter or subscription filter can filter lines, but filtering before Firehose via CloudWatch adds another service hop and complexity.' },
    ],
    explanation: { overall: 'Firehose Lambda transformation: Lambda function receives up to 3 MB per invocation. Returns each record with result: "Ok" (include), "Dropped" (discard), "ProcessingFailed" (send to error S3 prefix). Parse log level in Lambda and return Dropped for DEBUG. Reduces S3 storage and Athena query cost.', examTip: 'Kinesis Firehose destinations: S3, Redshift, OpenSearch, Splunk, HTTP endpoints, third-party services. Lambda transformation: inline ETL (filter, enrich, convert format). Dynamic partitioning: S3 path based on record content. Format conversion: JSON→Parquet/ORC in-flight.' },
    tags: ['kinesis-firehose', 'lambda-transformation', 'filtering', 'cost-optimization'],
  },
  {
    id: 'msg-017',
    stem: 'A company runs an IoT platform receiving sensor data from 100,000 devices. Each device sends messages every second. The platform uses IoT Core. They need to route messages to DynamoDB for real-time access and S3 for long-term storage simultaneously. Which IoT Core feature enables this?',
    type: 'single', difficulty: 2, topicSlug: 'messaging', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'IoT Core Message Broker with two separate MQTT subscriptions', correct: false, explanation: 'IoT Core message broker handles pub/sub but configuring multiple backends requires IoT Rules, not raw subscriptions from the cloud.' },
      { id: 'b', text: 'IoT Core Rules Engine with multiple actions per rule', correct: true, explanation: 'IoT Rules Engine: SQL-based rules that match MQTT messages and route to up to 10 actions per rule. One rule: SELECT * FROM topic/+ WHERE temp > 0 → Action 1: DynamoDB PutItem + Action 2: S3 PutObject. Both happen simultaneously.' },
      { id: 'c', text: 'IoT Core with Lambda function routing to DynamoDB and S3', correct: false, explanation: 'Lambda as an IoT Rule action works but adds unnecessary complexity when IoT Rules natively support multiple direct service actions.' },
      { id: 'd', text: 'IoT Greengrass with local routing to cloud targets', correct: false, explanation: 'Greengrass handles edge/local processing on devices. Cloud routing is handled by IoT Core Rules Engine.' },
    ],
    explanation: { overall: 'IoT Core Rules Engine: SQL-based topic filtering, up to 10 actions per rule. Direct service integrations: DynamoDB, S3, SQS, SNS, Kinesis, Lambda, Firehose, OpenSearch, Timestream, etc. Error action: one additional action for rule failures. Most efficient for IoT message routing to multiple destinations.', examTip: 'IoT Core: publish/subscribe for IoT devices (MQTT, HTTP, WebSocket). Rules Engine: route messages to AWS services. Device Shadow: maintain virtual device state in cloud. Greengrass: edge computing on devices. Fleet Indexing: query device state. Certificate rotation: manage device certificates.' },
    tags: ['iot-core', 'rules-engine', 'dynamodb', 's3', 'iot'],
  },
  {
    id: 'msg-018',
    stem: 'A company uses SQS and needs to ensure that certain high-priority messages are processed before low-priority messages, even if the high-priority messages arrive later. How can SQS queues be configured to support message prioritization?',
    type: 'single', difficulty: 3, topicSlug: 'messaging', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Set message priority attribute and have consumers process higher priority messages first', correct: false, explanation: 'SQS does not natively support priority queues. Consumers receive messages in approximately FIFO order (standard queue) or strict FIFO (FIFO queue). Priority-based retrieval is not supported.' },
      { id: 'b', text: 'Use two separate SQS queues — one high priority, one low priority; consumers poll high-priority queue first', correct: true, explanation: 'Priority queue pattern with SQS: create separate queues per priority. Consumer: long-poll high-priority queue first; if no messages, poll low-priority queue. This implements soft prioritization at the consumer level.' },
      { id: 'c', text: 'Use SQS message attributes to add a priority tag; SQS will automatically sort by priority', correct: false, explanation: 'Message attributes are metadata that consumers can read, but SQS does not use them for ordering or prioritization. SQS delivers messages in received order.' },
      { id: 'd', text: 'Use SQS FIFO queue with multiple message groups for different priorities', correct: false, explanation: 'FIFO MessageGroupId ensures ordering within a group, but different groups are not prioritized over each other. High-priority group messages do not pre-empt low-priority group processing.' },
    ],
    explanation: { overall: 'SQS priority patterns: (1) Multiple queues + consumer logic: consumer checks high-priority queue first, falls back to low-priority. Simple and effective. (2) Single consumer with visibility: consumer receives from both, processes high-priority, makes low-priority invisible temporarily. (3) Dead-letter for low priority: let low-priority messages age in queue while high-priority are processed first.', examTip: 'SQS does not natively support message priority. The standard pattern: separate queues per priority tier + consumer logic that checks highest-priority queue first. For sophisticated priority: Amazon MQ ActiveMQ supports native message priority. SQS FIFO = strict order, not priority.' },
    tags: ['sqs', 'priority-queue', 'message-prioritization', 'queue-patterns'],
  },
  {
    id: 'msg-019',
    stem: 'A company processes sensor data from thousands of IoT devices using Kinesis Data Streams. Each shard receives data from multiple devices. The Lambda consumer needs to process records from a specific device in order. What is the correct approach?',
    type: 'single', difficulty: 3, topicSlug: 'messaging', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Use deviceId as the partition key when producing records to Kinesis', correct: true, explanation: 'Kinesis partition key MD5 hash determines which shard receives the record. Same partition key = same shard = ordered delivery per device. Lambda processes each shard sequentially, maintaining order per device.' },
      { id: 'b', text: 'Use a FIFO Kinesis stream to ensure global ordering across all devices', correct: false, explanation: 'Kinesis does not have a FIFO mode. Global ordering requires a single shard (severely limits throughput). Use per-device ordering via partition keys instead.' },
      { id: 'c', text: 'Store records in DynamoDB with a timestamp; Lambda queries records in order', correct: false, explanation: 'DynamoDB-based ordering adds complexity and latency. Kinesis partition keys provide ordering natively.' },
      { id: 'd', text: 'Use Kinesis Consumer Library with checkpointing to maintain order', correct: false, explanation: 'KCL (and Lambda) already process records in order per shard. The key is ensuring same-device records land on the same shard, which is controlled by partition key.' },
    ],
    explanation: { overall: 'Kinesis ordering guarantee: records with the same partition key are stored in the same shard in arrival order. Consumers (Lambda/KCL) process records within a shard sequentially. For per-device ordering: partition key = deviceId. Hot shard risk: if one device produces much more than others, consider partition key = deviceId+randomSuffix for high-volume devices (sacrifice strict ordering for scale).', examTip: 'Kinesis ordering: within a shard (not global). Partition key determines shard placement. For per-entity ordering: entity ID as partition key. For global ordering: use one shard (throughput limited to 1 MB/sec write). Trade-off: more shards = more parallelism but only per-shard ordering.' },
    tags: ['kinesis', 'partition-key', 'ordering', 'iot', 'lambda'],
  },
  {
    id: 'msg-020',
    stem: 'A company migrates an existing on-premises Apache Kafka workload to AWS. The application uses Kafka client libraries and the team wants to minimize code changes. Which AWS service provides managed Kafka?',
    type: 'single', difficulty: 1, topicSlug: 'messaging', examDomain: 'migration-modernization',
    options: [
      { id: 'a', text: 'Amazon Kinesis Data Streams', correct: false, explanation: 'Kinesis uses the Kinesis Producer Library (KPL) and Kinesis Client Library (KCL) — different APIs from Kafka. Code changes required.' },
      { id: 'b', text: 'Amazon MSK (Managed Streaming for Apache Kafka)', correct: true, explanation: 'MSK runs Apache Kafka natively. Existing Kafka producer/consumer code works without changes — just update the broker endpoint. MSK handles cluster management, patching, and monitoring.' },
      { id: 'c', text: 'Amazon EventBridge', correct: false, explanation: 'EventBridge is an event bus with its own API. Not Kafka-compatible. Requires code changes.' },
      { id: 'd', text: 'Amazon SQS FIFO', correct: false, explanation: 'SQS is not Kafka-compatible. Requires AWS SDK and code changes.' },
    ],
    explanation: { overall: 'Amazon MSK: fully managed Apache Kafka. Supports Kafka versions (2.x, 3.x). Kafka client compatibility: any application using Kafka client libraries works unchanged. MSK Serverless: no capacity planning, automatically scales. MSK Connect: managed Kafka Connect workers for connectors (S3, Redshift, OpenSearch). Schema Registry: Glue Schema Registry.', examTip: 'MSK vs Kinesis: MSK = Kafka-compatible, existing Kafka tooling works. Kinesis = AWS-proprietary, higher per-shard cost, simpler setup, fewer ops. Choose MSK for Kafka migration or Kafka ecosystem tools. Kinesis for new greenfield AWS-native streaming.' },
    tags: ['msk', 'kafka', 'migration', 'messaging', 'managed-kafka'],
  },
];
