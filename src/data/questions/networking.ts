import type { Question } from '../../types/question';

export const networkingQuestions: Question[] = [
  {
    id: 'net-001',
    stem: 'A company has an existing 1 Gbps AWS Direct Connect dedicated connection to us-east-1. They are expanding to eu-west-1 and need to access VPCs in both regions over a single Direct Connect connection. Which component enables this?',
    type: 'single', difficulty: 2, topicSlug: 'networking', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Create a Private VIF to each VPC in both regions', correct: false, explanation: 'A Private VIF connects to a VGW or DXGW in a single region. You cannot create a Private VIF that spans regions.' },
      { id: 'b', text: 'Create a Direct Connect Gateway and associate VGWs or Transit Gateways from both regions', correct: true, explanation: 'Direct Connect Gateway (DXGW) is a globally available resource that allows a single DX connection to reach VPCs across multiple AWS regions. Associate the DXGW with VGWs/TGWs in each region.' },
      { id: 'c', text: 'Use a Transit VIF and configure BGP routing to both regions', correct: false, explanation: 'Transit VIFs connect to a Transit Gateway in a specific region, not across regions. DXGW bridges the regional gap.' },
      { id: 'd', text: 'Provision a second Direct Connect connection in eu-west-1', correct: false, explanation: 'A second DX connection is one option but not required — DXGW allows the existing connection to reach multiple regions.' },
    ],
    explanation: { overall: 'Direct Connect Gateway enables a single DX connection to access VPCs across multiple AWS regions and multiple accounts. You create a Transit VIF or Private VIF pointing to the DXGW, then associate the DXGW with VGWs or TGWs in each desired region. The DXGW itself is a global resource not tied to a region.', examTip: 'Direct Connect Gateway: one DX connection → multiple regions. Associate with up to 3 Transit Gateways or 10 VGWs. Traffic between associated VPCs does NOT flow through DXGW (VPC-to-VPC is blocked at DXGW level).' },
    tags: ['direct-connect', 'dxgw', 'multi-region', 'private-vif'],
  },
  {
    id: 'net-002',
    stem: 'A company has a 10 Gbps Direct Connect connection but needs 20 Gbps throughput for a data migration. They want to achieve this without provisioning a second DX connection. Which approach provides 20 Gbps of aggregate throughput over Direct Connect?',
    type: 'single', difficulty: 3, topicSlug: 'networking', examDomain: 'migration-modernization',
    options: [
      { id: 'a', text: 'Configure ECMP on the Direct Connect connection to double bandwidth', correct: false, explanation: 'ECMP for Direct Connect bandwidth aggregation requires multiple connections or LAG (Link Aggregation Group), not configuration on a single connection.' },
      { id: 'b', text: 'Create a Link Aggregation Group (LAG) combining two 10 Gbps connections at the same Direct Connect location', correct: true, explanation: 'LAG aggregates multiple physical Direct Connect connections at the same location into a single logical connection. Two 10 Gbps connections in a LAG provide 20 Gbps aggregate bandwidth with active-active load balancing.' },
      { id: 'c', text: 'Add a Site-to-Site VPN alongside Direct Connect and use ECMP for 20 Gbps aggregate', correct: false, explanation: 'Site-to-Site VPN maximum throughput is ~1.25 Gbps per tunnel. VPN cannot aggregate with DX for high bandwidth.' },
      { id: 'd', text: 'Upgrade to a 100 Gbps hosted Direct Connect connection', correct: false, explanation: '100 Gbps is a dedicated connection option but is a separate connection, not a configuration change on the existing one.' },
    ],
    explanation: { overall: 'Link Aggregation Group (LAG) bundles multiple Direct Connect connections at the same location into a single logical managed connection. All connections in a LAG must have the same bandwidth, use the same Direct Connect endpoint, and support LACP. LAG provides both bandwidth aggregation and redundancy (if one physical link fails, others continue). Minimum 2, maximum 4 connections per LAG.', examTip: 'LAG = bundle multiple DX connections at same location for aggregate bandwidth. ECMP = load balance across multiple VPN tunnels to TGW for VPN bandwidth aggregation. Different tools for DX vs VPN.' },
    tags: ['direct-connect', 'lag', 'bandwidth', 'ecmp'],
  },
  {
    id: 'net-003',
    stem: 'A company uses Direct Connect as the primary path to AWS. For disaster recovery, they need an encrypted backup path. Direct Connect itself is not encrypted by default. Which solution provides encrypted connectivity with automatic failover when Direct Connect fails?',
    type: 'single', difficulty: 2, topicSlug: 'networking', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Configure MACsec on the Direct Connect connection for encryption and redundancy', correct: false, explanation: 'MACsec encrypts Direct Connect traffic but does not provide a failover path — it is still a single physical connection.' },
      { id: 'b', text: 'Set up a Site-to-Site VPN as a backup, attached to the same VGW or TGW as the Direct Connect. BGP route metrics make DX preferred', correct: true, explanation: 'VPN over the internet provides encrypted backup. Attaching both DX and VPN to the same VGW/TGW with BGP routing (shorter AS path or higher MED for DX) ensures DX is preferred. Automatic BGP failover switches to VPN when DX fails.' },
      { id: 'c', text: 'Use AWS Global Accelerator as an encrypted fallback for Direct Connect', correct: false, explanation: 'Global Accelerator is for application acceleration and does not provide hybrid connectivity or encryption for on-premises data center traffic.' },
      { id: 'd', text: 'Create an IPsec VPN tunnel over the Direct Connect connection for encryption with a second Direct Connect for failover', correct: false, explanation: 'IPsec VPN over DX adds encryption but a second DX for failover is expensive. The standard pattern is internet VPN as backup.' },
    ],
    explanation: { overall: 'Direct Connect + VPN backup is the standard HA architecture. Both terminate at the same VGW or TGW. BGP metrics control preference: DX has a shorter AS path so it is preferred. When DX fails, BGP reconverges and routes traffic over VPN. VPN uses IPsec so it is encrypted — addressing the DX encryption gap. Total cost is much lower than a second DX connection.', examTip: 'Standard DX resilience: DX (primary, unencrypted by default) + VPN (backup, encrypted). Both attach to same VGW/TGW. BGP AS-path prepending on VPN makes DX preferred. MACsec encrypts DX but does not provide failover.' },
    tags: ['direct-connect', 'vpn', 'failover', 'bgp', 'encryption'],
  },
  {
    id: 'net-004',
    stem: 'A financial services company has strict requirements: all traffic between their on-premises data center and AWS must stay on the AWS global network, not traverse the public internet. Their workloads in multiple regions require this. What is the correct Direct Connect resiliency model to meet their SLA requirements?',
    type: 'single', difficulty: 2, topicSlug: 'networking', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Development resiliency model: single connection at one Direct Connect location', correct: false, explanation: 'Development model has a single point of failure (one DX location). Not suitable for financial SLAs.' },
      { id: 'b', text: 'High resiliency model: two connections at a single Direct Connect location', correct: false, explanation: 'High resiliency provides connection redundancy but both connections are at the same physical location — a location failure causes outage.' },
      { id: 'c', text: 'Maximum resiliency model: two connections at two separate Direct Connect locations', correct: true, explanation: 'Maximum resiliency uses two DX locations with two connections each (4 total). Provides protection against both individual connection failures and entire DX location failures. AWS recommends this for critical workloads.' },
      { id: 'd', text: 'High resiliency with VPN backup for cost savings', correct: false, explanation: 'VPN uses the public internet, violating the requirement that all traffic stays on the AWS global network.' },
    ],
    explanation: { overall: 'AWS Direct Connect resiliency models: (1) Maximum: 2 locations × 2 connections = 4 total connections, protects against location failure. (2) High: 1 location × 2 connections, protects against single connection failure. (3) Development: 1 connection, no redundancy. For financial services avoiding public internet, Maximum resiliency is required — VPN backup is not an option.', examTip: 'Maximum DX resiliency: 2 DX locations, 2 connections per location = 4 connections total. Protects against single connection AND entire location failure. Required for financial/regulated workloads where VPN backup is not acceptable.' },
    tags: ['direct-connect', 'resiliency', 'maximum-resiliency', 'ha'],
  },
  {
    id: 'net-005',
    stem: 'A company has 10 branch offices connected to AWS via Site-to-Site VPN. Each branch needs to communicate with AWS and with each other through AWS. Setting up full-mesh VPN between all branches is operationally complex. What is the most operationally efficient solution?',
    type: 'single', difficulty: 2, topicSlug: 'networking', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Create VPN peering connections between all branch offices directly', correct: false, explanation: 'Full-mesh VPN between 10 branches requires 10*(10-1)/2 = 45 tunnels and VPN appliances at each site — not operationally efficient.' },
      { id: 'b', text: 'Use AWS VPN CloudHub: all branches connect to a single Virtual Private Gateway with dynamic BGP routing', correct: true, explanation: 'VPN CloudHub enables hub-and-spoke connectivity where multiple Customer Gateways connect to one VGW. The VGW routes traffic between branches automatically. Only N VPN connections needed (not N*(N-1)/2).' },
      { id: 'c', text: 'Use Transit Gateway with one VPN attachment per branch', correct: false, explanation: 'TGW also works and provides more features, but VPN CloudHub using a single VGW is the simpler, lower-cost option specifically designed for this branch-to-branch-via-AWS pattern.' },
      { id: 'd', text: 'Configure BGP route reflectors at each branch office', correct: false, explanation: 'BGP route reflectors are for routing protocol optimization, not for replacing VPN tunnel management.' },
    ],
    explanation: { overall: 'AWS VPN CloudHub allows multiple Customer Gateways to connect to a single Virtual Private Gateway using dynamic BGP routing. The VGW acts as a hub, enabling branch-to-branch communication through AWS without direct branch-to-branch VPN tunnels. Each branch only needs one VPN connection to AWS. BGP propagates all routes so each branch learns about all others automatically.', examTip: 'VPN CloudHub = single VGW + multiple Customer Gateways + BGP = hub-and-spoke for branch offices. Branches communicate through AWS. Cost: only N connections vs N*(N-1)/2 for full mesh.' },
    tags: ['vpn-cloudhub', 'site-to-site-vpn', 'branch-connectivity', 'bgp'],
  },
  {
    id: 'net-006',
    stem: 'A company deploys a network firewall appliance (third-party NGFW) in a centralized "inspection VPC" connected via Transit Gateway. They notice that some TCP sessions are being asymmetrically routed — the outbound packets go through the firewall but return packets bypass it. What TGW feature resolves this?',
    type: 'single', difficulty: 3, topicSlug: 'networking', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Enable Transit Gateway route table propagation for all attachments', correct: false, explanation: 'Route propagation ensures routes are known but does not ensure symmetric routing for stateful inspection.' },
      { id: 'b', text: 'Enable Appliance Mode on the Transit Gateway VPC attachment for the inspection VPC', correct: true, explanation: 'Appliance Mode ensures that TGW uses a single fixed ENI in one AZ for all traffic of a given flow (forward and return). Without it, TGW may route traffic through different AZ\'s ENIs, causing asymmetric routing that breaks stateful firewalls.' },
      { id: 'c', text: 'Configure Equal Cost Multi-Path (ECMP) routing on the Transit Gateway', correct: false, explanation: 'ECMP on TGW is for VPN bandwidth aggregation across tunnels, not for symmetric routing through inline appliances.' },
      { id: 'd', text: 'Use a Gateway Load Balancer instead of routing through Transit Gateway', correct: false, explanation: 'GWLB is a valid architecture but the question asks specifically about fixing asymmetric routing with the existing TGW setup.' },
    ],
    explanation: { overall: 'Appliance Mode is a TGW VPC attachment feature that ensures all traffic for a given flow (5-tuple: src/dst IP/port, protocol) is sent to the same ENI in the same AZ within the appliance VPC. Without Appliance Mode, TGW uses multiple ENIs across AZs for different directions of a flow, breaking stateful inspection in NGFWs that expect to see both directions.', examTip: 'TGW Appliance Mode: required for stateful network appliances (NGFW, IDS/IPS) in an inspection VPC. Ensures symmetric routing — both directions of a TCP flow hit the same appliance ENI.' },
    tags: ['transit-gateway', 'appliance-mode', 'ngfw', 'symmetric-routing'],
  },
  {
    id: 'net-007',
    stem: 'A company has three VPCs: Production, Development, and Shared Services. The Shared Services VPC contains common resources (AD, monitoring) that both Prod and Dev need to access. However, Prod and Dev must NOT be able to communicate with each other. How should Transit Gateway route tables be configured?',
    type: 'single', difficulty: 3, topicSlug: 'networking', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Use a single TGW route table with all three VPCs — default routing handles isolation', correct: false, explanation: 'A single route table with all VPCs allows full mesh communication including Prod↔Dev, which is not desired.' },
      { id: 'b', text: 'Create separate route tables for Prod and Dev, each with a route to Shared Services only. Shared Services route table has routes to all VPCs', correct: true, explanation: 'Prod RT: route to Shared Services only. Dev RT: route to Shared Services only. Shared Services RT: routes to Prod and Dev. This enables Prod↔Shared and Dev↔Shared but blocks Prod↔Dev.' },
      { id: 'c', text: 'Use VPC Peering between each VPC and Shared Services, with no TGW involvement', correct: false, explanation: 'VPC Peering works for this topology but does not scale well and the question involves Transit Gateway. TGW route table isolation is the intended pattern.' },
      { id: 'd', text: 'Use Network ACLs in the Shared Services VPC to block traffic between Prod and Dev', correct: false, explanation: 'NACLs operate at the subnet level within a VPC, not at the TGW routing level. TGW route table isolation is the correct approach.' },
    ],
    explanation: { overall: 'Transit Gateway route table isolation is the key pattern for network segmentation. By creating separate route tables per attachment (or group of attachments), you control which VPCs can reach each other. Prod and Dev each have their own RT with only a Shared Services route — they cannot reach each other. Shared Services has routes to both, enabling bidirectional access to common resources.', examTip: 'TGW route table isolation: one RT per segment. Prod RT → Shared only. Dev RT → Shared only. Shared RT → Prod + Dev. Result: Prod and Dev isolated from each other, both access Shared.' },
    tags: ['transit-gateway', 'route-tables', 'isolation', 'network-segmentation'],
  },
  {
    id: 'net-008',
    stem: 'A SaaS company wants to expose their service to customers without giving customers access to the SaaS VPC. Customers should be able to connect using a private IP address within their own VPC, and the connection should be one-way (customers can access the service, but the service cannot initiate connections into customer VPCs). Which AWS feature provides this?',
    type: 'single', difficulty: 2, topicSlug: 'networking', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'VPC Peering between the SaaS VPC and each customer VPC', correct: false, explanation: 'VPC Peering is bidirectional and gives both VPCs routing access to each other. CIDR overlap prevents scaling to many customers.' },
      { id: 'b', text: 'AWS PrivateLink (VPC Endpoint Services) backed by a Network Load Balancer', correct: true, explanation: 'PrivateLink creates an interface VPC endpoint in the customer\'s VPC with a private IP. Traffic flows from customer→SaaS only (one-directional). The SaaS VPC cannot initiate connections into customer VPCs. CIDRs can overlap.' },
      { id: 'c', text: 'AWS Transit Gateway connecting the SaaS VPC and all customer VPCs', correct: false, explanation: 'TGW enables bidirectional routing between all attached VPCs, violating the one-way access requirement.' },
      { id: 'd', text: 'Site-to-Site VPN between SaaS and each customer', correct: false, explanation: 'VPN creates bidirectional encrypted tunnels and is complex to manage for many customers.' },
    ],
    explanation: { overall: 'AWS PrivateLink (Endpoint Services) enables service providers to expose services via an NLB in their VPC. Customers create Interface VPC Endpoints in their VPC which create ENIs with private IPs. All traffic flows from customer ENI → SaaS NLB. The SaaS provider cannot initiate connections back. No CIDR overlap constraints. The service provider must whitelist each customer account/VPC to create the endpoint.', examTip: 'PrivateLink = unidirectional private connectivity from customer to service. No VPC routing changes, CIDR overlap OK, provider cannot see customer VPC. Requires NLB in provider VPC. Used by AWS for interface endpoints to their own services.' },
    tags: ['privatelink', 'vpc-endpoint-services', 'saas', 'unidirectional'],
  },
  {
    id: 'net-009',
    stem: 'A company\'s on-premises DNS server resolves internal hostnames for servers in their data center. After migrating some servers to a VPC, the on-premises servers need to resolve DNS names of EC2 instances in the VPC (using Route 53 private hosted zone names). Which Route 53 feature enables this?',
    type: 'single', difficulty: 2, topicSlug: 'networking', examDomain: 'migration-modernization',
    options: [
      { id: 'a', text: 'Create public Route 53 records for the EC2 instances so on-premises servers can query public DNS', correct: false, explanation: 'Using public DNS for internal EC2 instances is a security risk and the instances may only have private IPs.' },
      { id: 'b', text: 'Configure Route 53 Resolver Outbound Endpoint in the VPC; configure on-premises DNS to forward VPC domain queries to the endpoint', correct: false, explanation: 'Outbound Endpoint sends queries FROM VPC TO on-premises. For on-premises to resolve VPC names, you need an Inbound Endpoint.' },
      { id: 'c', text: 'Configure Route 53 Resolver Inbound Endpoint in the VPC; configure on-premises DNS to forward queries for the VPC domain to the inbound endpoint IP', correct: true, explanation: 'Inbound Endpoints create ENIs in the VPC that on-premises DNS servers can forward queries to. Route 53 Resolver answers using the VPC\'s private hosted zones.' },
      { id: 'd', text: 'Enable DNS resolution on the Direct Connect connection', correct: false, explanation: 'Direct Connect does not have a DNS resolution setting. Route 53 Resolver endpoints handle hybrid DNS.' },
    ],
    explanation: { overall: 'Route 53 Resolver Hybrid DNS: Inbound Endpoints = ENIs in the VPC that on-premises can query for VPC DNS resolution. Outbound Endpoints = forward DNS queries from VPC to on-premises DNS servers. For on-premises to resolve VPC names, you need Inbound Endpoints. On-premises DNS is configured with forwarding rules sending e.g. "*.vpc.internal" queries to the Inbound Endpoint IP.', examTip: 'Inbound Endpoint: on-premises → AWS (on-prem resolves AWS private zones). Outbound Endpoint: AWS → on-premises (VPC resolves on-prem hostnames). Both directions require the corresponding endpoint type.' },
    tags: ['route53-resolver', 'inbound-endpoint', 'hybrid-dns', 'on-premises'],
  },
  {
    id: 'net-010',
    stem: 'A company needs to inspect all egress traffic from multiple VPCs before it leaves to the internet. They want a centralized inspection model using AWS Network Firewall. What is the recommended architecture?',
    type: 'single', difficulty: 3, topicSlug: 'networking', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Deploy Network Firewall in each VPC\'s public subnet before the NAT Gateway', correct: false, explanation: 'Deploying in each VPC works but is not centralized — it requires managing firewall policies in each VPC separately.' },
      { id: 'b', text: 'Deploy Network Firewall in a centralized Inspection VPC. Route all spoke VPC egress through TGW to the Inspection VPC, then to a NAT Gateway', correct: true, explanation: 'The centralized pattern: Spoke VPCs → TGW → Inspection VPC (Network Firewall) → NAT Gateway → Internet. All egress traffic is inspected centrally. Single firewall policy manages all traffic.' },
      { id: 'c', text: 'Use AWS WAF in all VPCs to inspect and filter egress traffic', correct: false, explanation: 'AWS WAF is for HTTP/HTTPS application-layer protection on ALBs and CloudFront, not for network-layer egress inspection.' },
      { id: 'd', text: 'Configure Security Groups with outbound rules to block unauthorized traffic patterns', correct: false, explanation: 'Security Groups support allow rules only — they cannot perform stateful deep-packet inspection or domain-based filtering like Network Firewall.' },
    ],
    explanation: { overall: 'The centralized Network Firewall pattern: Spoke VPCs have a 0.0.0.0/0 route in their private subnets pointing to TGW. TGW routes to the Inspection VPC. Network Firewall inspects all traffic (domain filtering, IPS rules, stateful inspection). Approved traffic exits via NAT Gateway. TGW Appliance Mode ensures symmetric routing. One firewall policy controls egress for all spoke VPCs.', examTip: 'Centralized egress inspection: Spoke VPCs → TGW (Appliance Mode on inspection attachment) → Inspection VPC (Network Firewall) → NAT GW → Internet. Single policy, centralized logging, lower cost than per-VPC deployment.' },
    tags: ['network-firewall', 'centralized-inspection', 'transit-gateway', 'egress'],
  },
  {
    id: 'net-011',
    stem: 'A startup shares a Transit Gateway across 5 AWS accounts in their organization. They want to add a new account to access the TGW without creating a new TGW. Which service enables sharing of the TGW to the new account?',
    type: 'single', difficulty: 1, topicSlug: 'networking', examDomain: 'organizational-complexity',
    options: [
      { id: 'a', text: 'AWS Organizations consolidated billing automatically shares network resources', correct: false, explanation: 'Consolidated billing handles cost aggregation but does not share infrastructure resources between accounts.' },
      { id: 'b', text: 'AWS Resource Access Manager (RAM) to share the Transit Gateway with the new account or OU', correct: true, explanation: 'RAM enables sharing AWS resources (TGW, subnets, Route 53 Resolver rules, etc.) across accounts within an Organization or directly to specific accounts. The new account can then create TGW attachments.' },
      { id: 'c', text: 'VPC Peering between the new account\'s VPC and the TGW owner account', correct: false, explanation: 'VPC Peering is a different connectivity model and does not involve Transit Gateway sharing.' },
      { id: 'd', text: 'Create a cross-account IAM role granting ec2:CreateTransitGatewayAttachment permissions', correct: false, explanation: 'While IAM permissions are required to create attachments, the TGW must first be shared to the account using RAM before attachment creation is possible.' },
    ],
    explanation: { overall: 'AWS Resource Access Manager (RAM) enables sharing of resources like Transit Gateways, VPC subnets, Route 53 Resolver rules, and others across AWS accounts or within an Organization. When sharing a TGW, the recipient account can create VPC attachments to the shared TGW without owning it. This enables a centralized networking model where one account owns the TGW and all accounts connect their VPCs to it.', examTip: 'RAM for TGW: owner shares TGW → participants attach their VPCs. RAM also shares VPC subnets (for VPC sharing pattern), Route 53 Resolver rules, and 30+ other resource types.' },
    tags: ['ram', 'resource-access-manager', 'transit-gateway', 'cross-account'],
  },
  {
    id: 'net-012',
    stem: 'A company has a 1 Gbps Direct Connect. They also use AWS Site-to-Site VPN. They want to increase their effective bandwidth to 5 Gbps for workloads going through Transit Gateway without adding more DX connections. How can they achieve 5 Gbps of VPN throughput?',
    type: 'single', difficulty: 3, topicSlug: 'networking', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Configure a single VPN connection with enhanced bandwidth settings', correct: false, explanation: 'A single Site-to-Site VPN tunnel has a maximum throughput of ~1.25 Gbps. There is no enhanced bandwidth setting for individual tunnels.' },
      { id: 'b', text: 'Create multiple VPN connections to the Transit Gateway and enable ECMP', correct: true, explanation: 'Each VPN connection to TGW provides ~1.25 Gbps per tunnel pair. With ECMP enabled on TGW, traffic is load-balanced across multiple VPN connections. 4 VPN connections = up to 5 Gbps aggregate.' },
      { id: 'c', text: 'Use AWS Global Accelerator to accelerate and aggregate VPN connections', correct: false, explanation: 'Global Accelerator routes application traffic, not VPN tunnels. It does not aggregate VPN bandwidth.' },
      { id: 'd', text: 'Configure BGP path prepending to force all traffic through Direct Connect', correct: false, explanation: 'BGP path prepending makes DX less preferred, not more. This would not increase bandwidth.' },
    ],
    explanation: { overall: 'Transit Gateway supports ECMP (Equal Cost Multi-Path) routing for VPN connections. Each VPN connection to TGW has two tunnels (~1.25 Gbps each). With ECMP enabled, you can aggregate bandwidth across multiple VPN connections. 4 VPN connections × ~1.25 Gbps = ~5 Gbps aggregate. This is done by advertising the same routes from multiple VPN connections with equal cost to TGW.', examTip: 'TGW + ECMP + multiple VPN connections = aggregate VPN bandwidth. Each VPN connection ≈ 1.25 Gbps. 4 connections = ~5 Gbps. ECMP must be enabled on TGW. Useful when DX is insufficient and more DX is too expensive.' },
    tags: ['transit-gateway', 'ecmp', 'vpn', 'bandwidth-aggregation'],
  },
  {
    id: 'net-013',
    stem: 'A network engineer needs to allow on-premises DNS resolution of private Route 53 hosted zones while also forwarding specific on-premises domain queries from the VPC to on-premises DNS. Which Route 53 Resolver configuration achieves both directions?',
    type: 'multiple', difficulty: 3, topicSlug: 'networking', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Create an Inbound Endpoint — allows on-premises to forward queries to the VPC\'s Route 53 Resolver', correct: true, explanation: 'Inbound Endpoints create ENIs in the VPC with IPs that on-premises DNS can forward to. Route 53 Resolver answers using associated private hosted zones.' },
      { id: 'b', text: 'Create an Outbound Endpoint with forwarding rules — allows VPC to forward on-premises domain queries to on-premises DNS', correct: true, explanation: 'Outbound Endpoints with Resolver rules send specific domain queries from the VPC to on-premises DNS servers. Rules specify which domains to forward and to which on-premises DNS IP.' },
      { id: 'c', text: 'Enable DNS hostnames and DNS resolution in the VPC settings', correct: false, explanation: 'These settings control whether EC2 instances get DNS names and whether the VPC uses Route 53 Resolver — they do not configure hybrid DNS forwarding.' },
      { id: 'd', text: 'Create a public hosted zone for the on-premises domain', correct: false, explanation: 'Creating a public hosted zone for internal on-premises domains exposes private DNS to the internet, which is a security risk and not the correct approach.' },
    ],
    explanation: { overall: 'Bidirectional hybrid DNS requires both endpoints: Inbound Endpoint (on-premises queries → AWS) and Outbound Endpoint + forwarding rules (AWS queries → on-premises). Both connect over Direct Connect or VPN. On-premises DNS is configured to forward e.g. "corp.aws.internal" queries to the Inbound Endpoint IP. The Outbound Endpoint forwards e.g. "corp.internal" queries to on-premises DNS.', examTip: 'Hybrid DNS: Inbound = on-premises resolves AWS zones. Outbound + Rules = VPC resolves on-premises zones. Need both for full bidirectional hybrid DNS resolution.' },
    tags: ['route53-resolver', 'inbound-endpoint', 'outbound-endpoint', 'hybrid-dns'],
  },
  {
    id: 'net-014',
    stem: 'A company uses Direct Connect with a Private VIF to connect to a VPC. They need their on-premises application to access an AWS service that only has a VPC Interface Endpoint (PrivateLink). Can the on-premises application access the interface endpoint via Direct Connect?',
    type: 'single', difficulty: 3, topicSlug: 'networking', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'No — VPC Interface Endpoints are only accessible from within the VPC they are created in', correct: false, explanation: 'Interface Endpoints can be accessed from connected networks (Direct Connect, VPN) when the endpoint has private DNS enabled and routing is in place.' },
      { id: 'b', text: 'Yes — Interface Endpoints are accessible from on-premises via Private VIF if the endpoint has private DNS or custom DNS forwarding is configured', correct: true, explanation: 'Traffic from on-premises flows over DX Private VIF → VPC → Interface Endpoint ENI. DNS resolution for the endpoint hostname must resolve to the endpoint private IP, which requires configuring on-premises DNS to forward the service domain to Route 53 Inbound Endpoints.' },
      { id: 'c', text: 'Yes — but only using a Public VIF on Direct Connect', correct: false, explanation: 'Public VIF is for accessing AWS public endpoints (S3, DynamoDB public endpoints). Interface Endpoints are accessed via Private VIF through the VPC network.' },
      { id: 'd', text: 'No — interface endpoints require VPC Gateway Endpoints for hybrid connectivity', correct: false, explanation: 'Gateway Endpoints are only for S3 and DynamoDB. Interface Endpoints are accessible from on-premises with proper routing and DNS configuration.' },
    ],
    explanation: { overall: 'On-premises can access VPC Interface Endpoints via Direct Connect Private VIF (or VPN). Traffic flows: On-premises → DX Private VIF → VPC → Interface Endpoint ENI → AWS service. DNS resolution is the key challenge: on-premises DNS must resolve the service hostname to the endpoint\'s private IP. This requires Route 53 Resolver Inbound Endpoints or custom DNS forwarding configuration.', examTip: 'On-premises → Interface Endpoint via DX: routing is handled by Private VIF. DNS is the challenge — configure on-premises DNS to forward the service domain (e.g., *.s3.us-east-1.amazonaws.com) to Route 53 Inbound Endpoint IPs.' },
    tags: ['direct-connect', 'interface-endpoint', 'privatelink', 'on-premises'],
  },
  {
    id: 'net-015',
    stem: 'A company has a VPC Gateway Endpoint for S3. They want to ensure that EC2 instances in the private subnet can ONLY access their own company S3 buckets through the endpoint, and cannot access other AWS customers\' S3 buckets. What controls this restriction?',
    type: 'single', difficulty: 3, topicSlug: 'networking', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'S3 bucket policy with aws:sourceVpc condition on the company\'s buckets', correct: false, explanation: 'A bucket policy restricts who can access a specific bucket. It does not prevent instances from reaching other customers\' buckets through the endpoint.' },
      { id: 'b', text: 'VPC Endpoint policy restricting s3:* to only the company\'s bucket ARNs', correct: true, explanation: 'A VPC Endpoint policy controls what actions and resources are accessible through the endpoint. Restricting the resource to "arn:aws:s3:::company-bucket/*" prevents access to any other S3 bucket, even if the instance\'s identity policy would allow it.' },
      { id: 'c', text: 'Security Group outbound rules blocking port 443 to S3 public IPs', correct: false, explanation: 'With a Gateway Endpoint, S3 traffic routes through the endpoint, not through public IPs. Security Group rules on public IPs would not prevent endpoint traffic.' },
      { id: 'd', text: 'S3 Block Public Access settings on the company\'s buckets', correct: false, explanation: 'Block Public Access prevents public access to the company\'s buckets but does not restrict what EC2 instances can access through the endpoint.' },
    ],
    explanation: { overall: 'VPC Endpoint policies are IAM resource policies attached to the endpoint itself. They act as a gateway filter — even if an EC2 instance\'s identity policy allows broad S3 access, the endpoint policy restricts what can be accessed through the endpoint. Restricting to specific bucket ARNs prevents data exfiltration to external S3 buckets while allowing access to company resources.', examTip: 'Endpoint policy controls what resources/actions are accessible through the endpoint. Use to prevent data exfiltration to external S3 buckets. Bucket policy (aws:sourceVpce) controls who can access the bucket. Both work together as defense-in-depth.' },
    tags: ['vpc-endpoint', 'endpoint-policy', 's3', 'data-exfiltration'],
  },
  {
    id: 'net-016',
    stem: 'A company connects branch offices to AWS using Site-to-Site VPN. They recently enabled split tunneling on their Client VPN for remote workers. What does split tunneling mean, and what is its impact?',
    type: 'single', difficulty: 2, topicSlug: 'networking', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Split tunneling sends all internet traffic through the VPN, increasing security but reducing performance', correct: false, explanation: 'This describes full tunneling, not split tunneling. Full tunneling routes ALL traffic through VPN.' },
      { id: 'b', text: 'Split tunneling routes only traffic destined for VPC CIDRs through the VPN; internet traffic goes directly from the client', correct: true, explanation: 'With split tunneling enabled, only traffic matching VPC routes goes through the Client VPN tunnel. Internet browsing, streaming, and other traffic goes directly from the client to the internet, reducing VPN bandwidth usage and improving performance.' },
      { id: 'c', text: 'Split tunneling divides the VPN bandwidth equally between users for fair sharing', correct: false, explanation: 'Split tunneling is about route selection (VPC vs internet), not bandwidth allocation between users.' },
      { id: 'd', text: 'Split tunneling creates two separate encrypted tunnels for redundancy', correct: false, explanation: 'Site-to-Site VPN connections always have two tunnels for HA, but that is unrelated to split tunneling.' },
    ],
    explanation: { overall: 'AWS Client VPN split tunneling: when disabled (full tunneling), ALL client traffic routes through the VPN (internet traffic hairpins through AWS). When enabled (split tunneling), only traffic matching configured routes goes through VPN — other traffic goes directly to internet. Benefits: reduced VPN bandwidth consumption, better internet performance for users. Risk: internet traffic from VPN clients is not inspected by corporate security controls.', examTip: 'Client VPN split tunneling: only VPC-destined traffic → VPN. Internet → direct. Reduces AWS VPN egress costs and latency. Full tunneling: ALL traffic → VPN (required for compliance environments needing traffic inspection).' },
    tags: ['client-vpn', 'split-tunneling', 'routing'],
  },
  {
    id: 'net-017',
    stem: 'A company wants to use Direct Connect with BGP communities to control route propagation. They want routes advertised from their on-premises network to be used only in us-east-1 and not advertised to other AWS regions. Which BGP community value achieves this?',
    type: 'single', difficulty: 3, topicSlug: 'networking', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: '7224:9300 — Global: propagate routes to all AWS regions', correct: false, explanation: '7224:9300 causes routes to be propagated globally across all AWS regions, which is the opposite of the requirement.' },
      { id: 'b', text: '7224:9100 — Local AWS Region: routes propagated only within the connected region', correct: true, explanation: 'BGP community 7224:9100 restricts route propagation to the AWS region where the Direct Connect is located. Routes are not advertised to other regions via the AWS backbone.' },
      { id: 'c', text: '7224:9200 — All regions in a continent', correct: false, explanation: '7224:9200 propagates routes to all AWS regions within the same continent (e.g., all of North America).' },
      { id: 'd', text: 'Set the AS-PATH prepend to a high value to reduce route preference', correct: false, explanation: 'AS-PATH prepending affects route preference (less preferred), not propagation scope. BGP communities control where routes are propagated to.' },
    ],
    explanation: { overall: 'AWS Direct Connect BGP communities control route propagation scope from on-premises to AWS: 7224:9100 = Local region only. 7224:9200 = Continental regions. 7224:9300 = Global all regions. When you tag your on-premises routes with these communities when advertising to AWS, AWS respects them to control propagation. This is useful for data sovereignty — ensuring on-premises routes only reach specific regions.', examTip: 'DX BGP communities: 9100=local region, 9200=continent, 9300=global. Attach to on-premises route advertisements to control which AWS regions learn about your on-premises prefixes.' },
    tags: ['direct-connect', 'bgp', 'community', 'route-propagation'],
  },
  {
    id: 'net-018',
    stem: 'A company wants to expose a new microservice running on an NLB in their VPC to several partner AWS accounts. Partners should connect using private IP addresses in their own VPCs, without VPC peering or IP overlap constraints. What is the most scalable approach?',
    type: 'single', difficulty: 2, topicSlug: 'networking', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Create VPC Peering connections between your VPC and each partner VPC', correct: false, explanation: 'VPC Peering requires non-overlapping CIDRs, is bidirectional, and requires creating one peering connection per partner — not scalable to many partners.' },
      { id: 'b', text: 'Create an AWS PrivateLink Endpoint Service backed by the NLB; grant each partner account permission to create an Interface Endpoint', correct: true, explanation: 'PrivateLink Endpoint Services allow you to expose a service to consumers in their own VPCs using private IP addresses. No CIDR constraints, unidirectional (consumers cannot reach your VPC except through the NLB), and scalable to hundreds of consumers.' },
      { id: 'c', text: 'Make the NLB internet-facing and share the DNS name with partners', correct: false, explanation: 'Internet-facing NLB exposes the service to the public internet — not private connectivity.' },
      { id: 'd', text: 'Share the VPC subnet with partner accounts using AWS RAM', correct: false, explanation: 'VPC subnet sharing allows partners to deploy resources into your subnet but does not expose a specific service privately.' },
    ],
    explanation: { overall: 'PrivateLink Endpoint Services is the purpose-built solution for service-provider SaaS patterns. You create an Endpoint Service linked to your NLB. Partner accounts create Interface Endpoints in their VPCs, getting private IPs in their address space. No CIDR overlap concerns, no bidirectional access, you control which accounts can connect. Each partner pays for their endpoint, not you.', examTip: 'PrivateLink Endpoint Services: service provider creates Endpoint Service (backed by NLB). Consumers create Interface Endpoints in their VPCs. Private IP, unidirectional, no CIDR constraints, scales to unlimited consumers.' },
    tags: ['privatelink', 'endpoint-services', 'nlb', 'partner-connectivity'],
  },
  {
    id: 'net-019',
    stem: 'A company has SD-WAN appliances in their data center and wants to connect them to AWS Transit Gateway to extend SD-WAN policies to AWS. They need high throughput (5+ Gbps) and the SD-WAN appliance uses GRE tunnels. Which TGW feature supports this directly?',
    type: 'single', difficulty: 3, topicSlug: 'networking', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Transit Gateway Site-to-Site VPN attachment using IPsec tunnels', correct: false, explanation: 'TGW VPN uses IPsec, not GRE. IPsec VPN tunnels cap at ~1.25 Gbps per tunnel pair.' },
      { id: 'b', text: 'Transit Gateway Connect attachment using GRE tunnels over Direct Connect or VPN', correct: true, explanation: 'TGW Connect is designed for SD-WAN integration. It uses GRE tunnels over an existing DX or VPN transport attachment. Each TGW Connect peer supports up to 5 Gbps. Supports BGP for dynamic routing.' },
      { id: 'c', text: 'AWS Direct Connect with a Transit VIF directly to the SD-WAN appliances', correct: false, explanation: 'Transit VIF connects to TGW using VLAN-tagged 802.1q, not GRE. This works for standard DX but not for SD-WAN GRE tunneling.' },
      { id: 'd', text: 'Deploy a virtual SD-WAN appliance in AWS EC2 and use VPC Peering to connect VPCs', correct: false, explanation: 'This requires deploying and managing virtual appliances in AWS and does not leverage the TGW Connect integration that natively supports GRE-based SD-WAN.' },
    ],
    explanation: { overall: 'TGW Connect enables SD-WAN integration by supporting GRE (Generic Routing Encapsulation) tunnels. A Connect attachment is created on top of an existing DX or VPN attachment. SD-WAN appliances create GRE tunnels to TGW. Each Connect peer supports up to 5 Gbps and BGP dynamic routing. This enables extending SD-WAN policies and routing intelligence to AWS workloads.', examTip: 'TGW Connect = GRE + BGP over existing DX or VPN transport. For SD-WAN integration. Each peer = up to 5 Gbps. Not to be confused with standard VPN (IPsec) or DX attachments.' },
    tags: ['transit-gateway', 'tgw-connect', 'sd-wan', 'gre'],
  },
  {
    id: 'net-020',
    stem: 'A company wants all AWS management API calls (CloudFormation, EC2, S3 management APIs) from their VPC to stay within the AWS network and not go through the internet via NAT Gateway. The VPC does not have a public route. How can they achieve this efficiently for all AWS services?',
    type: 'single', difficulty: 2, topicSlug: 'networking', examDomain: 'cost-optimization',
    options: [
      { id: 'a', text: 'Create a VPC Interface Endpoint for every AWS service they use', correct: false, explanation: 'Creating individual Interface Endpoints for dozens of AWS services is expensive (~$7.30/month/endpoint) and operationally burdensome.' },
      { id: 'b', text: 'Create a single VPC Interface Endpoint for com.amazonaws.REGION.ssm and route all API calls through it', correct: false, explanation: 'SSM endpoint only handles Systems Manager traffic. Other service APIs need their own endpoints.' },
      { id: 'c', text: 'Use AWS PrivateLink with a single endpoint for all AWS APIs via com.amazonaws.REGION.execute-api', correct: false, explanation: 'execute-api is for API Gateway custom APIs, not for AWS management APIs.' },
      { id: 'd', text: 'Create Interface Endpoints for the specific AWS services used (e.g., ec2, s3, cloudformation, sts). For S3 data, use a Gateway Endpoint (free)', correct: true, explanation: 'The correct approach is to create Interface Endpoints for each needed AWS service. S3 and DynamoDB are special — use free Gateway Endpoints for them. Interface Endpoints for management APIs (EC2, CloudFormation, STS, KMS) keep traffic private.' },
    ],
    explanation: { overall: 'For private VPCs without internet access, Interface VPC Endpoints (PrivateLink) provide private connectivity to AWS services. Create endpoints for the specific services needed. S3 and DynamoDB use free Gateway Endpoints. EC2, CloudFormation, STS, KMS, Secrets Manager, SSM etc. each need their own Interface Endpoint. Cost vs. benefit: compare endpoint hourly cost + data transfer vs. NAT Gateway data processing charges.', examTip: 'Private VPC API access: Gateway Endpoints (free) for S3 + DynamoDB. Interface Endpoints for all other services. Calculate cost: endpoint = ~$7.30/month. NAT Gateway = $0.045/hr + $0.045/GB. For high-traffic services, endpoint is cheaper.' },
    tags: ['vpc-endpoint', 'interface-endpoint', 'gateway-endpoint', 'private-vpc'],
  },
];
