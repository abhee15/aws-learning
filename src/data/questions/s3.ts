import type { Question } from '../../types/question';

export const s3Questions: Question[] = [
  {
    id: 's3-001',
    stem: 'A healthcare company must store patient records in S3 for 10 years and ensure no record can be deleted or overwritten during that period, including by the AWS account root user, to meet HIPAA compliance. Which S3 feature and mode should they use?',
    type: 'single', difficulty: 2, topicSlug: 's3', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Enable S3 Object Lock in Governance mode with a 10-year retention period', correct: false, explanation: 'Governance mode allows users with s3:BypassGovernanceRetention permission to delete objects. Does not meet strict regulatory requirements.' },
      { id: 'b', text: 'Enable S3 Object Lock in Compliance mode with a 10-year retention period', correct: true, explanation: 'Compliance mode enforces WORM — no user, including root, can delete or overwrite locked objects before retention expires. Meets HIPAA and SEC 17a-4 requirements.' },
      { id: 'c', text: 'Use S3 MFA Delete on versioned objects', correct: false, explanation: 'MFA Delete adds extra authentication but does not enforce a time-based retention period.' },
      { id: 'd', text: 'Implement an S3 bucket policy that denies all delete actions', correct: false, explanation: 'Bucket policies can be modified by account administrators and do not provide immutable WORM compliance.' },
    ],
    explanation: { overall: 'S3 Object Lock Compliance mode provides WORM protection that cannot be overridden by any user including AWS support. The bucket must have versioning enabled.', examTip: 'Compliance mode = absolute WORM, even root cannot delete. Governance mode = overridable with s3:BypassGovernanceRetention. For regulatory compliance (SEC, FINRA, HIPAA), always Compliance mode.' },
    tags: ['object-lock', 'worm', 'compliance', 'hipaa'],
  },
  {
    id: 's3-002',
    stem: 'A media company stores large video files in S3 Standard. After 30 days, files are rarely accessed. After 90 days, they are almost never accessed. After 1 year, they must be retained for 7 more years but rarely retrieved. What is the most cost-effective S3 lifecycle configuration?',
    type: 'single', difficulty: 2, topicSlug: 's3', examDomain: 'cost-optimization',
    options: [
      { id: 'a', text: 'S3 Standard → after 30 days S3 Standard-IA → after 90 days S3 Glacier Instant Retrieval → after 1 year S3 Glacier Deep Archive', correct: true, explanation: 'This transitions through storage classes as access frequency decreases. Standard-IA for infrequent access. Glacier Instant Retrieval for rarely accessed but fast retrieval needed. Glacier Deep Archive for long-term archival at lowest cost ($0.00099/GB/month).' },
      { id: 'b', text: 'Keep in S3 Standard for 8 years then delete', correct: false, explanation: 'S3 Standard is significantly more expensive than archive tiers. For rarely accessed data, lifecycle transitions save up to 90%.' },
      { id: 'c', text: 'S3 Standard → after 30 days S3 One Zone-IA → after 1 year S3 Glacier', correct: false, explanation: 'S3 One Zone-IA stores data in a single AZ. For compliance/archival data, multi-AZ storage (Glacier Deep Archive) is safer.' },
      { id: 'd', text: 'Enable S3 Intelligent-Tiering for all objects', correct: false, explanation: 'Intelligent-Tiering is good for unpredictable access but adds per-object monitoring fee. For predictable access patterns, lifecycle rules are more cost-effective.' },
    ],
    explanation: { overall: 'S3 lifecycle transitions follow the access pattern: Standard → Standard-IA (30 days min) → Glacier Instant/Flexible Retrieval → Glacier Deep Archive. Each tier reduces cost as access frequency decreases. Glacier Deep Archive is the cheapest long-term storage at ~$0.001/GB/month.', examTip: 'Minimum days before transition: Standard → Standard-IA or One Zone-IA: 30 days. Standard-IA/One Zone-IA → Glacier: 30 days. Glacier Deep Archive has a minimum 180-day storage duration billed.' },
    tags: ['lifecycle', 'glacier', 'cost-optimization', 'storage-classes'],
  },
  {
    id: 's3-003',
    stem: 'A company hosts a static website on S3. Users in Asia experience slow load times because the S3 bucket is in us-east-1. They want to reduce latency for global users with minimal infrastructure changes. Which solution is best?',
    type: 'single', difficulty: 1, topicSlug: 's3', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Enable S3 Transfer Acceleration on the bucket', correct: false, explanation: 'Transfer Acceleration speeds up uploads TO S3 via CloudFront edge locations, not downloads FROM S3.' },
      { id: 'b', text: 'Put CloudFront in front of the S3 bucket as the origin', correct: true, explanation: 'CloudFront caches content at edge locations worldwide. Asian users retrieve content from the nearest edge (low latency) instead of us-east-1 (high latency). CloudFront also provides HTTPS and caching benefits.' },
      { id: 'c', text: 'Create S3 buckets in multiple regions and use Route 53 latency routing', correct: false, explanation: 'Multi-region S3 with Route 53 requires S3 Cross-Region Replication and managing multiple buckets. CloudFront is simpler and more effective for read-heavy static content.' },
      { id: 'd', text: 'Enable S3 Cross-Region Replication to a bucket in Asia', correct: false, explanation: 'CRR copies objects to another region but users still connect to the regional endpoint. CloudFront with edge caching provides better performance.' },
    ],
    explanation: { overall: 'CloudFront + S3: CloudFront caches S3 objects at 400+ edge locations globally. First request to each edge fetches from S3 origin; subsequent requests are served from cache. TTL controls cache duration. Origin Access Control (OAC) restricts bucket access to CloudFront only.', examTip: 'S3 Transfer Acceleration = accelerate UPLOADS via edge. CloudFront = accelerate DOWNLOADS/reads via edge caching. For static website performance, CloudFront is the right answer.' },
    tags: ['cloudfront', 's3-static-website', 'global-performance', 'cdn'],
  },
  {
    id: 's3-004',
    stem: 'A company uses S3 to store sensitive financial data. They require that all objects must be encrypted with keys they manage themselves, and they want to audit every key usage. Which encryption configuration meets this?',
    type: 'single', difficulty: 2, topicSlug: 's3', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'SSE-S3 (Server-Side Encryption with S3-managed keys)', correct: false, explanation: 'SSE-S3 uses AWS-managed keys. The customer has no visibility into key usage or control over key material.' },
      { id: 'b', text: 'SSE-KMS with a Customer Managed Key (CMK) and CloudTrail logging of KMS API calls', correct: true, explanation: 'SSE-KMS with CMK: customer controls key policy, rotation, and deletion. Every S3 decrypt operation calls KMS, and CloudTrail logs every KMS API call providing full audit trail.' },
      { id: 'c', text: 'SSE-C (Server-Side Encryption with Customer-Provided Keys)', correct: false, explanation: 'SSE-C uses keys provided by the customer per request. There is no KMS audit trail. Customers must manage and store the keys externally.' },
      { id: 'd', text: 'Client-Side Encryption with application-managed keys', correct: false, explanation: 'Client-side encryption provides key control but requires application changes and does not integrate with S3 bucket policies or KMS audit logging natively.' },
    ],
    explanation: { overall: 'SSE-KMS with CMK provides: customer-controlled key material, key policy for access control, automatic key rotation (optional), and full CloudTrail audit logging of every encrypt/decrypt call. The bucket default encryption setting can enforce SSE-KMS on all new objects.', examTip: 'S3 encryption options: SSE-S3 (AWS keys, simplest), SSE-KMS (CMK, audit trail, access control), SSE-C (customer provides key per request, complex), Client-side (application encrypts before upload). For compliance with key audit requirements, SSE-KMS with CMK.' },
    tags: ['encryption', 'kms', 'cmk', 'audit', 'compliance'],
  },
  {
    id: 's3-005',
    stem: 'A company has a multi-account AWS Organization. They want to enforce that all S3 buckets created in member accounts must have Block Public Access enabled. If a bucket is created without it, it should be automatically remediated. Which approach achieves this?',
    type: 'single', difficulty: 2, topicSlug: 's3', examDomain: 'organizational-complexity',
    options: [
      { id: 'a', text: 'SCP that denies creation of S3 buckets without Block Public Access', correct: false, explanation: 'SCPs can deny actions but there is no SCP condition key that checks S3 Block Public Access settings at creation time.' },
      { id: 'b', text: 'AWS Config rule s3-bucket-level-public-access-prohibited with auto-remediation via SSM Automation', correct: true, explanation: 'AWS Config has a managed rule s3-bucket-level-public-access-prohibited that detects non-compliant buckets. Auto-remediation using SSM Automation document AWS-ConfigureS3BucketPublicAccessBlock automatically enables BPA on non-compliant buckets.' },
      { id: 'c', text: 'CloudFormation Guard rules deployed via StackSets to all accounts', correct: false, explanation: 'CloudFormation Guard validates CloudFormation templates, not buckets created via console or CLI directly.' },
      { id: 'd', text: 'S3 Organization-level Block Public Access settings', correct: true, explanation: 'S3 has an Organization-level Block Public Access setting that can be applied to all accounts in the Organization, preventing any account from making buckets or objects public.' },
    ],
    explanation: { overall: 'Two valid approaches: (1) Organization-level S3 Block Public Access (simplest — one setting applies to all accounts). (2) AWS Config with auto-remediation (catches buckets created before the organization setting, provides audit trail). For SA Pro, know that organization-level BPA is the simplest preventive control.', examTip: 'S3 Block Public Access: can be set at object, bucket, account, or Organization level. Organization-level setting overrides all — even account admins cannot remove it. Use for org-wide public access prevention.' },
    tags: ['block-public-access', 'organizations', 'config', 'remediation'],
  },
  {
    id: 's3-006',
    stem: 'A company needs to replicate S3 objects from a source bucket in us-east-1 to a destination bucket in eu-west-1 for disaster recovery. They also need to ensure that delete markers in the source bucket are NOT replicated to the destination (to protect against accidental mass deletions). How should they configure this?',
    type: 'single', difficulty: 2, topicSlug: 's3', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Enable Cross-Region Replication with delete marker replication disabled', correct: true, explanation: 'CRR by default does not replicate delete markers (they must be explicitly enabled). Leaving delete marker replication disabled protects the destination from accidental deletions in the source.' },
      { id: 'b', text: 'Enable CRR with delete marker replication enabled', correct: false, explanation: 'Enabling delete marker replication would replicate accidental deletions to the destination bucket, defeating the DR purpose.' },
      { id: 'c', text: 'Use S3 Batch Replication to copy objects between regions', correct: false, explanation: 'S3 Batch Replication copies existing objects on-demand. CRR is for continuous replication of new objects.' },
      { id: 'd', text: 'Configure an S3 lifecycle rule to copy objects to the destination region', correct: false, explanation: 'Lifecycle rules manage object transitions and expirations within the same account. They do not replicate objects to other regions.' },
    ],
    explanation: { overall: 'S3 Cross-Region Replication (CRR): replicates new objects (and optionally existing via Batch Replication) to another region. Delete marker replication is OFF by default — a best practice for DR. If an admin accidentally deletes objects in source, the destination remains intact.', examTip: 'CRR vs SRR: CRR = cross-region (DR, latency). SRR = same-region (data sovereignty, log aggregation). Neither replicates delete markers by default. Enable Replication Time Control (RTC) for 99.99% replication within 15 minutes SLA.' },
    tags: ['crr', 'replication', 'disaster-recovery', 'delete-markers'],
  },
  {
    id: 's3-007',
    stem: 'A data lake on S3 has petabytes of data. A company wants to query the data using SQL without loading it into a database. Query performance on large tables is poor due to full file scans. Which optimization significantly improves query performance?',
    type: 'multiple', difficulty: 3, topicSlug: 's3', examDomain: 'continuous-improvement',
    options: [
      { id: 'a', text: 'Partition the S3 data by date (year/month/day) and use Athena partition projection', correct: true, explanation: 'Partition pruning means Athena only scans partitions matching the query WHERE clause, dramatically reducing data scanned and cost.' },
      { id: 'b', text: 'Convert data to columnar format (Parquet or ORC)', correct: true, explanation: 'Columnar formats enable column pruning (only selected columns are read) and better compression. Parquet with Snappy compression can reduce data scanned by 10-100x vs CSV.' },
      { id: 'c', text: 'Enable S3 Requester Pays to reduce the cost per query', correct: false, explanation: 'S3 Requester Pays shifts data transfer cost to the requester, but does not improve query performance.' },
      { id: 'd', text: 'Increase Athena query result DPU (Data Processing Units)', correct: false, explanation: 'Athena is serverless and automatically scales. DPUs are an Apache Spark concept in Glue, not a tunable parameter in Athena.' },
    ],
    explanation: { overall: 'Athena cost and performance optimizations: (1) Partitioning: reduces files scanned. (2) Columnar format (Parquet/ORC): reduces bytes scanned per query. (3) Compression: Snappy/GZIP reduces data size. (4) File sizing: avoid many small files (use 128MB+ files). Together these can reduce query time and cost by 10-100x.', examTip: 'Athena pricing: $5 per TB of data scanned. Every optimization that reduces bytes scanned = cost reduction. Parquet + partitioning is the most impactful combination. Glue Data Catalog for partition management.' },
    tags: ['athena', 'parquet', 'partitioning', 'performance', 'data-lake'],
  },
  {
    id: 's3-008',
    stem: 'A company uses S3 as the source for a data pipeline. Multiple analytics teams consume data from the same bucket. They notice that when one team runs large queries, other teams experience slower S3 performance. Which S3 feature addresses request rate limitations?',
    type: 'single', difficulty: 2, topicSlug: 's3', examDomain: 'continuous-improvement',
    options: [
      { id: 'a', text: 'Enable S3 Transfer Acceleration to increase throughput', correct: false, explanation: 'Transfer Acceleration uses edge locations to speed up uploads, not concurrent read/write operations within a bucket.' },
      { id: 'b', text: 'Use a consistent prefix naming strategy to distribute requests across S3 partitions', correct: true, explanation: 'S3 scales to 3,500 PUT and 5,500 GET requests per second per prefix. Using unique prefixes (e.g., date-team-randomhash/) distributes load across multiple partitions, increasing aggregate throughput.' },
      { id: 'c', text: 'Enable S3 intelligent-tiering to automatically manage storage performance', correct: false, explanation: 'Intelligent-Tiering manages cost optimization by moving objects between tiers. It does not affect request throughput.' },
      { id: 'd', text: 'Create multiple S3 buckets for different teams', correct: false, explanation: 'Multiple buckets separate teams but are a management overhead. S3 can handle high request rates with proper prefix design in a single bucket.' },
    ],
    explanation: { overall: 'S3 performance scales per prefix. Each unique prefix supports 3,500 PUT/COPY/POST/DELETE + 5,500 GET/HEAD requests per second. With multiple prefixes, you get aggregate higher throughput. Modern S3 (after 2018) does not require random prefix hashing — S3 now automatically partitions based on prefix patterns.', examTip: 'S3 request rate: 5,500 GET per second PER PREFIX. Distribute objects across prefixes for higher aggregate throughput. Also: use S3 parallel multipart uploads for large objects (>100MB use multipart, >5GB required).' },
    tags: ['s3-performance', 'prefix', 'request-rate', 'throughput'],
  },
  {
    id: 's3-009',
    stem: 'A company wants to analyze S3 access patterns to determine which objects to transition to cheaper storage tiers. They want to see object-level access frequency over the past 30 days. Which S3 feature provides this?',
    type: 'single', difficulty: 1, topicSlug: 's3', examDomain: 'cost-optimization',
    options: [
      { id: 'a', text: 'S3 Storage Lens with advanced metrics enabled', correct: true, explanation: 'S3 Storage Lens provides organization-wide visibility with metrics including object-level access frequency, data age, and storage usage. Advanced metrics (paid) include activity metrics like request counts per prefix.' },
      { id: 'b', text: 'S3 Server Access Logging with CloudWatch metrics', correct: false, explanation: 'Server access logs provide raw request logs but require Athena/S3 Select for analysis. Storage Lens provides pre-built analytics dashboards.' },
      { id: 'c', text: 'AWS Cost Explorer S3 storage breakdown', correct: false, explanation: 'Cost Explorer shows storage cost by bucket/storage class but not object-level access frequency.' },
      { id: 'd', text: 'S3 Inventory reports showing object metadata', correct: false, explanation: 'S3 Inventory lists objects with metadata (size, encryption, last modified) but not access frequency/request counts.' },
    ],
    explanation: { overall: 'S3 Storage Lens: organization-wide storage analytics. Free tier: 28 usage metrics. Advanced (paid): adds 35+ usage and activity metrics, including request counts, bytes uploaded/downloaded per bucket/prefix, data age analysis. Dashboard updated daily. Export to S3 for custom analysis.', examTip: 'S3 Storage Lens = analytics and insights. S3 Inventory = object metadata list. Server Access Logs = raw API logs. Use Storage Lens for at-a-glance cost optimization opportunities. Intelligent-Tiering automates the transition.' },
    tags: ['storage-lens', 'analytics', 'cost-optimization', 'access-patterns'],
  },
  {
    id: 's3-010',
    stem: 'A company uses S3 Event Notifications to trigger Lambda when objects are uploaded. They are receiving duplicate Lambda invocations for the same upload event. What is the most likely cause?',
    type: 'single', difficulty: 3, topicSlug: 's3', examDomain: 'continuous-improvement',
    options: [
      { id: 'a', text: 'S3 event notifications are configured to trigger on both ObjectCreated:Put and ObjectCreated:CompleteMultipartUpload', correct: false, explanation: 'This would cause duplicates only if both events fire for the same upload, which would be two distinct event types, not duplicate events.' },
      { id: 'b', text: 'S3 event notifications deliver events at least once, and Lambda must be idempotent', correct: true, explanation: 'S3 event notifications have at-least-once delivery semantics. Rare duplicate delivery is possible. Lambda functions triggered by S3 should be idempotent — processing the same event twice should not cause issues (e.g., check if already processed using DynamoDB conditional writes).' },
      { id: 'c', text: 'Lambda is configured with too high concurrency, processing the same event in parallel', correct: false, explanation: 'Lambda concurrency controls parallel invocations, but each invocation receives a unique event. The issue is S3 event delivery semantics, not Lambda concurrency.' },
      { id: 'd', text: 'The Lambda function timeout is too short, causing the event to be retried', correct: false, explanation: 'Lambda timeout causing retry would result in function re-invocations, but those would be separate events, not S3 delivering the same event twice.' },
    ],
    explanation: { overall: 'S3 event notifications use at-least-once delivery. In rare cases, the same event may be delivered more than once. Lambda should be designed idempotently: use DynamoDB with condition expressions to track processed object keys, or check if processing has already occurred before taking action.', examTip: 'At-least-once delivery: S3 events, SQS standard queues, SNS, EventBridge. Exactly-once: SQS FIFO queues within deduplication window. Always design downstream consumers for idempotency when using at-least-once sources.' },
    tags: ['s3-events', 'lambda', 'idempotency', 'at-least-once'],
  },
  {
    id: 's3-011',
    stem: 'A company wants users to upload large files (up to 5 GB) directly to S3 from a browser without routing through their servers. They need to restrict uploads to authenticated users and specific file types. Which approach enables this securely?',
    type: 'single', difficulty: 2, topicSlug: 's3', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Give users IAM credentials with S3 PutObject permission', correct: false, explanation: 'Distributing IAM credentials to users is a security anti-pattern. Credentials could be compromised.' },
      { id: 'b', text: 'Use S3 Presigned URLs generated by the backend for authenticated users', correct: true, explanation: 'The backend generates time-limited presigned URLs after authenticating the user. The presigned URL embeds the S3 bucket, key, expiration, and IAM role signature. Users upload directly to S3 using the presigned URL. Content-Type can be specified in the presigned URL.' },
      { id: 'c', text: 'Enable S3 website hosting with CORS to accept browser uploads', correct: false, explanation: 'CORS is required for browser-based S3 uploads but alone does not provide authentication. It must be combined with presigned URLs or Cognito.' },
      { id: 'd', text: 'Use API Gateway as a proxy for S3 uploads', correct: false, explanation: 'API Gateway has a 10 MB payload limit, making it unsuitable for 5 GB file uploads.' },
    ],
    explanation: { overall: 'Presigned URLs: backend generates a URL with embedded credentials and expiration (up to 7 days or role session duration). The user uploads directly to S3 without going through the backend server — offloading bandwidth/compute. Restrictions: specific object key prefix, content-type, max file size (with presigned POST policies).', examTip: 'Presigned URLs: signed by IAM entity\'s credentials, valid for up to 7 days (role sessions limited by session duration). Presigned POST: upload from HTML forms with conditions (size limit, content type, prefix). Both avoid routing large files through your servers.' },
    tags: ['presigned-url', 'upload', 'security', 'direct-upload'],
  },
  {
    id: 's3-012',
    stem: 'A media company has millions of small files (10-100 KB each) in S3. They notice slow Athena query performance and high costs. The files are log files aggregated per minute. What is the recommended remediation?',
    type: 'single', difficulty: 2, topicSlug: 's3', examDomain: 'continuous-improvement',
    options: [
      { id: 'a', text: 'Compress individual files with GZIP', correct: false, explanation: 'Compressing individual small files reduces their size but does not address the small file problem — Athena still opens millions of files, which has high overhead.' },
      { id: 'b', text: 'Compact small files into larger Parquet files (128 MB+) using AWS Glue ETL', correct: true, explanation: 'Small file problem: S3 LIST + open overhead for millions of files is expensive. Compacting into larger columnar files (128MB-1GB) reduces file count, improves Athena scan efficiency, and saves cost. Glue ETL can automate this.' },
      { id: 'c', text: 'Move files to S3 Intelligent-Tiering', correct: false, explanation: 'Intelligent-Tiering optimizes storage cost but does not address query performance or small file overhead.' },
      { id: 'd', text: 'Create an S3 Batch Operations job to merge files', correct: false, explanation: 'S3 Batch Operations performs per-object operations (copy, tag, restore, invoke Lambda). It does not merge/compact files.' },
    ],
    explanation: { overall: 'Small files problem: Athena charges per byte scanned and has overhead per file opened. Millions of small files → high overhead and cost. Solution: compact into larger Parquet files using Glue ETL. Target: 128MB-1GB per file with Snappy compression. Also use partitioning to organize by date/region.', examTip: 'S3 data lake best practices: file size 128MB-1GB, columnar format (Parquet/ORC), Snappy compression, partitioned by query predicates. Compaction = periodic job to merge small files. Glue or EMR for compaction jobs.' },
    tags: ['small-files', 'compaction', 'athena', 'glue', 'performance'],
  },
  {
    id: 's3-013',
    stem: 'A company needs to provide an external auditor with temporary read access to specific S3 objects for 48 hours. The auditor has their own AWS account. Which method provides scoped, temporary access without creating IAM users?',
    type: 'single', difficulty: 2, topicSlug: 's3', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Generate S3 presigned URLs for each required object with 48-hour expiration', correct: true, explanation: 'Presigned URLs provide time-limited access to specific objects without requiring AWS credentials. Share the URLs with the auditor. Access expires automatically after 48 hours. No IAM user creation needed.' },
      { id: 'b', text: 'Create an IAM user with S3 read permissions and provide the access keys', correct: false, explanation: 'Creating IAM users for temporary external access is a security anti-pattern. The user and keys must be manually deleted after 48 hours.' },
      { id: 'c', text: 'Configure a cross-account IAM role and share the role ARN with the auditor', correct: false, explanation: 'Cross-account role assumption requires the auditor\'s AWS identity to assume the role. For an external auditor, presigned URLs are simpler and do not require their AWS account to be configured.' },
      { id: 'd', text: 'Make the S3 objects temporarily public and share the object URLs', correct: false, explanation: 'Making objects public is a security violation. Presigned URLs provide private, scoped access.' },
    ],
    explanation: { overall: 'Presigned URLs embed the signing IAM entity\'s authorization and expire automatically. For bulk sharing, S3 Presigned URL + a list can work. For AWS-to-AWS, cross-account roles are better. For non-AWS external parties needing specific objects, presigned URLs are the standard approach.', examTip: 'Presigned URL expiration: up to 7 days for IAM user credentials; limited by role session duration for IAM roles (default 1 hour, max 12 hours). Plan ahead: for 48h, use IAM user-signed presigned URLs.' },
    tags: ['presigned-url', 'external-access', 'temporary-access', 'auditor'],
  },
  {
    id: 's3-014',
    stem: 'A company uploads files to S3 and processes them with Lambda. Some files arrive in bursts causing Lambda throttling. They want to decouple the upload event from processing. Which architecture achieves this?',
    type: 'single', difficulty: 2, topicSlug: 's3', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'S3 → SNS → SQS → Lambda (fan-out to queue)', correct: true, explanation: 'S3 → SNS topic → SQS queue → Lambda. SNS fan-out decouples S3 from processing. SQS buffers bursts. Lambda polls SQS and processes at its own rate. Provides buffering, retry, and dead-letter queue for failed processing.' },
      { id: 'b', text: 'S3 Event Notification directly to Lambda', correct: false, explanation: 'Direct S3 → Lambda has no buffering. During burst uploads, Lambda is throttled and events may be lost.' },
      { id: 'c', text: 'S3 → EventBridge → Lambda directly', correct: false, explanation: 'EventBridge + Lambda has better error handling than direct S3 events but still lacks the buffering that SQS provides for burst scenarios.' },
      { id: 'd', text: 'S3 → Kinesis Data Streams → Lambda', correct: false, explanation: 'Kinesis provides buffering and ordering but is designed for streaming data, not event-driven object processing. SQS is simpler for S3 event queuing.' },
    ],
    explanation: { overall: 'S3 → SNS → SQS → Lambda is the standard decoupled pattern. SNS enables fan-out (multiple queues for different processors). SQS buffers the burst and Lambda polls at a controlled rate. DLQ captures processing failures. This is more resilient than direct S3 → Lambda.', examTip: 'SQS provides buffering for bursty workloads. Lambda polling SQS: 5 parallel pollers, each processing up to 10 messages in batch. For FIFO processing use SQS FIFO. DLQ for failed messages after maxReceiveCount retries.' },
    tags: ['s3-events', 'sqs', 'sns', 'lambda', 'decoupling'],
  },
  {
    id: 's3-015',
    stem: 'A company discovers that a public S3 bucket was accidentally created containing sensitive data. They want to immediately remediate this AND ensure it never happens again across all 200 accounts in their Organization. What is the most comprehensive solution?',
    type: 'multiple', difficulty: 2, topicSlug: 's3', examDomain: 'organizational-complexity',
    options: [
      { id: 'a', text: 'Enable S3 Block Public Access at the AWS Organizations level', correct: true, explanation: 'Organization-level S3 Block Public Access prevents any account from making buckets or objects public. It overrides account-level settings.' },
      { id: 'b', text: 'Use AWS Config with the s3-bucket-public-read-prohibited rule and auto-remediation for immediate and future detection', correct: true, explanation: 'Config rule detects existing non-compliant buckets and auto-remediation enables Block Public Access on them. Catches buckets created before the organization BPA setting.' },
      { id: 'c', text: 'Rotate the IAM credentials of all users who had S3 access', correct: false, explanation: 'Rotating credentials does not address the public bucket configuration. The bucket is still public until BPA is enabled.' },
      { id: 'd', text: 'Enable S3 Block Public Access only on the specific bucket that was exposed', correct: false, explanation: 'Fixing only the exposed bucket does not prevent future exposure across 200 accounts. Organization-level controls are needed.' },
    ],
    explanation: { overall: 'Comprehensive remediation: (1) Organization-level S3 BPA prevents future public buckets. (2) AWS Config rule + auto-remediation fixes existing non-compliant buckets across all accounts. (3) For the specific exposed bucket: rotate any exposed credentials, audit CloudTrail for unauthorized access.', examTip: 'Security incident response: immediate containment → eradication → prevention. Organization-level S3 BPA = prevention. Config auto-remediation = both remediation and ongoing prevention. Also check CloudTrail for data exfiltration.' },
    tags: ['block-public-access', 'security', 'organizations', 'config', 'remediation'],
  },
  {
    id: 's3-016',
    stem: 'A company uses S3 as a data lake. Multiple AWS accounts in the organization upload data to a central S3 bucket. They need fine-grained access control where different teams can only access their own data prefix (e.g., /team-a/ vs /team-b/). Which configuration enables this?',
    type: 'single', difficulty: 3, topicSlug: 's3', examDomain: 'organizational-complexity',
    options: [
      { id: 'a', text: 'Create separate S3 buckets per team', correct: false, explanation: 'Separate buckets per team work but create management overhead for a shared data lake. Prefix-based access in a single bucket is more manageable.' },
      { id: 'b', text: 'Use S3 bucket policy with conditions on s3:prefix to allow each team only their prefix', correct: true, explanation: 'Bucket policy with s3:prefix condition key restricts GetObject/PutObject to specific prefix paths. Combined with IAM roles per team, this provides fine-grained prefix-level isolation in a single bucket.' },
      { id: 'c', text: 'Use S3 Access Points, one per team, each with a policy restricting access to the team\'s prefix', correct: true, explanation: 'S3 Access Points provide individual entry points with custom policies. Each team gets an Access Point that restricts their access to their prefix. Simpler to manage than complex bucket policies.' },
      { id: 'd', text: 'Enable S3 Object Ownership to restrict access by object ownership', correct: false, explanation: 'Object Ownership controls whether the bucket owner or object uploader owns objects. It does not provide prefix-based access control.' },
    ],
    explanation: { overall: 'Two valid approaches for prefix-based access: (1) Bucket policy with s3:prefix conditions — works but complex bucket policy. (2) S3 Access Points: each team gets a dedicated Access Point URL with its own policy. Access Points are easier to manage, scale, and audit for large numbers of teams.', examTip: 'S3 Access Points: simplify managing access to shared datasets. Each access point has its own hostname and policy. Policies can restrict to specific prefixes, VPCs, or account conditions. Bucket policy must allow access points (or grant via bucket policy).' },
    tags: ['access-points', 'prefix', 'access-control', 'data-lake'],
  },
  {
    id: 's3-017',
    stem: 'A company stores regulatory documents in S3 with a 7-year retention requirement. They want to use S3 Intelligent-Tiering to manage costs automatically. However, documents must not be deleted before 7 years even accidentally. How do they combine both requirements?',
    type: 'single', difficulty: 3, topicSlug: 's3', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'S3 Intelligent-Tiering with Object Lock Governance mode retention for 7 years', correct: false, explanation: 'Governance mode can be bypassed by privileged users. Compliance mode is required for regulatory documents.' },
      { id: 'b', text: 'S3 Intelligent-Tiering combined with S3 Object Lock Compliance mode with 7-year retention', correct: true, explanation: 'S3 Intelligent-Tiering and Object Lock can be used together. Intelligent-Tiering automatically moves infrequently accessed objects to cheaper tiers. Object Lock Compliance ensures 7-year WORM protection. Both are set on the bucket at creation.' },
      { id: 'c', text: 'S3 Standard with lifecycle policy to Glacier after 7 years', correct: false, explanation: 'Lifecycle to Glacier after 7 years transitions the objects but does not provide WORM protection against deletion before 7 years.' },
      { id: 'd', text: 'Object Lock cannot be used with Intelligent-Tiering', correct: false, explanation: 'S3 Object Lock and Intelligent-Tiering are compatible. Both features can be enabled on the same bucket.' },
    ],
    explanation: { overall: 'S3 Intelligent-Tiering + Object Lock: The bucket enables versioning (required for Object Lock), Intelligent-Tiering (cost optimization via automatic tier transitions), and Object Lock (Compliance mode, 7-year retention). Intelligent-Tiering works across Archive Access and Deep Archive tiers too.', examTip: 'Intelligent-Tiering tiers: Frequent Access (default), Infrequent Access (30 days), Archive Instant Access (90 days), Archive Access (optional, 90-730 days), Deep Archive Access (optional, 180-730 days). Object Lock is compatible with all storage classes including Intelligent-Tiering.' },
    tags: ['intelligent-tiering', 'object-lock', 'compliance', 'cost-optimization'],
  },
  {
    id: 's3-018',
    stem: 'A company runs a serverless application where Lambda functions need to share temporary state between invocations (e.g., a processing status flag). They consider using an S3 object for this purpose. What is the risk and recommended alternative?',
    type: 'single', difficulty: 2, topicSlug: 's3', examDomain: 'continuous-improvement',
    options: [
      { id: 'a', text: 'S3 is not eventually consistent, so reads may return stale data; use DynamoDB instead', correct: false, explanation: 'Since December 2020, S3 provides strong read-after-write consistency for all operations. Eventual consistency is no longer an issue for S3.' },
      { id: 'b', text: 'S3 GetObject/PutObject operations have higher latency than in-memory stores; use ElastiCache or DynamoDB for low-latency state sharing', correct: true, explanation: 'S3 API calls have 10-100ms latency, making them unsuitable for high-frequency state sharing between Lambda invocations. DynamoDB or ElastiCache (Redis) provide sub-millisecond latency and atomic operations (DynamoDB Conditional Writes).' },
      { id: 'c', text: 'S3 does not support atomic operations; use SQS for state coordination', correct: false, explanation: 'SQS is a messaging queue, not a state store. While S3 lacks atomic compare-and-swap, DynamoDB conditional writes provide atomic operations for state management.' },
      { id: 'd', text: 'S3 cannot handle concurrent Lambda invocations; use a relational database', correct: false, explanation: 'S3 handles extremely high concurrency. The issue is latency and lack of atomic operations, not concurrency limitations.' },
    ],
    explanation: { overall: 'S3 strong consistency (since 2020): reads after writes see the latest version. However, S3 is not suitable for high-frequency state updates due to latency and no atomic compare-and-swap. Use DynamoDB for: atomic conditional updates, low latency, per-item TTL. Use ElastiCache for: sub-ms latency, ephemeral state.', examTip: 'S3 consistency model: strong read-after-write for all operations (PutObject, DeleteObject, ListObjects). No eventual consistency issues anymore. But S3 is not a database — use DynamoDB/ElastiCache for state, counters, and distributed locks.' },
    tags: ['s3-consistency', 'dynamodb', 'state-management', 'lambda'],
  },
  {
    id: 's3-019',
    stem: 'A company needs to ensure that all data uploaded to an S3 bucket is validated and transformed before being stored (e.g., virus scan, PII detection, format conversion). Which architecture achieves this transparently without requiring application changes?',
    type: 'single', difficulty: 3, topicSlug: 's3', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'S3 Object Lambda: intercept GetObject/PutObject calls and run Lambda to transform', correct: false, explanation: 'S3 Object Lambda intercepts GetObject (read-time transformation), not PutObject. It transforms data when read, not when written.' },
      { id: 'b', text: 'S3 Event Notification → Lambda for validation → S3 CopyObject to "validated" bucket if passes', correct: true, explanation: 'This is the standard validation pattern: objects land in a "raw" bucket, Lambda validates/transforms, and copies passing objects to a "validated" bucket. Applications only read from the validated bucket.' },
      { id: 'c', text: 'S3 Batch Operations to scan all objects after upload', correct: false, explanation: 'Batch Operations processes existing objects in batches — not real-time validation of new uploads.' },
      { id: 'd', text: 'S3 Access Points with a Lambda authorization policy', correct: false, explanation: 'S3 Access Point policies control access but do not intercept or transform objects during upload.' },
    ],
    explanation: { overall: 'Pre-upload validation pattern: raw bucket → S3 event → Lambda (validate/transform) → write to validated bucket or delete if invalid. Applications only access the validated bucket. Variations: use SQS between S3 event and Lambda for burst buffering. For inline transformation on reads, use S3 Object Lambda.', examTip: 'S3 Object Lambda: transforms data on GetObject (read-side). Good for: PII redaction on read, format conversion on read. For write-side validation: event notification + Lambda + two-bucket pattern.' },
    tags: ['s3-events', 'lambda', 'validation', 'two-bucket-pattern'],
  },
  {
    id: 's3-020',
    stem: 'A financial institution has an S3 bucket containing trading records. They use S3 versioning. An audit requires that they be able to retrieve any version of any object for the past 5 years. However, they want to minimize storage costs for old versions. Which configuration achieves this?',
    type: 'single', difficulty: 2, topicSlug: 's3', examDomain: 'cost-optimization',
    options: [
      { id: 'a', text: 'S3 Versioning enabled; lifecycle policy to expire all versions older than 5 years', correct: false, explanation: 'Expiring versions older than 5 years meets retention requirements. But without transitioning older versions to cheaper storage classes, current versions are expensive to keep.' },
      { id: 'b', text: 'S3 Versioning enabled; lifecycle policy to transition non-current versions to Glacier Deep Archive after 30 days; expire versions older than 5 years', correct: true, explanation: 'Current versions stay in Standard (for active access). Non-current versions transition to Glacier Deep Archive (lowest cost, $0.00099/GB/month) after 30 days. After 5 years, all versions are expired per compliance requirements.' },
      { id: 'c', text: 'S3 Versioning suspended; keep only the latest version', correct: false, explanation: 'Suspending versioning stops creating new versions but does not delete existing versions. Also violates the 5-year any-version retrieval requirement.' },
      { id: 'd', text: 'Use S3 Object Lock with versioning — automatically manages version retention', correct: false, explanation: 'Object Lock controls deletion protection, not storage cost optimization. It does not automatically transition versions to cheaper storage classes.' },
    ],
    explanation: { overall: 'Versioning + lifecycle for cost optimization: current version stays in Standard. Non-current versions (previous versions) transition to Glacier Deep Archive after N days. Expire non-current versions after 5 years * 365 days. This minimizes storage cost while meeting audit requirements.', examTip: 'Versioning lifecycle rules: "Current version" rules vs "Non-current version" rules. Transition non-current versions to cheaper storage quickly (e.g., 30 days). Expire old versions per retention policy. Keep current version in Standard for performance.' },
    tags: ['versioning', 'lifecycle', 'glacier', 'cost-optimization', 'compliance'],
  },
];
