import type { Question } from '../../types/question';

export const iacQuestions: Question[] = [
  {
    id: 'iac-001',
    stem: 'A company uses CloudFormation to manage infrastructure. A developer accidentally deleted a CloudFormation stack, removing the production database. Which CloudFormation feature would have prevented this?',
    type: 'single', difficulty: 2, topicSlug: 'iac', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'CloudFormation StackSets to manage the stack from a management account', correct: false, explanation: 'StackSets deploy stacks to multiple accounts/regions but do not prevent stack deletion in member accounts.' },
      { id: 'b', text: 'CloudFormation Termination Protection enabled on the stack', correct: true, explanation: 'Termination Protection prevents stack deletion. When enabled, delete stack actions are blocked until protection is explicitly disabled. Simple but effective safeguard for production stacks.' },
      { id: 'c', text: 'Set DeletionPolicy=Retain on the database resource', correct: true, explanation: 'DeletionPolicy=Retain: when the stack is deleted, the resource is retained (not deleted). The database survives even if the stack is deleted. Also available: Snapshot (creates final snapshot before deletion for RDS/EBS).' },
      { id: 'd', text: 'Enable AWS Config to detect CloudFormation stack deletions', correct: false, explanation: 'Config detects changes after they happen (reactive). It cannot prevent stack deletion.' },
    ],
    explanation: { overall: 'Two layers of protection: (1) Termination Protection: prevents stack deletion. (2) DeletionPolicy=Retain or Snapshot: protects individual resources even if stack is deleted. Use both for critical production stacks. Also: SCP denying cloudformation:DeleteStack on specific stacks.', examTip: 'CloudFormation protection: Termination Protection (stack level), DeletionPolicy (resource level). DeletionPolicy options: Delete (default), Retain, Snapshot (for stateful resources). UpdateReplacePolicy: same options but for resource replacement during update.' },
    tags: ['cloudformation', 'termination-protection', 'deletion-policy', 'resource-protection'],
  },
  {
    id: 'iac-002',
    stem: 'A company wants to deploy the same CloudFormation infrastructure to 50 AWS accounts in their organization. They need a single deployment that keeps all accounts consistent. Which CloudFormation feature enables this?',
    type: 'single', difficulty: 1, topicSlug: 'iac', examDomain: 'organizational-complexity',
    options: [
      { id: 'a', text: 'Deploy the CloudFormation template manually in each of the 50 accounts', correct: false, explanation: 'Manual deployment to 50 accounts is operationally intensive, error-prone, and hard to keep consistent.' },
      { id: 'b', text: 'CloudFormation StackSets with Organization-level deployment', correct: true, explanation: 'StackSets: deploy one template to multiple accounts and regions simultaneously. With SERVICE_MANAGED permissions and org integration: auto-deploy to new accounts, auto-remediate drifted accounts. One StackSet instance per account per region.' },
      { id: 'c', text: 'CloudFormation nested stacks across all accounts', correct: false, explanation: 'Nested stacks are parent-child stacks within a single account. They do not span multiple AWS accounts.' },
      { id: 'd', text: 'AWS Service Catalog products shared with all accounts', correct: false, explanation: 'Service Catalog shares product portfolios for self-service provisioning, not for consistent org-wide automated deployment.' },
    ],
    explanation: { overall: 'CloudFormation StackSets: SELF_MANAGED (requires trust roles in each account) or SERVICE_MANAGED (Organizations integration, automatic role creation). Auto-deployment: new accounts enrolled in OU automatically get StackSet. Organization deployment targets: specific OUs or entire org. Drift detection across all instances.', examTip: 'StackSets use cases: org-wide CloudTrail, Config rules, IAM roles, baseline security resources. SERVICE_MANAGED: simpler, auto-enroll new accounts. SELF_MANAGED: more control, manual trust role setup. Concurrent deployments: configure max concurrent accounts to control rollout speed.' },
    tags: ['cloudformation', 'stacksets', 'organizations', 'multi-account'],
  },
  {
    id: 'iac-003',
    stem: 'A company uses CloudFormation. During a stack update, one resource update fails. By default, what happens to the other resources that were successfully updated?',
    type: 'single', difficulty: 2, topicSlug: 'iac', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'CloudFormation keeps the successfully updated resources and leaves the failed resource unchanged', correct: false, explanation: 'CloudFormation does not partially apply changes. It maintains consistency through automatic rollback.' },
      { id: 'b', text: 'CloudFormation automatically rolls back ALL changes to the stack to the last stable state', correct: true, explanation: 'CloudFormation automatic rollback on failure: all successfully updated resources are rolled back to their previous state. Stack returns to the last successfully deployed state. Rollback can be disabled for debugging (DisableRollback: true).' },
      { id: 'c', text: 'CloudFormation deletes the entire stack and requires redeployment', correct: false, explanation: 'CloudFormation does not delete the entire stack on update failure. It rolls back to the previous stable state, preserving the stack.' },
      { id: 'd', text: 'CloudFormation continues updating remaining resources and generates an error report', correct: false, explanation: 'CloudFormation stops on failure and initiates rollback. It does not continue updating other resources after a failure by default.' },
    ],
    explanation: { overall: 'CloudFormation stack rollback: on failure, CloudFormation automatically reverts all changes. Stack transitions: UPDATE_IN_PROGRESS → UPDATE_ROLLBACK_IN_PROGRESS → UPDATE_ROLLBACK_COMPLETE. Rollback triggers: resource update failures, condition failures. Stack rollback failure: ROLLBACK_FAILED state — requires manual intervention.', examTip: 'CloudFormation rollback: automatic by default. DisableRollback: allow stack to be in a partially updated state for debugging. Rollback configuration: SNS notifications, CloudWatch alarm-based automatic rollback monitoring. Preserve Successfully Provisioned Resources: keep specific resources during rollback.' },
    tags: ['cloudformation', 'rollback', 'stack-update', 'failure-handling'],
  },
  {
    id: 'iac-004',
    stem: 'A DevOps team wants to share reusable CloudFormation templates across their organization. Development teams should be able to use these templates without needing to understand the underlying infrastructure details. Which service provides this self-service catalog?',
    type: 'single', difficulty: 1, topicSlug: 'iac', examDomain: 'organizational-complexity',
    options: [
      { id: 'a', text: 'AWS Service Catalog', correct: true, explanation: 'AWS Service Catalog: create a catalog of approved, pre-configured CloudFormation products. Portfolio: collection of products. Share with accounts/OUs. Users launch products (stacks) from Service Catalog without direct CloudFormation access. Governance: approved configurations only.' },
      { id: 'b', text: 'CloudFormation Registry', correct: false, explanation: 'CloudFormation Registry is for registering and using CloudFormation resource types (extensions), not for creating a self-service product catalog.' },
      { id: 'c', text: 'AWS CodeArtifact with template storage', correct: false, explanation: 'CodeArtifact is a package repository for code dependencies (npm, Maven, PyPI). Not for CloudFormation template self-service catalogs.' },
      { id: 'd', text: 'CloudFormation StackSets for automated deployment', correct: false, explanation: 'StackSets automates deployment to multiple accounts. Service Catalog provides the self-service discovery and launch experience for end users.' },
    ],
    explanation: { overall: 'Service Catalog: administrators create Products (CloudFormation templates with constraints). Portfolios: group products. Share with OUs/accounts. Users: browse catalog, launch products (stacks) — governed by constraints (IAM, launch, notification, template, tag). No direct CloudFormation console access needed. Version management: update product version without affecting existing stacks.', examTip: 'Service Catalog use cases: approved database configurations, standard VPC patterns, EC2 with required tags, application stacks with compliance controls. Constraints: LaunchConstraint (IAM role for launch), TagConstraint (required tags), TemplateConstraint (parameter restrictions).' },
    tags: ['service-catalog', 'self-service', 'governance', 'cloudformation'],
  },
  {
    id: 'iac-005',
    stem: 'A company wants to enforce that all EC2 instances launched via CloudFormation must have a specific tag (CostCenter) with a value from an approved list. If the tag is missing or has an unapproved value, the stack creation should fail. Which CloudFormation feature enables this?',
    type: 'single', difficulty: 2, topicSlug: 'iac', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'CloudFormation Hooks to validate tags before resource creation', correct: true, explanation: 'CloudFormation Hooks: run validation logic before or after CloudFormation creates/updates/deletes resources. A proactive hook can validate tag values and FAIL the operation if tags are missing or invalid.' },
      { id: 'b', text: 'AWS Config rule to detect missing tags and auto-remediate', correct: false, explanation: 'Config is reactive — it detects non-compliance after the resource is created. CloudFormation Hooks prevent creation of non-compliant resources proactively.' },
      { id: 'c', text: 'SCP to deny ec2:RunInstances without the CostCenter tag', correct: false, explanation: 'SCP with tag conditions (aws:RequestTag) can prevent instance creation without specific tags. However, this applies to direct API calls, not CloudFormation-mediated launches. CloudFormation uses IAM roles.' },
      { id: 'd', text: 'CloudFormation Parameters with AllowedValues to enforce the tag value', correct: false, explanation: 'Parameters with AllowedValues restrict template parameter choices, but this requires the user to pass the tag as a parameter. It does not automatically enforce tagging on all resources.' },
    ],
    explanation: { overall: 'CloudFormation Hooks: TypeName, TargetNames (which resources), TargetOperations (CREATE, UPDATE, DELETE), TargetFilters (specific resource types). Pre-create hook: validate resource properties before creation. If hook returns FAILED, CloudFormation fails the operation. Use for: tag validation, security controls, naming conventions.', examTip: 'CloudFormation Hooks are proactive guardrails at the IaC level. Config rules are reactive detective controls. Use both: Hooks prevent bad deployments, Config detects drift after. Hooks written in Java, Python, Go using CloudFormation CLI.' },
    tags: ['cloudformation', 'hooks', 'tags', 'proactive-controls', 'compliance'],
  },
  {
    id: 'iac-006',
    stem: 'A company uses AWS CDK (Cloud Development Kit) to define infrastructure. They want to ensure best practices are enforced programmatically — for example, all S3 buckets must have versioning enabled. Which CDK feature enables this policy enforcement?',
    type: 'single', difficulty: 2, topicSlug: 'iac', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'CDK Aspects', correct: true, explanation: 'CDK Aspects: visitor pattern that traverses the CDK construct tree. An Aspect can inspect and modify any construct. Use for: enforcing security policies, adding tags to all resources, validating configurations. Run during synthesis to catch violations before deployment.' },
      { id: 'b', text: 'CDK Context variables', correct: false, explanation: 'CDK Context variables store key-value configuration data (cached values like AMI IDs). Not for policy enforcement.' },
      { id: 'c', text: 'CDK custom resources', correct: false, explanation: 'CDK custom resources run Lambda functions during CloudFormation deployment. For enforcing policies, Aspects run at synth time (before deployment).' },
      { id: 'd', text: 'CDK Constructs with L2 library defaults', correct: false, explanation: 'CDK L2 constructs have sensible defaults but developers can override them. Aspects enforce policies even when developers deviate from defaults.' },
    ],
    explanation: { overall: 'CDK Aspects: implement IAspect interface with visit(node: IConstruct). Traverse every node in the construct tree. If a node is an S3 bucket without versioning, throw validation error or add versioning property. Runs during cdk synth. Catch policy violations before infrastructure is deployed.', examTip: 'CDK construct levels: L1 (CloudFormation resources, Cfn prefix), L2 (intent-based with defaults and methods), L3 (patterns, e.g., QueueProcessingFargateService). L2 and L3 use L1 under the hood. Aspects work at any level. CDK is a programming language abstraction over CloudFormation.' },
    tags: ['cdk', 'aspects', 'policy-enforcement', 'infrastructure-as-code', 'security'],
  },
  {
    id: 'iac-007',
    stem: 'A team uses Terraform to manage AWS infrastructure. Multiple team members work on the same Terraform state, causing conflicts. Which approach prevents simultaneous state modifications?',
    type: 'single', difficulty: 2, topicSlug: 'iac', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Store Terraform state in S3 with DynamoDB state locking', correct: true, explanation: 'Terraform remote state with S3 backend: state stored in S3 (durable, versioned). DynamoDB table for state locking: before applying changes, Terraform acquires a lock in DynamoDB. Other users who try to apply concurrently receive a lock error.' },
      { id: 'b', text: 'Use local Terraform state and manually coordinate team members', correct: false, explanation: 'Local state files cannot be shared across team members. Manual coordination is error-prone and scales poorly.' },
      { id: 'c', text: 'Use Terraform Cloud for remote state management', correct: false, explanation: 'Terraform Cloud works but is a separate product. S3 + DynamoDB is the AWS-native solution covered in SA Pro.' },
      { id: 'd', text: 'Keep one set of credentials for Terraform and share among team members', correct: false, explanation: 'Shared credentials are a security anti-pattern. Each team member should have individual IAM credentials. State locking is the solution for concurrent access control.' },
    ],
    explanation: { overall: 'Terraform S3 backend: backend "s3" { bucket, key, region, dynamodb_table }. State file in S3: versioned, encrypted. DynamoDB lock table: one item per state file (LockID attribute). terraform apply: acquires lock → apply → release lock. Version controlled state: recover from state corruption by restoring previous S3 version.', examTip: 'Terraform state locking with DynamoDB: table requires "LockID" as partition key (String type). State file path: s3://bucket/path/to/terraform.tfstate. Separate state per environment (dev/staging/prod). terraform state list: view resources. terraform state import: adopt existing resources.' },
    tags: ['terraform', 'state-locking', 'dynamodb', 's3', 'collaboration'],
  },
  {
    id: 'iac-008',
    stem: 'A company uses CloudFormation and notices that their stack drift detection shows several resources have drifted from their template definition. They want to automatically remediate drift. Which approach achieves automated drift remediation?',
    type: 'single', difficulty: 3, topicSlug: 'iac', examDomain: 'continuous-improvement',
    options: [
      { id: 'a', text: 'Manually run CloudFormation update-stack to reapply the template', correct: false, explanation: 'Manual update works but is not automated. Requires human intervention for each drift.' },
      { id: 'b', text: 'EventBridge rule triggering Lambda on drift detection, Lambda runs CloudFormation update-stack', correct: true, explanation: 'Automated remediation: EventBridge scheduled rule triggers drift detection. If drift detected, Lambda calls cloudformation:UpdateStack to reapply template, reverting drifted resources to desired state.' },
      { id: 'c', text: 'Enable CloudFormation Auto-Remediation in stack settings', correct: false, explanation: 'CloudFormation does not have an automatic drift remediation setting. Remediation requires an external trigger (EventBridge + Lambda).' },
      { id: 'd', text: 'Use AWS Config auto-remediation to fix drifted resources', correct: false, explanation: 'Config auto-remediation can fix individual resource configuration drifts but does not understand CloudFormation stack-level drift.' },
    ],
    explanation: { overall: 'CloudFormation drift automation: (1) AWS Config CloudFormation stack drift detection rule: detects drifted stacks. (2) EventBridge rule on Config finding + Lambda: calls detect-stack-drift, then update-stack to remediate. Alternative: AWS Config conformance pack with stack drift rule + SSM Automation remediation.', examTip: 'CloudFormation drift detection: detect-stack-drift (async) → describe-stack-drift-detection-status → describe-stack-resource-drifts (see what changed). Drift types: MODIFIED (changed), DELETED (removed), NOT_CHECKED, IN_SYNC. Drift remediation: update-stack (reapply template).' },
    tags: ['cloudformation', 'drift', 'eventbridge', 'lambda', 'remediation'],
  },
  {
    id: 'iac-009',
    stem: 'A company needs to provision AWS infrastructure using code that non-developers (cloud architects) can understand and maintain. The team prefers a declarative YAML/JSON syntax similar to CloudFormation but with higher-level abstractions. Which tool is appropriate?',
    type: 'single', difficulty: 2, topicSlug: 'iac', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'AWS CDK with TypeScript', correct: false, explanation: 'CDK uses general-purpose programming languages (TypeScript, Python, Java). Requires programming knowledge that non-developers may lack.' },
      { id: 'b', text: 'AWS CloudFormation with L2 constructs', correct: false, explanation: 'CloudFormation uses YAML/JSON but is low-level (L1 resources). Many details required for even simple patterns.' },
      { id: 'c', text: 'AWS CloudFormation with the SAM (Serverless Application Model) template', correct: true, explanation: 'SAM transforms: AWS::Serverless::Function → full Lambda + IAM Role + CloudWatch Logs Group + API Gateway. Higher-level YAML syntax. Non-developers can define serverless applications with fewer lines. SAM CLI: local testing.' },
      { id: 'd', text: 'Pulumi with Python', correct: false, explanation: 'Pulumi uses general-purpose languages. Like CDK, requires programming knowledge.' },
    ],
    explanation: { overall: 'SAM (Serverless Application Model): AWS-maintained extension of CloudFormation. Simplified resources: AWS::Serverless::Function, API, SimpleTable, HttpApi, StateMachine. SAM CLI: local Lambda testing, deployment. sam build, sam local invoke, sam deploy. Transform: sam transforms SAM template into full CloudFormation template.', examTip: 'SAM = CloudFormation shorthand for serverless. SAM template has Transform: AWS::Serverless-2016-10-31. SAM globals: apply settings to all functions (runtime, timeout, environment variables). SAM policy templates: pre-built IAM policies for common patterns (S3ReadPolicy, DynamoDBCrudPolicy).' },
    tags: ['sam', 'cloudformation', 'serverless', 'declarative', 'iac'],
  },
  {
    id: 'iac-010',
    stem: 'A company uses CloudFormation custom resources to extend CloudFormation capabilities. The custom resource calls a Lambda function to perform actions not natively supported by CloudFormation. The Lambda function must signal success or failure back to CloudFormation. How does it do this?',
    type: 'single', difficulty: 3, topicSlug: 'iac', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Lambda returns a JSON response with Status: SUCCESS or FAILED', correct: false, explanation: 'Lambda returning a JSON response to CloudFormation directly does not signal custom resource completion. Custom resources use a pre-signed S3 URL or cfn-response module.' },
      { id: 'b', text: 'Lambda sends an HTTP PUT request to the pre-signed S3 URL provided in the event (ResponseURL)', correct: true, explanation: 'Custom resource Lambda: receives Create/Update/Delete event with a ResponseURL (pre-signed S3 URL). Lambda sends an HTTP PUT to this URL with JSON payload containing Status (SUCCESS/FAILED), PhysicalResourceId, and Data. CloudFormation polls the URL to receive the signal.' },
      { id: 'c', text: 'Lambda calls the CloudFormation API SignalResource', correct: false, explanation: 'SignalResource is for EC2 creation policies (cfn-signal), not for Lambda-based custom resources.' },
      { id: 'd', text: 'Lambda raises an exception, which CloudFormation interprets as failure', correct: false, explanation: 'Lambda exceptions are not automatically reported to CloudFormation for custom resources. The ResponseURL PUT is required.' },
    ],
    explanation: { overall: 'CloudFormation custom resource: AWS::CloudFormation::CustomResource with ServiceToken (Lambda ARN). Lambda receives event.RequestType (Create/Update/Delete), event.ResponseURL, event.ResourceProperties. Lambda must PUT to ResponseURL within timeout (default 1 hour). Use cfn-response module (Node.js/Python) to simplify response sending.', examTip: 'Custom resource Lambda MUST send response to ResponseURL. If Lambda times out or fails without responding: CloudFormation waits up to 1 hour, then times out. Always include ResponseURL PUT in try/finally. cfn-response.send() helper module handles the PUT. Never forget to respond — orphaned waits cause stuck stacks.' },
    tags: ['cloudformation', 'custom-resource', 'lambda', 'response-url'],
  },
  {
    id: 'iac-011',
    stem: 'A company deploys applications using CodePipeline. They want to implement an approval gate where a senior engineer must approve each production deployment. If not approved within 24 hours, the deployment should be cancelled. Which CodePipeline feature enables this?',
    type: 'single', difficulty: 1, topicSlug: 'iac', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'CodePipeline Manual Approval action', correct: true, explanation: 'Manual Approval action: pauses the pipeline and sends an SNS notification with an approval URL. Approver can approve or reject via Console, CLI, or SNS. Configurable timeout period (default: no expiry). Configure 24-hour timeout in the action configuration.' },
      { id: 'b', text: 'CodePipeline Source stage with branch protection rules', correct: false, explanation: 'Branch protection is a source code management feature (GitHub/CodeCommit). Not a pipeline approval gate.' },
      { id: 'c', text: 'CodeBuild test stage that blocks deployment until all tests pass', correct: false, explanation: 'CodeBuild test stages block on automated test results, not human approval.' },
      { id: 'd', text: 'AWS Lambda action that checks an approval flag in DynamoDB', correct: false, explanation: 'Custom Lambda-based approval is possible but complex. CodePipeline natively provides manual approval actions.' },
    ],
    explanation: { overall: 'Manual Approval: add an Approval action in any stage. SNS notification: email to approvers with pipeline name, stage, action, and a link to approve/reject. In pipeline execution: when approval action is reached, pipeline pauses. Approver clicks link, reviews, approves or rejects. Rejection: pipeline stops with REJECTED status.', examTip: 'CodePipeline stage types: Source, Build, Test, Deploy, Approval, Invoke. Approval: human gate. Invoke: Lambda or Step Functions. Multiple approvers: use SNS with multiple subscribers. Custom approval messages: include deployment info in the SNS notification body.' },
    tags: ['codepipeline', 'manual-approval', 'deployment', 'governance'],
  },
  {
    id: 'iac-012',
    stem: 'A company uses AWS Systems Manager (SSM) to automate patching across 500 EC2 instances in multiple accounts and regions. They need a consistent patching schedule with minimal downtime during business hours. Which SSM feature provides this?',
    type: 'single', difficulty: 2, topicSlug: 'iac', examDomain: 'organizational-complexity',
    options: [
      { id: 'a', text: 'SSM Patch Manager with Patch Groups and Maintenance Windows', correct: true, explanation: 'Patch Manager: define Patch Baselines (approved patches). Patch Groups: tag instances with Patch Group tag. Maintenance Windows: schedule patching (e.g., weekends 2AM-6AM). Rate control: max concurrency, max errors. Cross-account/region patching: SSM Automation with org-level execution.' },
      { id: 'b', text: 'AWS Systems Manager Run Command with scheduled CloudWatch Events', correct: false, explanation: 'Run Command can execute scripts including patching, but Patch Manager provides richer policy controls (baselines, approvals, compliance reporting).' },
      { id: 'c', text: 'EC2 User Data script to patch on instance launch', correct: false, explanation: 'User Data patches only at launch time, not for ongoing maintenance of running instances.' },
      { id: 'd', text: 'AWS Config rule to detect unpatched instances with auto-remediation', correct: false, explanation: 'Config can detect non-compliant patch status. Patch Manager provides the actual patching mechanism.' },
    ],
    explanation: { overall: 'SSM Patch Manager + Maintenance Windows: Patch baseline defines approved patches (OS vendor, classification, severity). Patch Group tag on instances routes to specific baseline. Maintenance Window: schedule + tasks (Run Patch Baseline). Rate control: max 20% instances concurrent. Compliance: view patch status in SSM Compliance. Cross-account: Quick Setup for org-wide baseline.', examTip: 'SSM Patch Manager components: Patch Baseline (rules for what to patch), Patch Group (which instances, via tag), Maintenance Window (when to patch). Baseline can auto-approve patches after N days. Default baselines per OS available. Custom baseline: approve specific patches, reject known bad ones.' },
    tags: ['ssm', 'patch-manager', 'maintenance-window', 'patching', 'multi-account'],
  },
  {
    id: 'iac-013',
    stem: 'A company wants to validate their CloudFormation templates for security vulnerabilities and compliance before deployment. Which tool analyzes CloudFormation templates and IaC for security issues?',
    type: 'single', difficulty: 1, topicSlug: 'iac', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'AWS CloudFormation Linter (cfn-lint)', correct: false, explanation: 'cfn-lint validates template syntax and resource property types. It checks for CloudFormation format errors but not security misconfigurations.' },
      { id: 'b', text: 'AWS CloudFormation Guard (cfn-guard)', correct: true, explanation: 'CloudFormation Guard: policy-as-code validation. Write rules in cfn-guard DSL to enforce security policies. Example: DENY if S3 bucket has public access enabled. Runs before deployment in CI/CD pipelines.' },
      { id: 'c', text: 'Amazon Inspector for CloudFormation analysis', correct: false, explanation: 'Inspector scans deployed EC2 instances and container images for vulnerabilities. Not for static IaC template analysis.' },
      { id: 'd', text: 'AWS Config with CloudFormation template rules', correct: false, explanation: 'Config evaluates deployed resources, not CloudFormation templates before deployment. Pre-deployment checks require cfn-guard or similar.' },
    ],
    explanation: { overall: 'CloudFormation Guard (cfn-guard): rule language to validate CloudFormation template properties. Rules: when/then logic, block access, require encryption, enforce tag policies. Run in CI/CD pipeline before cdk deploy or CloudFormation deploy. Also works with Terraform, CDK (synthesized templates). Alternative: Checkov, tfsec, Terrascan (third-party).', examTip: 'IaC security tools: cfn-guard = AWS-native CloudFormation policy validation. Checkov = open-source multi-IaC (CF, TF, Kubernetes, ARM). AWS Security Hub: has CloudFormation template scanning integration via Checkov. Shift-left security: catch misconfigurations before deployment.' },
    tags: ['cloudformation-guard', 'iac-security', 'policy-as-code', 'shift-left'],
  },
  {
    id: 'iac-014',
    stem: 'A company uses CodeCommit for source code and CodeBuild for CI. They want to scan application code for vulnerabilities automatically on every pull request. Which AWS service provides automated code vulnerability scanning in the CI/CD pipeline?',
    type: 'single', difficulty: 1, topicSlug: 'iac', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Amazon CodeGuru Reviewer', correct: true, explanation: 'CodeGuru Reviewer: ML-based code review. Analyzes Java and Python code for: security vulnerabilities (injection flaws, hardcoded credentials), resource leaks, concurrency issues. Integrates with CodeCommit, GitHub, Bitbucket. Automated PR comments with findings.' },
      { id: 'b', text: 'Amazon Inspector v2 for code scanning', correct: false, explanation: 'Inspector v2 scans EC2, ECR images, and Lambda code packages for software vulnerabilities (CVEs). Not a static code analysis tool for application logic flaws.' },
      { id: 'c', text: 'AWS CodeBuild with SonarQube installed', correct: false, explanation: 'SonarQube is a third-party tool. While it can be used in CodeBuild, CodeGuru Reviewer is the AWS-native managed code review service.' },
      { id: 'd', text: 'AWS Macie for source code classification', correct: false, explanation: 'Macie discovers and classifies sensitive data (PII) in S3. Not a code review or security vulnerability scanning tool.' },
    ],
    explanation: { overall: 'CodeGuru Reviewer: associate with a repository. Creates findings as PR comments (GitHub PR, CodeCommit PR). Security detectors: finds OWASP Top 10 issues, hardcoded credentials, log injection, path traversal. Code quality: identifies resource leaks, dead code, performance issues. CodeGuru Profiler: runtime performance analysis (separate from Reviewer).', examTip: 'CodeGuru Reviewer = static code analysis (find bugs before runtime). CodeGuru Profiler = runtime performance profiling (find CPU hotspots during execution). Both use ML. Reviewer: Java and Python. Integrates with: CodeCommit, GitHub (via GitHub Actions), Bitbucket Cloud.' },
    tags: ['codeguru', 'code-review', 'security', 'cicd', 'static-analysis'],
  },
  {
    id: 'iac-015',
    stem: 'A company uses multiple CI/CD tools (CodePipeline, Jenkins, GitHub Actions) for different teams. They need a consistent way to release software to production that provides: deployment tracking, approval workflows, and rollback capabilities, regardless of which CI tool is used. Which AWS service provides this?',
    type: 'single', difficulty: 2, topicSlug: 'iac', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'AWS CodeDeploy', correct: true, explanation: 'CodeDeploy: deployment service that works with any CI tool. Integrates with CodePipeline, Jenkins, GitHub Actions, or standalone. Deployment groups, deployment configurations (rolling, blue-green, canary). Rollback: automatic on CloudWatch alarm. Deployment tracking, approval through CodePipeline integration.' },
      { id: 'b', text: 'AWS CodePipeline with parallel pipelines', correct: false, explanation: 'CodePipeline is a CI/CD orchestration service. It cannot integrate with external CI tools like Jenkins or GitHub Actions as a unified deployment layer.' },
      { id: 'c', text: 'AWS Elastic Beanstalk for deployment management', correct: false, explanation: 'Elastic Beanstalk provides an opinionated PaaS deployment experience. Not a tool-agnostic deployment service for existing applications.' },
      { id: 'd', text: 'AWS Systems Manager Change Manager for deployment approvals', correct: false, explanation: 'Change Manager provides structured change approval workflows but is not specifically a CI-tool-agnostic deployment service with rollback.' },
    ],
    explanation: { overall: 'CodeDeploy: agent-based deployment to EC2/on-premises, serverless to Lambda, container to ECS. Integrates with any CI: CodePipeline (native), Jenkins (CodeDeploy plugin), GitHub Actions (aws-actions/aws-codedeploy-action). AppSpec.yml: defines deployment steps (BeforeInstall, AfterInstall, ApplicationStart hooks). Rollback: on CloudWatch alarm or manual trigger.', examTip: 'CodeDeploy targets: EC2/On-premises (agent-based), Lambda (traffic shifting via aliases), ECS (blue-green via ALB). Deployment config: OneAtATime, HalfAtATime, AllAtOnce, custom (e.g., 10% every 5 min). Rollback: automatic (CloudWatch alarm) or manual. Always define AppSpec.yml in application root.' },
    tags: ['codedeploy', 'cicd', 'deployment', 'rollback', 'multi-tool'],
  },
  {
    id: 'iac-016',
    stem: 'A company uses CloudFormation to manage their AWS infrastructure. During a routine stack update, a typo in a resource property causes a deployment failure. CloudFormation rolls back, but a DynamoDB table in the stack was deleted before the rollback completed, causing data loss. Which CloudFormation feature, if enabled, would have prevented this data loss during rollback?',
    type: 'single', difficulty: 3, topicSlug: 'iac', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Stack termination protection to prevent stack deletion', correct: false, explanation: 'Termination protection prevents the entire stack from being deleted via the console or CLI, but does not prevent resource deletion during a rollback triggered by a failed update.' },
      { id: 'b', text: 'DeletionPolicy: Retain on the DynamoDB table resource', correct: true, explanation: 'DeletionPolicy: Retain instructs CloudFormation to keep a resource even if the stack operation would normally delete it (during rollback, resource replacement, or stack deletion). The table persists independently of the stack, preventing data loss.' },
      { id: 'c', text: 'UpdateReplacePolicy: Retain on the DynamoDB table resource', correct: false, explanation: 'UpdateReplacePolicy controls what happens to the old resource when CloudFormation replaces it during an update (e.g., changing a property that requires replacement). It does not affect behavior during rollbacks caused by failed updates.' },
      { id: 'd', text: 'A Stack Policy denying updates to the DynamoDB table', correct: false, explanation: 'Stack policies prevent updates to specific resources during stack updates. They would block the update entirely but not protect the resource during a rollback of a failed update elsewhere in the stack.' },
    ],
    explanation: { overall: 'DeletionPolicy options for CloudFormation resources: (1) Delete (default for most resources) — resource is deleted when stack is deleted or during rollback replacement. (2) Retain — resource persists after stack deletion/rollback; stack loses the reference but resource continues to exist. (3) Snapshot — supported by EBS volumes, RDS, Redshift; takes a final snapshot before deletion. For stateful resources (databases, S3 buckets with data), always use DeletionPolicy: Retain or Snapshot as a safety net.', examTip: 'DeletionPolicy vs UpdateReplacePolicy: DeletionPolicy = what happens when the resource is removed from the stack (deletion or rollback). UpdateReplacePolicy = what happens to the OLD resource when CloudFormation REPLACES it during an update (e.g., you change a DynamoDB table name, forcing recreation). Use both for critical stateful resources: DeletionPolicy: Retain AND UpdateReplacePolicy: Retain.' },
    tags: ['cloudformation', 'deletion-policy', 'data-protection', 'rollback', 'stateful-resources'],
  },
  {
    id: 'iac-017',
    stem: 'A platform team wants to allow application teams to deploy approved infrastructure patterns (VPC, RDS, ECS cluster) without giving them direct CloudFormation access or the ability to create IAM roles. The patterns must be versioned, and provisioning must be audited. Application teams should self-serve through a catalog interface. Which AWS service architecture best satisfies these requirements?',
    type: 'single', difficulty: 2, topicSlug: 'iac', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'CloudFormation StackSets with automatic deployment to application team accounts', correct: false, explanation: 'StackSets deploys the same template to multiple accounts, managed centrally by the platform team. It does not provide a self-service catalog for application teams to choose and provision products on-demand.' },
      { id: 'b', text: 'AWS Service Catalog with portfolios, products, and launch constraints using a launch IAM role', correct: true, explanation: 'Service Catalog: platform team creates products (CloudFormation templates), organizes them into portfolios, and shares portfolios with application team accounts. Launch constraints specify an IAM role that Service Catalog assumes during provisioning, so application teams never need direct IAM or CloudFormation permissions. All provisioning is tracked in CloudTrail.' },
      { id: 'c', text: 'AWS Systems Manager Automation runbooks with resource-based policies for cross-account access', correct: false, explanation: 'SSM Automation can orchestrate infrastructure provisioning but lacks the catalog, versioning, self-service UI, and launch constraint (role separation) features that Service Catalog provides natively.' },
      { id: 'd', text: 'AWS CDK with CodePipeline for each application team to deploy approved CDK constructs', correct: false, explanation: 'CDK constructs with pipelines require application teams to have deployment IAM permissions. This does not provide the role separation (launch constraints) or managed self-service catalog experience described.' },
    ],
    explanation: { overall: 'AWS Service Catalog architecture: (1) Admin creates CloudFormation templates as "products" with versioning. (2) Products grouped into "portfolios" shared with AWS accounts or OUs. (3) "Launch constraints" attach an IAM role — when an end user provisions a product, Service Catalog assumes this role, meaning end users need no direct IAM/CloudFormation permissions. (4) "Tag enforcement" and "template constraints" restrict configurations. (5) All provisioning logged in CloudTrail. Perfect for platform engineering / golden path infrastructure.', examTip: 'Service Catalog key concepts: Portfolio (collection of products shared to accounts/OUs) → Product (versioned CloudFormation template) → Launch Constraint (IAM role for provisioning, enabling least-privilege self-service) → Provisioned Product (running instance of a product). Exam differentiator: when you see "self-service", "approved patterns", "without IAM permissions", "application team autonomy" → Service Catalog.' },
    tags: ['service-catalog', 'platform-engineering', 'self-service', 'launch-constraints', 'governance'],
  },
  {
    id: 'iac-018',
    stem: 'A CloudFormation stack includes a Lambda-backed custom resource that calls a third-party API to provision an external resource during stack creation. The third-party resource is successfully created, but then the CloudFormation stack fails on a subsequent resource and rolls back. The external resource now exists but the CloudFormation stack is gone. How should the custom resource Lambda function be designed to handle this correctly?',
    type: 'single', difficulty: 3, topicSlug: 'iac', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Set a DeletionPolicy: Retain on the custom resource to prevent deletion calls', correct: false, explanation: 'DeletionPolicy: Retain on a custom resource suppresses the Delete event sent to Lambda, but then CloudFormation never signals Lambda to clean up the external resource, leaving it orphaned permanently in both success and rollback scenarios.' },
      { id: 'b', text: 'Implement the Delete handler in the Lambda function to call the third-party API and deprovision the external resource when CloudFormation sends a Delete request during rollback', correct: true, explanation: 'When a CloudFormation stack rolls back, it sends a Delete event to any custom resources that were successfully created. The Lambda must handle this Delete event by calling the third-party API to deprovision the external resource, then respond to CloudFormation with SUCCESS. This ensures no orphaned external resources.' },
      { id: 'c', text: 'Use a CloudFormation macro instead of a custom resource to avoid rollback issues', correct: false, explanation: 'Macros transform CloudFormation templates before deployment (like search-and-replace or adding resources). They do not interact with external APIs during resource lifecycle events and would not solve the orphaned resource problem.' },
      { id: 'd', text: 'Store the external resource ID in SSM Parameter Store and use a separate cleanup Lambda triggered by CloudWatch Events', correct: false, explanation: 'This is an indirect workaround that adds operational complexity. The correct pattern is to implement proper Delete handling within the custom resource Lambda itself, which CloudFormation will automatically invoke during rollback.' },
    ],
    explanation: { overall: 'CloudFormation Custom Resource lifecycle: Create (stack creation) → Update (stack update when properties change) → Delete (stack deletion OR rollback). Your Lambda must handle all three events. Crucially: during rollback, CloudFormation sends Delete to successfully-created custom resources. If your Lambda only implements Create/Update, rollbacks leave orphaned external resources. Always implement idempotent Delete handlers. Respond to CloudFormation using the pre-signed S3 URL in the event (cfnresponse module) or the ResponseURL.', examTip: 'Custom resource Lambda must: (1) Handle Create, Update, Delete RequestTypes. (2) Always respond to CloudFormation (even on failure) — if no response within the timeout, the stack hangs for 1 hour then fails. (3) Use PhysicalResourceId consistently — changing it on Update causes CloudFormation to send Delete to the old PhysicalResourceId. (4) Be idempotent — may be retried. Common pattern: use cfnresponse.send() helper.' },
    tags: ['cloudformation', 'custom-resource', 'lambda', 'rollback', 'lifecycle-management'],
  },
  {
    id: 'iac-019',
    stem: 'An AWS CDK application has grown to include 50+ stacks across 8 AWS accounts and 4 regions. Developers frequently encounter "bootstrap version mismatch" errors and inconsistent CDK toolkit versions across teams. The CI/CD pipeline takes 45 minutes to deploy all stacks sequentially. Which combination of CDK features and practices should the team adopt to resolve these issues?',
    type: 'multiple', difficulty: 3, topicSlug: 'iac', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Use cdk bootstrap with --trust and --cloudformation-execution-policies flags to standardize bootstrap across all accounts via a CI role', correct: true, explanation: 'Centralized bootstrapping with --trust allows a CI account/role to bootstrap all target accounts using cdk bootstrap aws://ACCOUNT/REGION --trust CI_ACCOUNT_ID. Pinning --cloudformation-execution-policies ensures consistent permissions. Bootstrap version mismatches are eliminated.' },
      { id: 'b', text: 'Implement CDK Pipelines (CodePipeline-based) with parallel wave deployment for concurrent multi-account, multi-region stack deployment', correct: true, explanation: 'CDK Pipelines (aws-cdk-lib/pipelines) provides self-mutating pipelines with built-in Stage/Wave abstractions. Parallel waves deploy stacks concurrently across accounts and regions, reducing 45-minute sequential deployment to minutes by parallelizing independent stacks.' },
      { id: 'c', text: 'Pin the CDK version in package.json and enforce it via npm ci in all CI/CD pipelines to ensure version consistency', correct: true, explanation: 'Pinning aws-cdk-lib version in package.json and using npm ci (clean install from package-lock.json) ensures all developers and CI environments use identical CDK versions, eliminating version drift and "bootstrap mismatch" issues related to feature differences.' },
      { id: 'd', text: 'Convert all CDK stacks to CloudFormation JSON templates using cdk synth and commit the raw templates to version control', correct: false, explanation: 'Committing synthesized CloudFormation JSON defeats the purpose of CDK — you lose the type safety, construct abstractions, and code-review benefits. Teams would need to manually update JSON, which is error-prone and does not solve bootstrap or parallelism issues.' },
    ],
    explanation: { overall: 'CDK at scale best practices: (1) Bootstrap standardization — use centralized bootstrap with cross-account trust; pin bootstrap version. (2) CDK Pipelines — self-mutating pipeline that deploys CDK apps; Stages model application environments; Waves enable parallel multi-region/multi-account deployment. (3) Version pinning — pin aws-cdk-lib in package.json; npm ci in CI (not npm install). (4) Projen or Nx for monorepo CDK project management. (5) CDK Aspects for policy-as-code across all stacks (security checks, tagging enforcement).', examTip: 'CDK Pipelines key concepts: ShellStep (shell command in pipeline), CodeBuildStep (custom CodeBuild project), Stage (group of stacks = one environment), Wave (group of stages deployed in parallel). Self-mutating: pipeline updates itself when you add new stages. Cross-account: needs bootstrap trust between pipeline account and target accounts. For the exam: CDK Pipelines = best practice for deploying CDK apps, not raw CodePipeline configurations.' },
    tags: ['cdk', 'cdk-pipelines', 'multi-account', 'bootstrap', 'parallel-deployment'],
  },
  {
    id: 'iac-020',
    stem: 'A company has 200 CloudFormation stacks deployed across 15 AWS accounts. A security audit reveals that many stacks have drifted from their template definitions — EC2 security groups have been manually modified, and S3 bucket policies have been changed outside of CloudFormation. The company wants to detect, report, and automatically remediate drift across all accounts. Which approach provides the most operationally efficient solution?',
    type: 'single', difficulty: 3, topicSlug: 'iac', examDomain: 'continuous-improvement',
    options: [
      { id: 'a', text: 'Run CloudFormation drift detection manually on each stack monthly and review the console reports', correct: false, explanation: 'Manual drift detection on 200 stacks across 15 accounts is operationally inefficient and not scalable. Monthly cadence means drift can exist for up to 30 days before detection, and manual remediation provides no automation.' },
      { id: 'b', text: 'Use AWS Config with the cloudformation-stack-drift-detection-check managed rule, aggregate findings in a Config aggregator across all accounts, and trigger automatic remediation via Systems Manager Automation', correct: true, explanation: 'AWS Config cloudformation-stack-drift-detection-check rule triggers drift detection automatically. Config Aggregator consolidates findings across all 15 accounts into a single view. Non-compliant stacks trigger Config remediation actions (SSM Automation) that run CloudFormation stack updates to reconcile drift. This provides continuous, automated, multi-account drift governance.' },
      { id: 'c', text: 'Enable CloudTrail in all accounts and use Athena to query for CloudFormation-unmanaged API calls on stacked resources', correct: false, explanation: 'CloudTrail captures API calls but does not directly correlate them with CloudFormation stack drift. You would need complex Athena queries to identify which calls modified stack resources, and this approach provides detection only, not remediation.' },
      { id: 'd', text: 'Implement SCPs that deny all API calls that would modify resources tagged with aws:cloudformation:stack-name', correct: false, explanation: 'While SCPs can prevent unauthorized modifications, denying all direct API calls would also block legitimate emergency changes and CloudFormation itself from updating resources. Overly restrictive SCPs can cause operational incidents.' },
    ],
    explanation: { overall: 'CloudFormation drift detection at scale: (1) CloudFormation native drift detection — per-stack, triggered manually or via API; results show MODIFIED, DELETED, or NOT_CHECKED resources. (2) AWS Config rule cloudformation-stack-drift-detection-check — continuously monitors; integrates with Config Aggregator for multi-account/multi-region view. (3) Remediation — Config remediation actions can trigger SSM Automation documents that run aws cloudformation deploy to reconcile state. (4) EventBridge — trigger Lambda on Config compliance changes for custom workflows.', examTip: 'Drift detection lifecycle: Initiate detection (StartStackDriftDetection) → CloudFormation checks each resource against template → DescribeStackDriftDetectionStatus (DETECTION_COMPLETE) → DescribeStackResourceDrifts (per-resource results). Drifted resources: MODIFIED (resource exists but properties changed), DELETED (resource deleted outside CF), IN_SYNC (matches template), NOT_CHECKED (resource type does not support drift detection). Remediation: re-run cloudformation deploy with same template — CF will update drifted resources back to desired state.' },
    tags: ['cloudformation', 'drift-detection', 'config', 'multi-account', 'remediation'],
  },
];
