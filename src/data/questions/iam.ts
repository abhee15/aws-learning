import type { Question } from '../../types/question';

export const iamQuestions: Question[] = [
  {
    id: 'iam-001',
    stem: 'A company uses AWS Organizations with multiple accounts. The security team needs to prevent all accounts from disabling CloudTrail, even if an administrator in those accounts has full IAM permissions. Which solution achieves this with the least operational overhead?',
    type: 'single',
    difficulty: 2,
    topicSlug: 'iam',
    examDomain: 'organizational-complexity',
    options: [
      { id: 'a', text: 'Create an IAM policy in each account that denies cloudtrail:StopLogging and attach it to all IAM users', correct: false, explanation: 'This requires managing policies in every account separately and can be bypassed by account root users.' },
      { id: 'b', text: 'Create a Service Control Policy (SCP) that denies cloudtrail:StopLogging and attach it to the root of the Organization', correct: true, explanation: 'SCPs apply to all principals in all accounts under the OU/root, including account administrators. They cannot be overridden by any IAM policy.' },
      { id: 'c', text: 'Use AWS Config rules with auto-remediation to re-enable CloudTrail when disabled', correct: false, explanation: 'Config remediation is reactive — there is a window of time when CloudTrail is disabled. SCPs are preventive.' },
      { id: 'd', text: 'Enable AWS CloudTrail Organization-level trail with immutable log file validation', correct: false, explanation: 'An Organization trail consolidates logs but does not prevent individual account administrators from stopping their trails.' },
    ],
    explanation: {
      overall: 'SCPs are preventive guardrails that restrict what actions can be performed within an AWS Organization. When attached to the root OU, they apply to every account and every principal (except the management/master account). Even an account root user cannot bypass an SCP. This is the standard pattern for enforcing Organization-wide security baselines.',
      examTip: 'SCPs do not grant permissions — they only limit the maximum permissions available. An SCP deny overrides any IAM allow. Attach SCPs to the root OU or specific OUs to apply uniformly.',
    },
    tags: ['scp', 'organizations', 'cloudtrail', 'governance'],
  },
  {
    id: 'iam-002',
    stem: 'An application running on EC2 in Account A needs to write objects to an S3 bucket in Account B. The EC2 instance has an IAM role with s3:PutObject permission. The S3 bucket in Account B has no bucket policy. What is the outcome, and how should this be fixed?',
    type: 'single',
    difficulty: 3,
    topicSlug: 'iam',
    examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Access is allowed because the EC2 role has s3:PutObject permission', correct: false, explanation: 'Cross-account S3 access requires both the identity policy AND the resource policy (bucket policy) to allow the access.' },
      { id: 'b', text: 'Access is denied. Add a bucket policy in Account B allowing the EC2 role from Account A to PutObject', correct: true, explanation: 'For cross-account access to S3, the bucket policy in Account B must explicitly allow the principal from Account A. The EC2 role identity policy alone is insufficient.' },
      { id: 'c', text: 'Access is denied. Create a VPC Endpoint for S3 in Account A to enable cross-account access', correct: false, explanation: 'VPC Endpoints provide private connectivity to S3 but do not solve cross-account authorization.' },
      { id: 'd', text: 'Access is denied. The EC2 role must use AssumeRole to assume a role in Account B that has bucket access', correct: false, explanation: 'While cross-account role assumption is a valid pattern, a direct bucket policy allowing Account A\'s role is simpler and equally valid.' },
    ],
    explanation: {
      overall: 'Cross-account S3 access requires the resource policy (bucket policy) in the destination account to explicitly allow the requesting principal from the source account. Without a bucket policy, S3 defaults to denying cross-account access even if the identity policy grants the permission. Within the same account, S3 access only requires an identity policy — the bucket policy is optional.',
      examTip: 'Same-account S3 access: identity policy alone works. Cross-account S3 access: bucket policy MUST explicitly allow the cross-account principal. This is a key distinction tested on SA Pro.',
    },
    tags: ['s3', 'cross-account', 'bucket-policy', 'iam'],
  },
  {
    id: 'iam-003',
    stem: 'A large enterprise wants to allow developers to create IAM roles for their applications, but prevent them from creating roles with more permissions than the developers themselves have. Which IAM feature achieves this?',
    type: 'single',
    difficulty: 3,
    topicSlug: 'iam',
    examDomain: 'organizational-complexity',
    options: [
      { id: 'a', text: 'Service Control Policies (SCPs) that restrict IAM role creation', correct: false, explanation: 'SCPs can restrict which actions are allowed but do not provide the per-developer permission boundary scoping needed here.' },
      { id: 'b', text: 'IAM Permission Boundaries attached to the developer role itself', correct: false, explanation: 'Permission boundaries limit the developer\'s own permissions but alone do not prevent them from creating roles with higher permissions.' },
      { id: 'c', text: 'Require developers to attach a Permission Boundary when creating new IAM roles, enforced via IAM condition keys', correct: true, explanation: 'Using iam:PermissionsBoundary condition in the developer\'s IAM policy requires that any role they create must have the specified permission boundary attached. This prevents privilege escalation.' },
      { id: 'd', text: 'Enable IAM Access Analyzer to detect and deny roles with excessive permissions', correct: false, explanation: 'IAM Access Analyzer is detective, not preventive — it cannot prevent role creation.' },
    ],
    explanation: {
      overall: 'This is the "delegated administration with permission boundaries" pattern. The developer\'s IAM policy includes a condition: "StringEquals": {"iam:PermissionsBoundary": "arn:aws:iam::ACCOUNT:policy/BoundaryPolicy"} on iam:CreateRole. This forces developers to always attach the permission boundary, which limits the maximum permissions the created role can ever have — preventing privilege escalation.',
      examTip: 'Permission boundaries + IAM conditions = delegated administration without privilege escalation. This is a key SA Pro exam pattern for enabling developer self-service while maintaining security guardrails.',
    },
    tags: ['permission-boundary', 'privilege-escalation', 'delegated-admin'],
  },
  {
    id: 'iam-004',
    stem: 'A company has a SaaS application serving thousands of customers. Each customer\'s data is tagged with their customer ID. Instead of creating IAM policies per customer, they want a scalable approach. Which IAM feature enables this?',
    type: 'single',
    difficulty: 2,
    topicSlug: 'iam',
    examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'IAM Groups with per-customer policies', correct: false, explanation: 'Creating a group and policy per customer does not scale to thousands of customers.' },
      { id: 'b', text: 'Attribute-Based Access Control (ABAC) using resource tags and principal tags as conditions', correct: true, explanation: 'ABAC allows policies like "Allow if aws:ResourceTag/CustomerId == aws:PrincipalTag/CustomerId". When a user is tagged with their customer ID, they can only access resources with matching tags.' },
      { id: 'c', text: 'Resource-based policies on each resource granting specific customer principals', correct: false, explanation: 'Adding a resource policy per customer per resource does not scale and requires manual management.' },
      { id: 'd', text: 'AWS Cognito Identity Pools with per-customer IAM roles', correct: false, explanation: 'While Cognito can issue temporary credentials, creating per-customer roles does not scale to thousands of customers.' },
    ],
    explanation: {
      overall: 'ABAC (Attribute-Based Access Control) uses tags as the access control mechanism instead of explicit principal ARNs. A single policy statement can govern access for all customers: Allow s3:GetObject when aws:ResourceTag/CustomerID equals aws:PrincipalTag/CustomerID. As new customers are added, no policy changes are needed — only the customer tag on their resources and principal is needed.',
      examTip: 'ABAC scales linearly with tagging. RBAC (Role-Based) scales exponentially with the number of roles. For multi-tenant SaaS on AWS, ABAC with customer tags is the modern, scalable approach.',
    },
    tags: ['abac', 'tagging', 'multi-tenant', 'scalable-access'],
  },
  {
    id: 'iam-005',
    stem: 'A Lambda function in Account A needs to call an API in Account B. The Lambda function has an execution role. The developer has configured the Lambda to call AssumeRole for a role in Account B. The role in Account B has a trust policy allowing the Lambda execution role from Account A. However, calls are failing with "not authorized to assume". What is missing?',
    type: 'single',
    difficulty: 3,
    topicSlug: 'iam',
    examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'The Lambda execution role in Account A needs sts:AssumeRole permission for the role in Account B', correct: true, explanation: 'Cross-account role assumption requires: (1) identity policy in Account A allowing sts:AssumeRole for the target role ARN, AND (2) trust policy in Account B allowing the Account A principal. Both are required.' },
      { id: 'b', text: 'The Lambda function needs a resource-based policy allowing Account B to invoke it', correct: false, explanation: 'Lambda resource-based policies control who can invoke the function, not what the function can call.' },
      { id: 'c', text: 'Account B needs to enable cross-account access in its root account settings', correct: false, explanation: 'No such setting exists. Cross-account access is controlled by IAM trust policies and identity policies.' },
      { id: 'd', text: 'A VPC peering connection must be established between the two accounts\' VPCs', correct: false, explanation: 'VPC peering is for network connectivity, not IAM authorization.' },
    ],
    explanation: {
      overall: 'Cross-account role assumption is a two-part authorization: (1) The caller\'s identity policy must include sts:AssumeRole permission for the target role ARN, and (2) The target role\'s trust policy must allow the caller\'s principal. Both conditions must be true. Missing either results in an access denied error.',
      examTip: 'Remember: for cross-account AssumeRole, you need BOTH an identity policy allowing sts:AssumeRole in Account A AND a trust policy in Account B. The trust policy alone is not sufficient.',
    },
    tags: ['cross-account', 'sts', 'assume-role', 'trust-policy'],
  },
  {
    id: 'iam-006',
    stem: 'A company wants to allow all AWS services to use a specific S3 bucket for logging, but only when they are performing actions within the same AWS account. They want to prevent the confused deputy problem. Which condition key should be included in the S3 bucket policy?',
    type: 'single', difficulty: 3, topicSlug: 'iam', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'aws:SourceAccount matching the account ID', correct: false, explanation: 'aws:SourceAccount alone is correct but aws:SourceArn is more specific and prevents any service in the account from writing, not just the intended service.' },
      { id: 'b', text: 'aws:SourceArn matching the specific service ARN making the request', correct: true, explanation: 'aws:SourceArn specifies the exact ARN of the resource making the cross-service call, preventing any other service or account from exploiting the bucket policy.' },
      { id: 'c', text: 'aws:PrincipalArn matching the service principal', correct: false, explanation: 'aws:PrincipalArn matches the IAM principal making the call, not the source service in a cross-service scenario.' },
      { id: 'd', text: 'aws:CalledVia matching the service', correct: false, explanation: 'aws:CalledVia is used when a service calls another service on behalf of a user, not for resource-based policy confused deputy prevention.' },
    ],
    explanation: { overall: 'The confused deputy problem occurs when a trusted service is manipulated into acting on behalf of an unauthorized party. Using aws:SourceArn (and optionally aws:SourceAccount) in S3 bucket policies restricts writes to only the specific AWS service resource that should be logging, preventing other services or accounts from exploiting the trust relationship.', examTip: 'Confused deputy fix: add aws:SourceArn and/or aws:SourceAccount conditions to resource-based policies for service principals. Common for S3 logging buckets, SNS delivery policies, and CloudWatch logs.' },
    tags: ['confused-deputy', 'source-arn', 'bucket-policy', 'service-principal'],
  },
  {
    id: 'iam-007',
    stem: 'An enterprise has Active Directory on-premises and wants employees to sign in to the AWS Management Console using their corporate credentials. They want a single place to manage AWS account access across 20 AWS accounts. Which AWS service provides the most operationally efficient solution?',
    type: 'single', difficulty: 2, topicSlug: 'iam', examDomain: 'organizational-complexity',
    options: [
      { id: 'a', text: 'Create IAM users in each account and configure SAML federation per account', correct: false, explanation: 'Configuring SAML federation separately in each of 20 accounts is highly operational overhead and duplicates work.' },
      { id: 'b', text: 'AWS IAM Identity Center (SSO) integrated with on-premises Active Directory via AD Connector or AWS Managed AD', correct: true, explanation: 'IAM Identity Center provides centralized SSO across all AWS Organization accounts from a single portal. Integration with on-premises AD via AWS Managed AD or AD Connector enables corporate credentials for all accounts.' },
      { id: 'c', text: 'Deploy AWS Directory Service in each account and synchronize with on-premises AD', correct: false, explanation: 'Deploying Directory Service per account is unnecessary overhead. IAM Identity Center with a single AD connection handles all accounts.' },
      { id: 'd', text: 'Use Amazon Cognito User Pools with SAML federation for AWS Console access', correct: false, explanation: 'Cognito is for application authentication, not AWS Management Console access across multiple accounts.' },
    ],
    explanation: { overall: 'IAM Identity Center (formerly AWS SSO) is the purpose-built service for centralized, multi-account AWS access management. It integrates with external IdPs (Active Directory, Okta, Azure AD) and enables users to access any assigned AWS account from a single portal using their corporate credentials. Permission sets define what access users get in each account.', examTip: 'IAM Identity Center = centralized SSO for AWS Organization accounts. Single integration point for external IdP. Users get a portal listing all their assigned accounts.' },
    tags: ['identity-center', 'sso', 'active-directory', 'federation', 'organizations'],
  },
  {
    id: 'iam-008',
    stem: 'A third-party auditing company needs access to your AWS account to perform security assessments. You want to grant them read-only access but ensure they can only assume the role from their specific AWS account and only when they provide a unique identifier that you share with them out-of-band. Which trust policy element enforces this?',
    type: 'single', difficulty: 3, topicSlug: 'iam', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Add a Condition with aws:SourceAccount matching the third-party account ID', correct: false, explanation: 'aws:SourceAccount is for cross-service calls, not cross-account role assumption.' },
      { id: 'b', text: 'Add a Condition with sts:ExternalId matching the unique identifier you shared', correct: true, explanation: 'ExternalId prevents the confused deputy problem in cross-account scenarios. The third party must provide the ExternalId when calling AssumeRole. Without it, even with valid credentials from the right account, the assumption fails.' },
      { id: 'c', text: 'Add a Condition with aws:PrincipalOrgID matching the third-party organization', correct: false, explanation: 'aws:PrincipalOrgID restricts to principals within a specific AWS Organization — not useful for third-party external accounts.' },
      { id: 'd', text: 'Add a Condition with aws:MultiFactorAuthPresent: true', correct: false, explanation: 'MFA enforcement is valid for human users but does not provide the unique identifier verification needed for third-party cross-account access.' },
    ],
    explanation: { overall: 'The ExternalId condition in a cross-account role trust policy prevents the confused deputy attack. When you grant a third-party (like a SaaS vendor or auditor) access via a role, they could potentially assume your role to access other customer accounts that also trust them. The ExternalId is a secret shared only between you and the third party — without it, the role cannot be assumed even from the correct account.', examTip: 'ExternalId is specifically for cross-account role access granted to third parties. It appears in the role trust policy Condition: "StringEquals": {"sts:ExternalId": "unique-secret"}. Required whenever granting third-party cross-account access.' },
    tags: ['external-id', 'trust-policy', 'cross-account', 'confused-deputy'],
  },
  {
    id: 'iam-009',
    stem: 'A developer assumes a role that has S3 full access. When calling AssumeRole, they pass a session policy that only allows s3:GetObject. What S3 operations can the developer perform during this session?',
    type: 'single', difficulty: 3, topicSlug: 'iam', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'All S3 operations — the role has full access and overrides the session policy', correct: false, explanation: 'Session policies cannot expand permissions — they can only reduce them. The effective permission is the intersection.' },
      { id: 'b', text: 'Only s3:GetObject — effective permissions are the intersection of role policy and session policy', correct: true, explanation: 'Session policies restrict the role permissions for that specific session. Effective permissions = role permissions ∩ session policy. Even though the role allows full S3 access, the session is limited to s3:GetObject only.' },
      { id: 'c', text: 'No operations — combining a full-access role with a restricted session policy results in a deny', correct: false, explanation: 'Session policies do not create implicit denies. They restrict to the intersection, not block everything.' },
      { id: 'd', text: 'All S3 operations except those explicitly denied in the session policy', correct: false, explanation: 'Session policies are additive restrictions (intersection model), not a deny-list. Only what the session policy explicitly allows (AND the role allows) is permitted.' },
    ],
    explanation: { overall: 'Session policies are passed when programmatically assuming a role or when federating users. They cannot grant more permissions than the role policy, only restrict them. Effective permissions = intersection of (role identity policy) AND (session policy) AND (any permission boundaries). This is useful for temporarily restricting a role\'s permissions for a specific job or automation task.', examTip: 'Session policies can only REDUCE permissions, never INCREASE them. Effective = role ∩ session policy. Used by: AssumeRole with policy, AWS SSO permission sets, and federated users.' },
    tags: ['session-policy', 'assume-role', 'effective-permissions', 'intersection'],
  },
  {
    id: 'iam-010',
    stem: 'A company has a policy requiring MFA for all sensitive operations. A developer writes a Lambda function that calls AWS APIs to manage EC2 instances. The Lambda execution role has the required EC2 permissions, but the policy has a Condition requiring "aws:MultiFactorAuthPresent: true". What happens when Lambda calls the EC2 API?',
    type: 'single', difficulty: 3, topicSlug: 'iam', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'The call succeeds because Lambda uses temporary credentials that include MFA context', correct: false, explanation: 'Lambda uses a service role with temporary credentials. These credentials do not include MFA context because no human authenticated with MFA.' },
      { id: 'b', text: 'The call fails because Lambda\'s temporary credentials do not have MFA context, making the condition evaluate to false', correct: true, explanation: 'aws:MultiFactorAuthPresent is false or absent for service role temporary credentials. The condition fails and EC2 calls are denied. Use aws:MultiFactorAuthPresent with BoolIfExists to handle service role scenarios.' },
      { id: 'c', text: 'The call succeeds because SCPs override the MFA condition for service roles', correct: false, explanation: 'SCPs have no special interaction with MFA conditions. The IAM policy condition still applies to service role calls.' },
      { id: 'd', text: 'The call fails with a throttling error because Lambda cannot use MFA', correct: false, explanation: 'The failure is an authorization error (Access Denied), not a throttling error.' },
    ],
    explanation: { overall: 'aws:MultiFactorAuthPresent is only true when a human user authenticated with MFA. Service roles (Lambda, EC2 instance profiles, ECS task roles) never have MFA context. Using a strict "Bool: aws:MultiFactorAuthPresent: true" condition blocks all service-role access. Best practice: use "BoolIfExists" to allow the condition to pass when the key is not present (service roles), while still enforcing MFA for human users.', examTip: 'For MFA enforcement that doesn\'t break service roles: "BoolIfExists": {"aws:MultiFactorAuthPresent": false} — allows service roles (key absent) while blocking humans without MFA.' },
    tags: ['mfa', 'condition', 'service-role', 'lambda', 'bool-if-exists'],
  },
  {
    id: 'iam-011',
    stem: 'A company uses AWS Organizations. They want to ensure that no account in the organization can create resources in regions outside of us-east-1 and eu-west-1. What is the most efficient approach?',
    type: 'single', difficulty: 2, topicSlug: 'iam', examDomain: 'organizational-complexity',
    options: [
      { id: 'a', text: 'Create an IAM policy denying all actions outside us-east-1 and eu-west-1 and attach to all IAM users in all accounts', correct: false, explanation: 'This requires managing policies in every account and every IAM principal. Any new user or role would need the policy attached manually.' },
      { id: 'b', text: 'Create an SCP with a Deny for all actions where aws:RequestedRegion is not in [us-east-1, eu-west-1] and attach to the root OU', correct: true, explanation: 'SCPs apply to all principals in all accounts under the attached OU/root, including new accounts. A StringNotEquals condition on aws:RequestedRegion with a Deny statement blocks all calls outside the allowed regions.' },
      { id: 'c', text: 'Enable AWS Config rules in each account to detect and remediate resources in disallowed regions', correct: false, explanation: 'Config is reactive — resources get created first, then remediated. SCPs prevent creation proactively.' },
      { id: 'd', text: 'Use AWS Control Tower to create account-level region restrictions', correct: false, explanation: 'Control Tower uses SCPs under the hood for region restriction guardrails. Creating the SCP directly is equivalent and doesn\'t require Control Tower.' },
    ],
    explanation: { overall: 'The aws:RequestedRegion condition key in an SCP can restrict all API calls to specific regions. A Deny SCP with "StringNotEquals": {"aws:RequestedRegion": ["us-east-1","eu-west-1"]} attached to the root OU blocks all principals in all accounts from creating or managing resources in any other region. Global services (IAM, Route 53, CloudFront) are region-independent and must be exempted if needed.', examTip: 'SCP region restriction: Deny + StringNotEquals + aws:RequestedRegion. Attach to root OU for org-wide effect. Remember to add NotAction for global services (IAM, Route 53, STS, CloudFront).' },
    tags: ['scp', 'region-restriction', 'requested-region', 'organizations'],
  },
  {
    id: 'iam-012',
    stem: 'A company runs a SaaS application on AWS. Each customer gets an isolated S3 prefix (s3://bucket/customer-id/). Developers want to write a single IAM policy that allows each customer\'s IAM user to access only their own prefix, without creating a separate policy per customer. How can this be achieved?',
    type: 'single', difficulty: 2, topicSlug: 'iam', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Use IAM policy variables with ${aws:username} in the S3 resource ARN', correct: true, explanation: 'IAM policy variables like ${aws:username} are substituted with the actual value at evaluation time. A single policy with "Resource": "arn:aws:s3:::bucket/${aws:username}/*" grants each user access only to their own prefix.' },
      { id: 'b', text: 'Use ABAC with customer ID tags and aws:ResourceTag conditions', correct: false, explanation: 'ABAC with tags works well but requires tagging each S3 object with the customer ID. Policy variables with username/principal tags are simpler for prefix-based isolation.' },
      { id: 'c', text: 'Create S3 Access Points one per customer with a policy scoped to their prefix', correct: false, explanation: 'Access Points per customer work but require creating and managing one access point per customer — not a single policy solution.' },
      { id: 'd', text: 'Use a bucket policy with aws:SourceIp conditions per customer', correct: false, explanation: 'IP-based conditions are not reliable for isolating customers (shared IPs) and do not scale.' },
    ],
    explanation: { overall: 'IAM policy variables (${aws:username}, ${aws:userid}, ${aws:principaltype}) are placeholders in policy documents that AWS resolves to actual values at evaluation time. A single policy "Allow s3:GetObject on arn:aws:s3:::bucket/${aws:username}/*" applies to all users but each user can only access their named prefix. This eliminates the need for per-customer policies.', examTip: 'IAM policy variables: ${aws:username}, ${aws:userid}, ${aws:principaltype}. Used in Resource ARNs or condition values. Enables one policy to serve many principals with isolated access.' },
    tags: ['policy-variables', 's3', 'multi-tenant', 'single-policy'],
  },
  {
    id: 'iam-013',
    stem: 'A company wants its developers to be able to create IAM roles for new microservices, but only if those roles are limited to specific permissions and cannot exceed the developers\' own permissions. A security team member suggests using permission boundaries. How should this be implemented?',
    type: 'single', difficulty: 3, topicSlug: 'iam', examDomain: 'organizational-complexity',
    options: [
      { id: 'a', text: 'Create a permission boundary policy and give developers iam:CreateRole permission only', correct: false, explanation: 'Giving iam:CreateRole without a condition to enforce boundary attachment allows developers to create roles without the boundary, gaining unlimited permissions.' },
      { id: 'b', text: 'Give developers iam:CreateRole and iam:PutRolePolicy with a condition requiring iam:PermissionsBoundary on any role they create', correct: true, explanation: 'The condition "StringEquals": {"iam:PermissionsBoundary": "arn:aws:iam::ACCOUNT:policy/BoundaryPolicy"} on iam:CreateRole forces developers to always attach the boundary. Without it, the role creation is denied.' },
      { id: 'c', text: 'Attach a permission boundary to the developers\' IAM roles to limit what roles they can create', correct: false, explanation: 'Permission boundaries on developers limit their effective permissions but do not automatically propagate to roles they create.' },
      { id: 'd', text: 'Use SCPs to restrict iam:CreateRole to only allow roles with specific name prefixes', correct: false, explanation: 'SCPs can restrict role creation by name but cannot enforce permission boundary attachment or limit the policies attached to created roles.' },
    ],
    explanation: { overall: 'Delegated IAM administration with permission boundaries: grant developers iam:CreateRole and iam:AttachRolePolicy/iam:PutRolePolicy, but add a condition requiring "iam:PermissionsBoundary" equals a specific boundary policy ARN. The boundary defines the maximum permissions any created role can have. This prevents privilege escalation — developers cannot create roles with more permissions than the boundary allows, even if they have broad IAM permissions.', examTip: 'Delegated admin pattern: iam:CreateRole with StringEquals iam:PermissionsBoundary condition = developer can create roles but all roles are bounded. The boundary is the ceiling, preventing privilege escalation.' },
    tags: ['permission-boundary', 'delegated-admin', 'privilege-escalation', 'condition'],
  },
  {
    id: 'iam-014',
    stem: 'An EC2 instance in Account A has an instance profile with s3:GetObject permission. The target S3 bucket is in Account B. The bucket policy in Account B grants s3:GetObject to the IAM role ARN from Account A. What additional step is required for the EC2 instance to successfully access the bucket?',
    type: 'single', difficulty: 2, topicSlug: 'iam', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'No additional step — the bucket policy granting access to the Account A role is sufficient for cross-account access', correct: false, explanation: 'For cross-account S3 access, BOTH the identity policy (Account A role) AND the resource policy (Account B bucket) must allow. However, the identity policy already has s3:GetObject — so this is actually correct.' },
      { id: 'b', text: 'The EC2 instance role in Account A already has s3:GetObject and the bucket policy allows it — this is already sufficient', correct: true, explanation: 'Cross-account S3 access requires both the identity policy in Account A to allow s3:GetObject AND the resource policy (bucket policy) in Account B to explicitly allow the Account A principal. Both conditions are met here.' },
      { id: 'c', text: 'Create a VPC Gateway Endpoint for S3 in Account A\'s VPC', correct: false, explanation: 'VPC Gateway Endpoints are for routing optimization and private connectivity within a region, not for cross-account authorization.' },
      { id: 'd', text: 'Add an explicit Allow for the Account A role in Account B\'s IAM policies', correct: false, explanation: 'IAM policies in Account B apply to principals in Account B. The Account A role is a foreign principal — it is managed via the bucket policy, not Account B\'s IAM.' },
    ],
    explanation: { overall: 'Cross-account S3 access requires: (1) Identity policy in Account A allowing the S3 action, AND (2) Bucket policy in Account B explicitly granting the Account A principal access. Both conditions are already satisfied in this scenario. Compare: same-account S3 access only requires the identity policy OR bucket policy — either alone is sufficient within the same account.', examTip: 'Cross-account S3: need BOTH identity policy (Account A) AND bucket policy (Account B). Same-account S3: identity policy OR bucket policy suffices. This is a very common SA Pro distinction.' },
    tags: ['cross-account', 's3', 'bucket-policy', 'identity-policy'],
  },
  {
    id: 'iam-015',
    stem: 'A company wants to require that all API calls to sensitive AWS services originate from within their corporate network (specific IP range: 10.0.0.0/8). However, they also have Lambda functions that need to call these same APIs. How should the IAM policy condition be written?',
    type: 'single', difficulty: 3, topicSlug: 'iam', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Use Condition: IpAddress aws:SourceIp: "10.0.0.0/8"', correct: false, explanation: 'This blocks Lambda from calling the APIs because Lambda\'s outbound IP is not in 10.0.0.0/8. Lambda would be denied access.' },
      { id: 'b', text: 'Use Condition: NotIpAddress aws:SourceIp plus aws:ViaAWSService: true to allow service calls', correct: true, explanation: 'The aws:ViaAWSService: true condition allows requests made by AWS services on behalf of users (like Lambda calling another service). Combined with IP restriction, this permits both corporate network calls and service-to-service calls.' },
      { id: 'c', text: 'Deploy Lambda functions in a VPC with a NAT Gateway using a corporate IP range', correct: false, explanation: 'NAT Gateway uses a public Elastic IP, not the corporate 10.0.0.0/8 range. This would require the corporate network to own the EIP.' },
      { id: 'd', text: 'Use aws:CalledVia condition to exempt Lambda from the IP restriction', correct: false, explanation: 'aws:CalledVia identifies when a service makes a call through another service (like CloudFormation calling S3). It does not broadly exempt Lambda execution roles from IP conditions.' },
    ],
    explanation: { overall: 'aws:ViaAWSService is a boolean condition key that is true when a request is made by an AWS service using service role credentials. Adding a Deny condition with NotIpAddress + a second condition NOT matching aws:ViaAWSService:true creates an IP restriction that applies to human users but exempts AWS service-to-service calls like Lambda executions.', examTip: 'aws:ViaAWSService: true exempts AWS service calls from conditions like IP restrictions. Useful when you want IP-based policies that still allow automation/Lambda to function normally.' },
    tags: ['source-ip', 'via-aws-service', 'condition', 'network-restriction'],
  },
  {
    id: 'iam-016',
    stem: 'What actions can ONLY be performed by the AWS account root user and cannot be delegated to IAM users or roles? (Select TWO)',
    type: 'multiple', difficulty: 2, topicSlug: 'iam', examDomain: 'organizational-complexity',
    options: [
      { id: 'a', text: 'Changing the account support plan', correct: true, explanation: 'Changing the AWS support plan (Basic, Developer, Business, Enterprise) requires root user authentication and cannot be performed by IAM entities.' },
      { id: 'b', text: 'Creating an Organization and inviting accounts', correct: false, explanation: 'An IAM user or role with appropriate Organizations permissions can create and manage an Organization.' },
      { id: 'c', text: 'Closing an AWS account', correct: true, explanation: 'Closing (deleting) an AWS account requires root user authentication. This cannot be performed by any IAM user or role.' },
      { id: 'd', text: 'Enabling MFA on IAM users', correct: false, explanation: 'IAM administrators with appropriate permissions (iam:EnableMFADevice) can manage MFA for IAM users.' },
    ],
    explanation: { overall: 'Root user-only actions: (1) Change account support plan, (2) Close the account, (3) Change account email address, (4) Enable or disable MFA on the root user itself, (5) Create X.509 signing certificates, (6) Restore IAM user permissions (when the only admin is locked out). Root should never be used for day-to-day operations.', examTip: 'Root-only: close account, change support plan, change root email, manage root MFA, restore IAM when locked out. Everything else can be delegated to IAM.' },
    tags: ['root-user', 'account-closure', 'support-plan'],
  },
  {
    id: 'iam-017',
    stem: 'A mobile application uses Amazon Cognito for user authentication. After users sign in, the app needs to call AWS services directly from the mobile device. Which Cognito component provides temporary AWS credentials to authenticated users?',
    type: 'single', difficulty: 2, topicSlug: 'iam', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Cognito User Pools — issues JWT tokens that can be exchanged for AWS credentials', correct: false, explanation: 'Cognito User Pools handles user authentication and issues JWT tokens. JWT tokens cannot be directly used to call AWS APIs — they need to be exchanged via Cognito Identity Pools.' },
      { id: 'b', text: 'Cognito Identity Pools — exchanges user pool tokens or social provider tokens for temporary AWS credentials via STS', correct: true, explanation: 'Cognito Identity Pools (Federated Identities) exchanges authenticated user tokens for temporary AWS credentials using STS AssumeRoleWithWebIdentity. The app can then call AWS services directly using these temporary credentials.' },
      { id: 'c', text: 'Cognito Sync — provides AWS credentials alongside data synchronization', correct: false, explanation: 'Cognito Sync is for syncing user data across devices, not for issuing AWS credentials.' },
      { id: 'd', text: 'AWS STS directly — users call AssumeRoleWithWebIdentity from the mobile device using their JWT token', correct: false, explanation: 'While technically possible to call STS directly, Cognito Identity Pools is the managed service that handles this flow, including unauthenticated guest access and cross-provider federation.' },
    ],
    explanation: { overall: 'Cognito User Pools + Identity Pools is the standard pattern: User Pools authenticates users and issues JWTs. Identity Pools exchanges those JWTs (or social IdP tokens) for temporary AWS credentials by calling STS AssumeRoleWithWebIdentity. The mobile app receives temporary IAM role credentials to call DynamoDB, S3, API Gateway, etc. directly with fine-grained access control.', examTip: 'User Pools = authentication (who you are). Identity Pools = authorization (what you can do with AWS). Combined pattern: User Pools token → Identity Pools → STS → temporary AWS credentials.' },
    tags: ['cognito', 'identity-pools', 'user-pools', 'mobile', 'sts'],
  },
  {
    id: 'iam-018',
    stem: 'An IAM Access Analyzer finding shows that an S3 bucket in your account is accessible from a principal in an external AWS account that is not in your organization. The access is intentional — it is a partner company. How should you handle this finding?',
    type: 'single', difficulty: 1, topicSlug: 'iam', examDomain: 'continuous-improvement',
    options: [
      { id: 'a', text: 'Delete the bucket policy to resolve the finding', correct: false, explanation: 'If the access is intentional, removing the policy breaks the legitimate use case.' },
      { id: 'b', text: 'Archive the finding in IAM Access Analyzer to mark it as known and expected', correct: true, explanation: 'Archiving a finding marks it as reviewed and expected. Future policy changes that result in new external access will generate new findings, while archived ones remain acknowledged.' },
      { id: 'c', text: 'Add the partner account to your AWS Organization to resolve the finding', correct: false, explanation: 'External partner accounts should not be added to your Organization. The finding should be acknowledged/archived instead.' },
      { id: 'd', text: 'Create a suppression rule in AWS Security Hub to ignore this finding type', correct: false, explanation: 'IAM Access Analyzer findings are managed within Access Analyzer itself using archive rules, not Security Hub suppression.' },
    ],
    explanation: { overall: 'IAM Access Analyzer continuously monitors resource policies for cross-account and public access. When you review a finding and determine it is intentional (like a known partner relationship), you archive it. This removes it from the active findings list. You can create archive rules to automatically archive findings matching specific patterns (e.g., access from a specific known account).', examTip: 'IAM Access Analyzer: Archive = reviewed and expected. Active = requires review. Create archive rules for recurring expected patterns. New access after archiving generates a new finding.' },
    tags: ['access-analyzer', 'findings', 'archive', 'cross-account'],
  },
  {
    id: 'iam-019',
    stem: 'A company has multiple AWS accounts in an Organization. They want to restrict S3 bucket policies so that buckets cannot be made publicly accessible, across ALL accounts automatically, including newly created accounts. Which combination achieves this with the least effort?',
    type: 'multiple', difficulty: 2, topicSlug: 'iam', examDomain: 'organizational-complexity',
    options: [
      { id: 'a', text: 'Enable S3 Block Public Access at the account level in each account', correct: false, explanation: 'Account-level Block Public Access must be manually enabled in each account. It does not apply automatically to new accounts.' },
      { id: 'b', text: 'Create an SCP denying s3:PutBucketPublicAccessBlock with a condition that it cannot be disabled', correct: false, explanation: 'An SCP cannot grant S3 Block Public Access — it can deny turning it off, but you still need to initially enable it per account.' },
      { id: 'c', text: 'Use AWS CloudFormation StackSets with Organizations service-managed permissions to deploy an S3 Block Public Access configuration to all accounts and new accounts', correct: true, explanation: 'StackSets with Organizations service-managed permissions auto-deploy to existing accounts and all new accounts joining the target OU. A CloudFormation template with AWS::S3::AccountPublicAccessBlock enables S3 BPA organization-wide.' },
      { id: 'd', text: 'Create an SCP denying s3:PutBucketAcl and s3:PutBucketPolicy on any resource', correct: false, explanation: 'Denying bucket policy updates would break legitimate use cases. The correct control is S3 Block Public Access, not restricting all policy changes.' },
    ],
    explanation: { overall: 'The combination of CloudFormation StackSets with Organizations service-managed permissions + Automatic deployment = all current and future accounts get the configuration. The StackSet deploys AWS::S3::AccountPublicAccessBlock resource enabling all 4 Block Public Access settings. New accounts added to the target OU automatically receive the stack. This is the automation-first approach to organization-wide controls.', examTip: 'StackSets + Organizations service-managed + Automatic deployment = zero-touch deployment to all current and future accounts in an OU. Perfect for security baselines (S3 BPA, CloudTrail, Config rules).' },
    tags: ['stacksets', 's3-block-public-access', 'organizations', 'account-baseline'],
  },
  {
    id: 'iam-020',
    stem: 'A service team creates IAM roles for EC2 instances. They have a broad iam:PassRole permission to pass any role to EC2. The security team is concerned this allows developers to pass highly privileged roles to EC2 instances. How can iam:PassRole be restricted to only allow passing roles intended for EC2?',
    type: 'single', difficulty: 3, topicSlug: 'iam', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Use a resource condition limiting iam:PassRole to role ARNs with a specific naming prefix', correct: false, explanation: 'Name prefix restrictions help but do not enforce that the role is intended for EC2 specifically. A naming convention can be bypassed.' },
      { id: 'b', text: 'Add a condition "StringEquals": {"iam:PassedToService": "ec2.amazonaws.com"} to the iam:PassRole policy', correct: true, explanation: 'iam:PassedToService restricts which service principal the role can be passed to. "ec2.amazonaws.com" ensures the developer can only pass roles whose trust policy allows EC2 to assume them, not Lambda, ECS, or other services.' },
      { id: 'c', text: 'Use a permission boundary on the developer role that excludes iam:PassRole', correct: false, explanation: 'Removing iam:PassRole entirely prevents all role passing, breaking legitimate EC2 usage.' },
      { id: 'd', text: 'Create separate IAM roles for each service type and require developers to use specific roles', correct: false, explanation: 'While a good practice, this requires operational enforcement. The iam:PassedToService condition technically enforces the restriction in IAM itself.' },
    ],
    explanation: { overall: 'The iam:PassedToService condition key in iam:PassRole policies restricts which AWS service the role can be passed to. "StringEquals": {"iam:PassedToService": "ec2.amazonaws.com"} ensures developers can only assign roles with EC2 as the trusted service. This prevents passing admin Lambda roles to EC2 instances as a privilege escalation path.', examTip: 'iam:PassedToService is the correct condition for restricting iam:PassRole to specific services. Common values: ec2.amazonaws.com, lambda.amazonaws.com, ecs-tasks.amazonaws.com. Critical for preventing privilege escalation via role passing.' },
    tags: ['pass-role', 'passed-to-service', 'privilege-escalation', 'condition'],
  },
];
