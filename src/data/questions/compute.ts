import type { Question } from '../../types/question';

export const computeQuestions: Question[] = [
  {
    id: 'compute-001',
    stem: 'A financial services company runs a high-performance trading application that requires the lowest possible network latency between EC2 instances. The application must complete tasks without interruption. Which EC2 placement configuration is optimal?',
    type: 'single', difficulty: 2, topicSlug: 'compute', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Spread Placement Group across multiple AZs', correct: false, explanation: 'Spread placement groups maximize fault tolerance but do not minimize latency — instances are on different physical hardware.' },
      { id: 'b', text: 'Cluster Placement Group with On-Demand instances in a single AZ', correct: true, explanation: 'Cluster placement groups place instances in the same rack in a single AZ, achieving 10 Gbps enhanced networking and lowest latency. On-Demand ensures no interruptions.' },
      { id: 'c', text: 'Partition Placement Group with Reserved Instances across 3 AZs', correct: false, explanation: 'Partition groups are for distributed systems (HDFS/Cassandra). They do not minimize inter-instance latency.' },
      { id: 'd', text: 'Cluster Placement Group with Spot Instances', correct: false, explanation: 'Spot instances can be interrupted with 2-minute notice — unacceptable for a trading application.' },
    ],
    explanation: { overall: 'Cluster Placement Groups pack instances into the same physical rack within a single AZ, enabling 10 Gbps enhanced networking between instances. On-Demand instances avoid interruption risk. Tradeoff: single rack is a single point of failure.', examTip: 'Cluster PG = max throughput, min latency. Spread PG = max HA, max 7/AZ. Partition PG = HDFS/Cassandra failure domain isolation.' },
    tags: ['placement-groups', 'cluster', 'hpc', 'latency'],
  },
  {
    id: 'compute-002',
    stem: 'A company runs batch processing jobs that can tolerate interruptions. Jobs checkpoint their progress every 5 minutes to S3. The jobs typically run for 2-4 hours. Which purchasing option provides the best cost savings?',
    type: 'single', difficulty: 1, topicSlug: 'compute', examDomain: 'cost-optimization',
    options: [
      { id: 'a', text: 'On-Demand instances', correct: false, explanation: 'On-Demand has no discount. For interruptible workloads, Spot instances provide up to 90% savings.' },
      { id: 'b', text: 'Spot Instances with checkpointing to S3 every 5 minutes', correct: true, explanation: 'Spot Instances can be 60-90% cheaper. With 5-minute checkpoints, at most 5 minutes of work is lost on interruption. The job restarts from the last checkpoint.' },
      { id: 'c', text: 'Reserved Instances with a 1-year commitment', correct: false, explanation: 'Reserved Instances are for steady-state workloads. Batch jobs are not continuously running, so RIs waste money on idle time.' },
      { id: 'd', text: 'Dedicated Hosts with Savings Plans', correct: false, explanation: 'Dedicated Hosts are for license compliance, not cost optimization of batch jobs.' },
    ],
    explanation: { overall: 'Spot Instances are ideal for fault-tolerant batch workloads. The key requirement is checkpointing: saving progress frequently so interruptions only lose minimal work. AWS provides 2-minute interruption notice before reclaiming Spot instances.', examTip: 'Spot use cases: batch processing, HPC, CI/CD, containerized workloads, stateless web servers. Non-Spot: databases, stateful applications, anything needing guaranteed uptime.' },
    tags: ['spot-instances', 'batch', 'cost-optimization', 'checkpointing'],
  },
  {
    id: 'compute-003',
    stem: 'A company wants to run a mixed fleet of On-Demand and Spot instances for their web application. During peak hours they need guaranteed capacity; during off-peak they want to minimize costs. Which EC2 Auto Scaling feature supports this?',
    type: 'single', difficulty: 2, topicSlug: 'compute', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Separate On-Demand and Spot Auto Scaling groups with different desired capacities', correct: false, explanation: 'Two separate groups require separate scaling policies and management. EC2 Auto Scaling mixed instances policy handles this in a single group.' },
      { id: 'b', text: 'EC2 Auto Scaling with mixed instances policy, defining On-Demand base capacity and percentage above base', correct: true, explanation: 'Mixed instances policy allows defining: on-demand base (always On-Demand), then a percentage split (e.g., 20% On-Demand, 80% Spot) for capacity above the base. ASG automatically manages both pools.' },
      { id: 'c', text: 'Spot Fleet with target capacity and diversified allocation strategy', correct: false, explanation: 'Spot Fleet can mix Spot and On-Demand but is a separate fleet construct, not Auto Scaling groups with lifecycle hooks and scaling policies.' },
      { id: 'd', text: 'Schedule scaling to switch between On-Demand and Spot based on time of day', correct: false, explanation: 'You cannot "switch" instance types via scheduled scaling. Scheduled scaling changes the desired capacity, not the purchase type.' },
    ],
    explanation: { overall: 'Mixed instances policy in Auto Scaling: set on-demand base capacity (e.g., 2 instances always On-Demand) + on-demand percentage above base (e.g., 20%). The remaining 80% above base uses Spot. Multiple instance types diversify Spot pool to reduce interruption risk.', examTip: 'Mixed instances policy = best of both worlds. On-Demand base = baseline reliability. Spot percentage = cost savings. Instance diversification = Spot interruption resilience. This is the standard production pattern for cost-optimized web tiers.' },
    tags: ['auto-scaling', 'mixed-instances', 'spot', 'on-demand'],
  },
  {
    id: 'compute-004',
    stem: 'A company is running EC2 instances and wants to ensure that new instances launched in an Auto Scaling group always have the latest security patches applied. Which is the most automated approach?',
    type: 'single', difficulty: 2, topicSlug: 'compute', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'SSM Patch Manager with a maintenance window to patch all running instances', correct: false, explanation: 'Patching running instances is reactive. New instances launched from an old AMI are unpatched until the next maintenance window.' },
      { id: 'b', text: 'EC2 Image Builder pipeline that builds new AMIs with patches; Auto Scaling group launch template updated automatically', correct: true, explanation: 'EC2 Image Builder automates AMI creation with latest patches. Combined with Systems Manager Parameter Store for the AMI ID (updated by the pipeline), Auto Scaling groups automatically use the latest patched AMI for new instances.' },
      { id: 'c', text: 'User Data script that runs yum update on each new instance launch', correct: false, explanation: 'User Data runs at launch time but downloads patches over the internet, is slower, and can fail. Building patches into the AMI is more reliable.' },
      { id: 'd', text: 'AWS Config rule to detect instances with old AMIs and trigger lambda remediation', correct: false, explanation: 'Config is detective. It can detect old AMIs but cannot force new instances to use updated AMIs proactively.' },
    ],
    explanation: { overall: 'Golden AMI pipeline: EC2 Image Builder schedules regular AMI builds that install the latest patches. The pipeline stores the new AMI ID in SSM Parameter Store. Auto Scaling launch templates reference the SSM parameter (not the AMI ID directly). When Image Builder updates the parameter, new instances from the ASG use the patched AMI automatically.', examTip: 'EC2 Image Builder = automated golden AMI. SSM Parameter Store as AMI ID reference in launch templates = auto-pick-up new AMIs. This is the immutable infrastructure pattern for EC2 patching.' },
    tags: ['ec2-image-builder', 'golden-ami', 'patching', 'auto-scaling'],
  },
  {
    id: 'compute-005',
    stem: 'A media company runs video transcoding workloads on EC2. Jobs arrive unpredictably throughout the day. Individual jobs take 10-60 minutes each. They need elastic scaling, cost efficiency, and simplified management. Which compute service is best suited?',
    type: 'single', difficulty: 2, topicSlug: 'compute', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'EC2 Auto Scaling group with scheduled scaling', correct: false, explanation: 'Scheduled scaling is for predictable patterns. Unpredictable job arrivals require reactive (metric-based or queue-based) scaling.' },
      { id: 'b', text: 'AWS Batch with Spot compute environments using SQS queue as job source', correct: true, explanation: 'AWS Batch manages EC2/Spot fleet provisioning, job scheduling, and retry logic automatically. Spot compute environments minimize cost. Jobs from SQS enable elastic scaling based on queue depth.' },
      { id: 'c', text: 'Lambda functions with 15-minute maximum timeout', correct: false, explanation: 'Lambda has a 15-minute maximum timeout, which is insufficient for 60-minute transcoding jobs.' },
      { id: 'd', text: 'ECS Fargate with SQS-triggered tasks', correct: false, explanation: 'Fargate can run transcoding tasks but does not have the built-in job scheduling, retry, and dependency management that AWS Batch provides.' },
    ],
    explanation: { overall: 'AWS Batch is purpose-built for batch computing. It manages the compute environment (EC2/Spot), queue depth-based scaling, job dependencies, and retries. For Spot-based batch jobs, AWS Batch handles interruptions by retrying jobs. Managed compute environments eliminate fleet management overhead.', examTip: 'AWS Batch vs Lambda: Batch for jobs >15 min, high-CPU/memory, complex job dependencies. Lambda for short event-driven functions. Batch vs ECS: Batch for batch workloads with scheduling. ECS for long-running services.' },
    tags: ['aws-batch', 'spot', 'transcoding', 'hpc'],
  },
  {
    id: 'compute-006',
    stem: 'A startup wants to run stateless web servers behind an Application Load Balancer. They need to scale based on CPU utilization and want the lowest operational overhead. They do not need SSH access to the instances. Which configuration is best?',
    type: 'single', difficulty: 1, topicSlug: 'compute', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'EC2 Auto Scaling with target tracking policy on CPUUtilization, using SSM Session Manager for access', correct: true, explanation: 'Target tracking scaling automatically adjusts capacity to maintain CPU at the target. SSM Session Manager provides secure shell access without SSH key pairs or open port 22.' },
      { id: 'b', text: 'EC2 with manual capacity planning and SSH access via bastion host', correct: false, explanation: 'Manual capacity planning does not scale elastically. Bastion hosts add operational overhead and attack surface.' },
      { id: 'c', text: 'Lambda behind API Gateway for zero-server management', correct: false, explanation: 'Lambda is not suited for long-running web server workloads and has execution time limits. The question specifies web servers.' },
      { id: 'd', text: 'Auto Scaling with simple scaling policy and CloudWatch alarms with 5-minute periods', correct: false, explanation: 'Simple scaling waits for cooldown periods before additional actions, reacting slowly. Target tracking is more responsive and self-managing.' },
    ],
    explanation: { overall: 'Target tracking scaling is the simplest and most effective scaling policy. You set the desired metric value (e.g., CPU 50%) and Auto Scaling automatically adds/removes instances. SSM Session Manager eliminates bastion hosts and SSH key management while providing audit logging.', examTip: 'Scaling policy types: Target Tracking (simplest, self-manages), Step Scaling (preconfigured steps), Simple Scaling (single action with cooldown). Target Tracking is recommended for most use cases.' },
    tags: ['auto-scaling', 'target-tracking', 'ssm', 'alb'],
  },
  {
    id: 'compute-007',
    stem: 'A company runs EC2 instances 24/7 with a consistent compute requirement. They want maximum cost savings with a commitment. Which purchasing option provides the highest discount for continuous workloads?',
    type: 'single', difficulty: 1, topicSlug: 'compute', examDomain: 'cost-optimization',
    options: [
      { id: 'a', text: 'Compute Savings Plans with a 3-year all-upfront commitment', correct: true, explanation: 'Compute Savings Plans with 3-year all-upfront commitment provides up to 66% savings over On-Demand with flexibility across instance families, regions, and even Lambda/Fargate.' },
      { id: 'b', text: 'Standard Reserved Instances with a 1-year partial-upfront', correct: false, explanation: 'Standard RIs provide ~40% savings on 1-year. Lower discount than 3-year Compute Savings Plans, and less flexible (locked to specific instance family/region).' },
      { id: 'c', text: 'On-Demand Capacity Reservations', correct: false, explanation: 'Capacity Reservations guarantee availability but do not provide any pricing discount.' },
      { id: 'd', text: 'EC2 Instance Savings Plans for a specific instance family', correct: false, explanation: 'EC2 Instance Savings Plans provide up to 72% savings but are locked to a specific instance family/region. Compute Savings Plans are more flexible at slightly lower savings.' },
    ],
    explanation: { overall: 'For maximum savings on continuous workloads: 3-year all-upfront Compute Savings Plans (up to 66%) or EC2 Instance Savings Plans (up to 72% but less flexible). Key distinction: Compute Savings Plans work across instance families, regions, and even Lambda/Fargate. EC2 Instance Savings Plans are locked to a family/region.', examTip: 'Savings hierarchy (highest to lowest discount): 3yr EC2 Instance SP > 3yr Compute SP > 1yr SP > On-Demand. Flexibility hierarchy: Compute SP (most) > EC2 Instance SP > Standard RI. SA Pro often asks for "highest savings" vs "most flexibility" tradeoffs.' },
    tags: ['savings-plans', 'reserved-instances', 'cost-optimization', 'commitment'],
  },
  {
    id: 'compute-008',
    stem: 'A company uses EC2 Auto Scaling. During a deployment, they want to ensure new instances are healthy and serving traffic before old instances are terminated. The instances need a 5-minute warm-up period after launch. Which lifecycle hook configuration enables this?',
    type: 'single', difficulty: 3, topicSlug: 'compute', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Set the health check grace period to 5 minutes in Auto Scaling group settings', correct: false, explanation: 'Health check grace period delays health checks but does not pause scaling activities or provide lifecycle hooks for custom logic.' },
      { id: 'b', text: 'Configure a lifecycle hook on EC2_INSTANCE_LAUNCHING state; send heartbeat after warm-up and ALB health check passes', correct: true, explanation: 'Lifecycle hooks pause instance launch in a Wait state. A Lambda or SSM document can wait for the warm-up period and ALB health check, then complete the hook. The instance only moves to InService after the hook completes.' },
      { id: 'c', text: 'Set the cooldown period to 5 minutes', correct: false, explanation: 'Cooldown periods delay the next scaling action after a scaling event, not the individual instance launch process.' },
      { id: 'd', text: 'Use a scheduled action to delay instance termination by 5 minutes', correct: false, explanation: 'Scheduled actions modify desired capacity at specific times — they do not control individual instance launch warm-up.' },
    ],
    explanation: { overall: 'Lifecycle hooks intercept Auto Scaling events (launching/terminating). EC2_INSTANCE_LAUNCHING hook pauses the instance in Pending:Wait state. A Lambda listens to the EventBridge event, waits for the application to be ready, then calls complete-lifecycle-action to move to InService. Default timeout is 1 hour.', examTip: 'Lifecycle hooks: EC2_INSTANCE_LAUNCHING = customize before InService. EC2_INSTANCE_TERMINATING = drain/backup before terminated. Use for rolling deployments, pre-registration, draining connections.' },
    tags: ['lifecycle-hooks', 'auto-scaling', 'warm-up', 'deployment'],
  },
  {
    id: 'compute-009',
    stem: 'A company has a regulatory requirement to run their workloads on dedicated physical hardware, but they want to retain the flexibility to change instance types without giving up the licensing compliance benefit. Which EC2 option provides this?',
    type: 'single', difficulty: 2, topicSlug: 'compute', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Dedicated Instances', correct: false, explanation: 'Dedicated Instances run on dedicated hardware but are not associated with a specific physical host. You cannot control placement or bring BYOL per-socket/per-core licenses.' },
      { id: 'b', text: 'Dedicated Hosts with host affinity disabled', correct: true, explanation: 'Dedicated Hosts provide a dedicated physical server with control over instance placement. With host affinity disabled, you can launch different instance types on the same host. Hosts support BYOL per-socket/per-core/per-VM licensing.' },
      { id: 'c', text: 'Spot Instances on dedicated hardware', correct: false, explanation: 'Spot Instances do not have dedicated hardware guarantees.' },
      { id: 'd', text: 'EC2 with Placement Groups on dedicated hardware', correct: false, explanation: 'Placement Groups optimize placement strategies but do not provide dedicated hardware isolation.' },
    ],
    explanation: { overall: 'Dedicated Hosts provide dedicated physical servers. You can see sockets, cores, and control instance placement. This is required for BYOL licenses (Windows Server, SQL Server, SUSE) that are licensed per physical socket/core. Host affinity can be configured to allow or require specific instance-to-host mapping.', examTip: 'Dedicated Hosts vs Dedicated Instances: Hosts = specific server, BYOL support, host visibility. Instances = dedicated hardware but no host visibility/control, no BYOL. Use Hosts for compliance + BYOL.' },
    tags: ['dedicated-hosts', 'byol', 'dedicated-instances', 'compliance'],
  },
  {
    id: 'compute-010',
    stem: 'An EC2 Auto Scaling group is configured with target tracking scaling. During a sudden traffic spike, instances take 8 minutes to launch (AMI bake time + application startup). Users experience poor performance during this 8-minute window. Which approach reduces the impact?',
    type: 'single', difficulty: 2, topicSlug: 'compute', examDomain: 'continuous-improvement',
    options: [
      { id: 'a', text: 'Reduce the AMI size to speed up launch time', correct: false, explanation: 'AMI size affects EBS snapshot restoration time but application startup is often the bottleneck. A smaller AMI alone may not reduce 8 minutes significantly.' },
      { id: 'b', text: 'Enable predictive scaling to forecast demand and pre-launch instances before the spike', correct: true, explanation: 'Predictive scaling uses ML to forecast traffic patterns and launches instances in advance of demand. For recurring spikes (daily peaks), it pre-warms capacity before the bottleneck window.' },
      { id: 'c', text: 'Switch to a larger instance type to reduce the number of instances needed', correct: false, explanation: 'Larger instances reduce the count needed but do not reduce the 8-minute launch time bottleneck itself.' },
      { id: 'd', text: 'Increase the desired capacity permanently to avoid scaling', correct: false, explanation: 'Permanently increasing capacity wastes money during off-peak times — contradicting cost efficiency goals.' },
    ],
    explanation: { overall: 'Predictive Scaling analyzes historical CloudWatch metrics and schedules Auto Scaling activities before demand arrives. For applications with predictable daily/weekly patterns (business hours, market open), predictive scaling pre-launches instances to have them ready before the spike.', examTip: 'Predictive Scaling: trains on 14 days of history, forecasts 48 hours ahead, works alongside reactive (target tracking) policies. Best for: predictable workloads. Combine predictive (proactive) + target tracking (reactive) for best results.' },
    tags: ['predictive-scaling', 'auto-scaling', 'performance', 'warm-up'],
  },
  {
    id: 'compute-011',
    stem: 'A company runs a web application on EC2 behind an ALB. They want to perform blue-green deployments where the entire traffic switches instantaneously from blue to green. Which approach achieves instantaneous cutover?',
    type: 'single', difficulty: 2, topicSlug: 'compute', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Route 53 weighted routing with 0% blue, 100% green after validation', correct: false, explanation: 'Route 53 DNS changes are subject to TTL propagation — not instantaneous. Old DNS records may persist up to TTL duration.' },
      { id: 'b', text: 'Update the ALB listener rules to forward traffic to the green Target Group', correct: true, explanation: 'ALB listener rule changes are instantaneous. Blue and green environments each have their own Target Group. Changing the ALB listener default action from blue TG to green TG takes effect immediately for all new connections.' },
      { id: 'c', text: 'Update Auto Scaling group launch configuration to use new AMI', correct: false, explanation: 'Updating the launch config only affects new instances. Existing blue instances remain until they are terminated.' },
      { id: 'd', text: 'Deploy CodeDeploy rolling deployment to gradually shift traffic', correct: false, explanation: 'Rolling deployment gradually replaces instances — not an instantaneous cutover.' },
    ],
    explanation: { overall: 'Blue-green with ALB: maintain two Target Groups (blue + green), each backed by a separate Auto Scaling group. The ALB listener rule points to one TG. To cut over: update listener rule to the new TG. This is instantaneous at the load balancer level. To roll back: update listener rule back to blue TG.', examTip: 'Instantaneous blue-green: ALB target group swap. Gradual blue-green: ALB weighted target groups (shift % over time). Route 53 weighted: DNS-based, not instantaneous due to TTL. Use ALB for true instant cutover.' },
    tags: ['blue-green', 'alb', 'target-groups', 'deployment'],
  },
  {
    id: 'compute-012',
    stem: 'A company uses Elastic Load Balancing with EC2 instances. During peak hours, some instances become unhealthy and are deregistered from the load balancer. The company notices that in-flight requests are being abruptly terminated. Which ELB feature prevents this?',
    type: 'single', difficulty: 2, topicSlug: 'compute', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Enable Connection Draining (Deregistration Delay) on the Target Group', correct: true, explanation: 'Connection Draining (called Deregistration Delay in ALB/NLB) keeps the target registered for a configured time (default 300s) to complete in-flight requests before fully deregistering. New requests are not sent to the draining target.' },
      { id: 'b', text: 'Increase the ALB idle timeout', correct: false, explanation: 'Idle timeout controls how long an idle connection is kept open, not in-flight request completion during deregistration.' },
      { id: 'c', text: 'Enable cross-zone load balancing', correct: false, explanation: 'Cross-zone load balancing distributes traffic evenly across AZs but does not handle graceful instance deregistration.' },
      { id: 'd', text: 'Configure health check grace period to 300 seconds', correct: false, explanation: 'Health check grace period delays health checks after instance launch, not during deregistration/scale-in.' },
    ],
    explanation: { overall: 'Deregistration Delay (Connection Draining) tells the load balancer: "stop sending new requests to this target, but wait up to X seconds for existing connections to finish." Default is 300 seconds. Set lower for fast-cycling stateless apps; higher for long-lived connections like file uploads.', examTip: 'Deregistration Delay / Connection Draining: in-flight request protection during scale-in. Set = 0 for stateless apps with instant drain. Set = connection duration for long-lived requests. Always enable for graceful scale-in.' },
    tags: ['connection-draining', 'alb', 'deregistration-delay', 'graceful-shutdown'],
  },
  {
    id: 'compute-013',
    stem: 'A company wants to use Spot Instances for a distributed processing workload but wants to minimize interruptions. The job can run on multiple instance families and sizes. Which Spot allocation strategy minimizes interruption risk?',
    type: 'single', difficulty: 2, topicSlug: 'compute', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Lowest price allocation strategy targeting the cheapest instance type', correct: false, explanation: 'Lowest price concentrates instances in one pool. If that pool runs out of capacity, all instances are interrupted simultaneously.' },
      { id: 'b', text: 'Capacity Optimized allocation strategy across multiple instance types and AZs', correct: true, explanation: 'Capacity Optimized selects Spot pools with the most available capacity, reducing the likelihood of interruption. Diversifying across instance types and AZs provides additional resilience.' },
      { id: 'c', text: 'Request the maximum possible Spot capacity in a single pool', correct: false, explanation: 'A single pool concentrates risk. If AWS needs the capacity back, all instances are interrupted at once.' },
      { id: 'd', text: 'Use On-Demand instances as backup when Spot is not available', correct: false, explanation: 'This is a valid pattern (mixed instances) but does not specifically minimize Spot interruptions on its own.' },
    ],
    explanation: { overall: 'Capacity Optimized strategy selects Spot pools with surplus capacity, resulting in fewer interruptions. Diversification across instance families (m5.large, m4.large, m5a.large) and multiple AZs further reduces correlated interruptions. This is the recommended strategy for fault-sensitive Spot workloads.', examTip: 'Spot allocation strategies: Capacity Optimized = least interruptions (recommended for most). Lowest Price = cheapest but highest interruption risk. Price Capacity Optimized = balance of price and capacity (newer option). Diversify = always spread across pools.' },
    tags: ['spot', 'capacity-optimized', 'interruption', 'diversification'],
  },
  {
    id: 'compute-014',
    stem: 'A company is migrating from on-premises VMs to EC2. Their license agreement for SQL Server requires that they use their existing per-core licenses. The SQL Server instances need to run on dedicated hardware. Which EC2 configuration is required?',
    type: 'single', difficulty: 2, topicSlug: 'compute', examDomain: 'migration-modernization',
    options: [
      { id: 'a', text: 'EC2 Dedicated Instances with Windows Server AMI and SQL Server installation', correct: false, explanation: 'Dedicated Instances run on dedicated hardware but do not provide host visibility (number of physical cores/sockets), which is required for per-core BYOL.' },
      { id: 'b', text: 'EC2 Dedicated Hosts with host affinity, running SQL Server BYOL AMI', correct: true, explanation: 'Dedicated Hosts provide visibility into physical cores and sockets, enabling per-core BYOL license compliance. The BYOL AMI contains the pre-installed SQL Server license.' },
      { id: 'c', text: 'RDS for SQL Server with existing license model', correct: false, explanation: 'RDS SQL Server supports License Included or BYOL but BYOL on RDS uses a different licensing model than physical per-core. BYOL on RDS requires specific SQL Server Enterprise agreements.' },
      { id: 'd', text: 'EC2 with SQL Server license included AMI', correct: false, explanation: 'License-included AMIs charge per-hour for the SQL Server license — they cannot use existing BYOL per-core licenses.' },
    ],
    explanation: { overall: 'SQL Server per-core BYOL requires knowing the number of physical cores. Dedicated Hosts provide this visibility. The BYOL license is bound to the specific Dedicated Host. Host affinity ensures the SQL Server instance always launches on the same host, maintaining license compliance.', examTip: 'BYOL per-socket/per-core = Dedicated Hosts required (need physical core count). BYOL per-VM = either Dedicated Hosts or Dedicated Instances work. License Included = any instance type.' },
    tags: ['byol', 'dedicated-hosts', 'sql-server', 'licensing'],
  },
  {
    id: 'compute-015',
    stem: 'A company runs EC2 instances behind an Application Load Balancer. They use Auto Scaling and want to implement a canary deployment where 10% of traffic goes to the new version. Which ALB feature enables this?',
    type: 'single', difficulty: 2, topicSlug: 'compute', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'ALB weighted target groups in a single listener rule', correct: true, explanation: 'ALB listener rules support weighted target groups. You can set the current version TG to weight 90 and the canary TG to weight 10. This distributes 10% of requests to the canary without DNS changes.' },
      { id: 'b', text: 'Route 53 weighted routing between two ALBs', correct: false, explanation: 'Route 53 weighted routing works at the DNS level with TTL delays. Not as precise as ALB weighted TGs and requires two separate ALBs.' },
      { id: 'c', text: 'Lambda@Edge to route 10% of requests to canary servers', correct: false, explanation: 'Lambda@Edge works with CloudFront distributions, not ALBs directly. ALB weighted TGs are simpler for EC2-based canary deployments.' },
      { id: 'd', text: 'ALB header-based routing to send requests with a specific header to the canary', correct: false, explanation: 'Header-based routing sends specific requests (those with the header) to canary, not a percentage of all requests.' },
    ],
    explanation: { overall: 'ALB weighted target groups allow splitting traffic by percentage across multiple target groups in a single listener rule. This is ideal for canary deployments, A/B testing, and gradual rollouts. No DNS changes needed, changes take effect immediately.', examTip: 'ALB routing types: host-based (different domains), path-based (/api vs /web), header-based (specific headers), query string, weighted (% split). Weighted target groups = precise traffic splitting without DNS.' },
    tags: ['alb', 'weighted-routing', 'canary', 'deployment'],
  },
  {
    id: 'compute-016',
    stem: 'An application runs on EC2 instances and stores session state in memory. The Auto Scaling group scales in during low traffic. When instances are terminated, active user sessions are lost. Which solution preserves sessions without major application changes?',
    type: 'single', difficulty: 2, topicSlug: 'compute', examDomain: 'continuous-improvement',
    options: [
      { id: 'a', text: 'Use Instance store volumes so session data survives termination', correct: false, explanation: 'Instance store is ephemeral — data is lost when the instance is stopped or terminated. The opposite of what is needed.' },
      { id: 'b', text: 'Offload session state to Amazon ElastiCache (Redis) or DynamoDB', correct: true, explanation: 'Externalizing session state to Redis or DynamoDB decouples sessions from instances. Any instance can serve any request because sessions are stored centrally. Instances can be terminated without losing user sessions.' },
      { id: 'c', text: 'Enable connection draining for 30 minutes before termination', correct: false, explanation: 'Connection draining keeps existing connections alive during deregistration but does not preserve sessions after the instance terminates.' },
      { id: 'd', text: 'Use sticky sessions (session affinity) on the ALB', correct: false, explanation: 'Sticky sessions bind users to specific instances. When that instance terminates, the session is lost. This creates scaling rigidity.' },
    ],
    explanation: { overall: 'Stateful applications need external session stores for elastic scaling. ElastiCache Redis: in-memory, sub-millisecond latency, ideal for session data. DynamoDB: durable, serverless, slightly higher latency. Both allow any EC2 instance to serve any user request, enabling true horizontal scaling.', examTip: 'Stateful EC2 + Auto Scaling = externalize state. Use ElastiCache for sessions/cache, DynamoDB for durable state, S3 for user uploads. Instances must be stateless for elastic scaling.' },
    tags: ['session-state', 'elasticache', 'stateless', 'auto-scaling'],
  },
  {
    id: 'compute-017',
    stem: 'A company wants to run Kubernetes workloads on AWS. They have existing investments in EC2 infrastructure. They want managed Kubernetes control plane but want to run worker nodes on their own EC2 instances for cost control and custom configurations. Which service fits this requirement?',
    type: 'single', difficulty: 1, topicSlug: 'compute', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Amazon EKS with self-managed node groups', correct: true, explanation: 'EKS provides a managed Kubernetes control plane. Self-managed node groups use your EC2 instances as worker nodes, giving full control over instance types, AMIs, and configuration.' },
      { id: 'b', text: 'Amazon EKS with Fargate profiles', correct: false, explanation: 'EKS Fargate eliminates EC2 nodes entirely (serverless pods). The company wants EC2 worker nodes for cost control and custom config.' },
      { id: 'c', text: 'AWS App Runner for containerized workloads', correct: false, explanation: 'App Runner is a fully managed service for containerized web applications. It does not support custom Kubernetes workloads.' },
      { id: 'd', text: 'Self-managed Kubernetes on EC2 (kops)', correct: false, explanation: 'Self-managed Kubernetes (kops) requires managing both control plane and worker nodes. EKS provides a managed control plane, reducing operational overhead.' },
    ],
    explanation: { overall: 'EKS offers three node types: Managed node groups (AWS manages EC2 lifecycle), Self-managed node groups (you manage EC2 with your AMIs), and Fargate (serverless). Self-managed gives most control; managed node groups balance control and convenience.', examTip: 'EKS node options: Managed nodes (recommended, AWS patches) > Self-managed (full control) > Fargate (no EC2 management). For custom AMIs or instance configs, self-managed is the choice.' },
    tags: ['eks', 'kubernetes', 'self-managed-nodes', 'ec2'],
  },
  {
    id: 'compute-018',
    stem: 'A company processes IoT sensor data with an application that requires GPU compute for ML inference. Traffic varies from near-zero at night to high during business hours. Provisioning GPU instances 24/7 is too expensive. Which solution optimizes costs while meeting compute requirements?',
    type: 'single', difficulty: 2, topicSlug: 'compute', examDomain: 'cost-optimization',
    options: [
      { id: 'a', text: 'G4dn Spot Instances with Auto Scaling based on SQS queue depth', correct: true, explanation: 'G4dn instances provide NVIDIA T4 GPUs for ML inference at lower cost than G5/P3. Spot pricing reduces cost by up to 70% vs On-Demand. Scaling based on SQS queue depth ensures capacity when needed and scales to zero at night.' },
      { id: 'b', text: '24/7 G4dn Reserved Instances for guaranteed availability', correct: false, explanation: 'Running GPU instances 24/7 even when unused at night wastes significant cost. Dynamic scaling is more appropriate.' },
      { id: 'c', text: 'AWS Lambda with GPU acceleration', correct: false, explanation: 'Lambda does not offer GPU acceleration. It is CPU-only.' },
      { id: 'd', text: 'Inf1 instances for inferentia chips with Lambda integration', correct: false, explanation: 'AWS Inferentia (Inf1) provides custom ML accelerators, but Lambda does not run on Inf1. Inf1 is EC2-based.' },
    ],
    explanation: { overall: 'Spot GPU instances for variable ML inference workloads: G4dn Spot (T4 GPU) or G5 Spot (A10G GPU) scale to match demand. SQS queue depth (ApproximateNumberOfMessagesVisible) as a custom CloudWatch metric drives Auto Scaling. Scales to 0 when queue is empty, saving 100% cost overnight.', examTip: 'GPU instance types: G4dn (NVIDIA T4, inference+training), G5 (A10G, ML), P3 (V100, deep learning training), P4 (A100, large-scale training), Inf1/Inf2 (Inferentia, inference). Spot + SQS scaling = cost-optimal variable inference.' },
    tags: ['gpu', 'spot', 'ml-inference', 'auto-scaling', 'sqs'],
  },
  {
    id: 'compute-019',
    stem: 'A company\'s web application experiences gradual memory leaks. Over hours, instances start failing health checks and are replaced by Auto Scaling. However, by the time the replacement instance is serving traffic, the application is already degraded. Which CloudWatch feature helps detect this before health checks fail?',
    type: 'single', difficulty: 2, topicSlug: 'compute', examDomain: 'continuous-improvement',
    options: [
      { id: 'a', text: 'Enable detailed monitoring (1-minute metrics) on EC2 instances', correct: false, explanation: 'Detailed monitoring increases metric frequency but does not add custom memory metrics — EC2 does not report memory usage to CloudWatch by default.' },
      { id: 'b', text: 'Install CloudWatch Agent to collect memory metrics and create alarms on high memory utilization', correct: true, explanation: 'CloudWatch Agent on EC2 instances can collect memory (mem_used_percent), disk usage, and custom application metrics. Alarms on memory trending upward can trigger pre-emptive replacement before application degradation.' },
      { id: 'c', text: 'Enable AWS X-Ray tracing to detect memory issues', correct: false, explanation: 'X-Ray traces requests and latency, not OS-level memory metrics.' },
      { id: 'd', text: 'Use EC2 instance recovery to automatically recover instances with memory issues', correct: false, explanation: 'EC2 instance recovery handles underlying hardware failures, not application-level memory issues.' },
    ],
    explanation: { overall: 'EC2 does not report memory metrics to CloudWatch by default (for privacy — AWS does not have access inside the OS). CloudWatch Agent must be installed to collect mem_used_percent, disk_used_percent, and custom application metrics. These can be used for alarms and Auto Scaling policies.', examTip: 'CloudWatch default EC2 metrics: CPU, network, disk I/O (at block device level), status checks. NOT memory, disk space. Install CloudWatch Agent for OS-level metrics. Use SSM distributor to deploy CloudWatch Agent at scale.' },
    tags: ['cloudwatch-agent', 'memory-metrics', 'monitoring', 'auto-scaling'],
  },
  {
    id: 'compute-020',
    stem: 'An application on EC2 makes API calls to AWS services (S3, DynamoDB, SQS). A security audit finds that the application is using hardcoded IAM access keys in environment variables. What is the correct way to remediate this without changing application code?',
    type: 'single', difficulty: 1, topicSlug: 'compute', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Store the access keys in AWS Secrets Manager and retrieve them at runtime', correct: false, explanation: 'Secrets Manager is better than environment variables, but the proper solution is to eliminate static credentials entirely using IAM roles.' },
      { id: 'b', text: 'Attach an IAM role to the EC2 instance with the required permissions; remove the hardcoded keys', correct: true, explanation: 'EC2 instance roles provide temporary credentials via the instance metadata service (IMDS). AWS SDKs automatically retrieve and rotate these credentials. No code changes needed — SDKs use the default credential provider chain which checks IMDS.' },
      { id: 'c', text: 'Rotate the access keys regularly using IAM credentials rotation', correct: false, explanation: 'Rotating keys reduces risk but still uses static credentials. IAM roles are the correct solution — no static credentials at all.' },
      { id: 'd', text: 'Use AWS KMS to encrypt the access keys stored in environment variables', correct: false, explanation: 'Encrypting hardcoded credentials is still storing credentials. IAM roles eliminate the need for any stored credentials.' },
    ],
    explanation: { overall: 'EC2 instance roles: attach an IAM role to the EC2 instance. The instance metadata service (IMDS) provides temporary credentials that are automatically rotated by AWS. AWS SDKs check IMDS automatically (credential provider chain). No keys to store, rotate, or manage.', examTip: 'Credential provider chain order: environment variables → AWS config file → IAM role for EC2 (IMDS). EC2 instance roles = best practice, no static credentials. Use IMDSv2 (token-based) for enhanced security against SSRF attacks.' },
    tags: ['iam-roles', 'imds', 'security', 'credentials'],
  },
];
