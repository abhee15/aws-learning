import type { Question } from '../../types/question';

export const containersQuestions: Question[] = [
  {
    id: 'ct-001',
    stem: 'A company runs microservices on ECS Fargate. They need service-to-service communication without exposing services publicly. Service A needs to call Service B. Which pattern provides private service-to-service communication?',
    type: 'single', difficulty: 2, topicSlug: 'containers', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Route traffic through a public ALB with HTTPS', correct: false, explanation: 'Public ALB exposes services to the internet. Service-to-service communication should stay private.' },
      { id: 'b', text: 'AWS Cloud Map for service discovery + internal ALB or direct DNS resolution', correct: true, explanation: 'Cloud Map provides DNS-based service discovery. Each ECS service registers with Cloud Map. Service A calls Service B via the Cloud Map DNS name, which resolves to the internal IP. No public exposure needed.' },
      { id: 'c', text: 'Use ECS Service Connect for automatic service-to-service routing', correct: true, explanation: 'ECS Service Connect provides managed service-to-service connectivity. Services are identified by a consistent endpoint name. Metrics (request count, latency) available per connection. No external load balancer needed for internal traffic.' },
      { id: 'd', text: 'Assign public IPs to all Fargate tasks for direct communication', correct: false, explanation: 'Public IPs expose services to the internet. Private communication should use private IPs within the VPC.' },
    ],
    explanation: { overall: 'ECS Service Connect (newer, recommended): managed mesh, automatic endpoint naming, built-in metrics. Cloud Map + internal ALB (classic): manual registration, more control. For new ECS architectures, Service Connect provides the simplest service-to-service connectivity.', examTip: 'ECS Service Connect = simplified microservice communication, automatic service discovery, built-in metrics. Cloud Map = generic service registry (supports EC2, Lambda, external). App Mesh = advanced traffic management (weighted routing, retries, circuit breaker).' },
    tags: ['ecs', 'service-connect', 'cloud-map', 'service-discovery', 'microservices'],
  },
  {
    id: 'ct-002',
    stem: 'A company runs an ECS cluster on EC2 launch type. During peak hours, ECS tasks cannot be placed because there is insufficient EC2 capacity in the cluster. Which feature automatically adds EC2 instances to the cluster when needed?',
    type: 'single', difficulty: 2, topicSlug: 'containers', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'ECS Auto Scaling with target tracking on CPU utilization', correct: false, explanation: 'ECS Auto Scaling adjusts the number of tasks, not EC2 instances. It does not add EC2 capacity to the cluster.' },
      { id: 'b', text: 'ECS Cluster Auto Scaling with Capacity Providers', correct: true, explanation: 'Capacity Providers link an ECS cluster to an Auto Scaling group. When ECS cannot place tasks due to insufficient capacity, the Capacity Provider triggers ASG scale-out. When tasks terminate, the ASG scales in.' },
      { id: 'c', text: 'Manual scaling: add EC2 instances to the Auto Scaling group when capacity is low', correct: false, explanation: 'Manual scaling requires human intervention and does not respond automatically to ECS placement failures.' },
      { id: 'd', text: 'Switch to Fargate to eliminate the need for EC2 capacity management', correct: false, explanation: 'Fargate eliminates EC2 management entirely, but the question asks about EC2 launch type with automatic scaling.' },
    ],
    explanation: { overall: 'ECS Capacity Providers: link cluster to ASG. Managed scaling: ECS calculates required instances based on task count/resources and adjusts ASG accordingly. Managed termination protection: prevents ECS from terminating instances with running tasks during scale-in.', examTip: 'ECS EC2 capacity: Capacity Provider + Managed Scaling = fully automated. Without Capacity Provider: ASG scales by CPU/memory metrics but ECS task placement may still fail if scaling lags. Capacity Provider calculates EXACTLY how many instances are needed.' },
    tags: ['ecs', 'capacity-providers', 'ec2', 'auto-scaling', 'cluster-scaling'],
  },
  {
    id: 'ct-003',
    stem: 'A company wants to migrate a monolithic application to containers. The application is not cloud-native and has several configuration files and binaries. The container image build takes 45 minutes due to the large number of dependencies. Which approach speeds up subsequent builds?',
    type: 'single', difficulty: 2, topicSlug: 'containers', examDomain: 'migration-modernization',
    options: [
      { id: 'a', text: 'Use AWS CodeBuild with a Docker build cache stored in ECR', correct: true, explanation: 'Docker layer caching: each RUN instruction in Dockerfile creates a layer. Cached layers are reused if unchanged. CodeBuild can pull the previous image from ECR (--cache-from) so unchanged layers are not rebuilt.' },
      { id: 'b', text: 'Use multi-stage Docker builds to reduce final image size', correct: false, explanation: 'Multi-stage builds reduce the final image size but do not speed up the build process itself (dependencies still need to be installed in the build stage).' },
      { id: 'c', text: 'Increase the CodeBuild instance size (higher vCPU) to speed up builds', correct: false, explanation: 'Larger CodeBuild instances help with CPU-bound tasks. Package downloads are typically I/O and network bound, not CPU bound.' },
      { id: 'd', text: 'Pre-build a base image with all dependencies and push to ECR; use it as FROM in application Dockerfile', correct: true, explanation: 'A base image with stable dependencies rarely changes. Application code changes only affect upper layers. Pulling the base image from ECR is faster than rebuilding from scratch.' },
    ],
    explanation: { overall: 'Dockerfile optimization for build speed: (1) Order instructions from least-changing to most-changing (dependencies before code). (2) Use multi-stage builds to separate build and runtime stages. (3) Pre-build base images. (4) Enable Docker build cache. (5) Use .dockerignore to exclude unnecessary files.', examTip: 'Docker layer caching: each instruction is a layer. If a layer changes, all subsequent layers rebuild. Put apt-get install/pip install BEFORE COPY of source code. Source code changes invalidate only the last few layers.' },
    tags: ['ecr', 'docker', 'build-optimization', 'layer-caching', 'codebuild'],
  },
  {
    id: 'ct-004',
    stem: 'A company runs ECS Fargate tasks that need access to AWS Secrets Manager. Instead of hardcoding credentials in environment variables, they want the secrets to be injected securely at task startup. Which ECS feature enables this?',
    type: 'single', difficulty: 2, topicSlug: 'containers', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Pass secrets as environment variables in the ECS task definition', correct: false, explanation: 'Hardcoded environment variables in task definitions are visible in plaintext. This is not secure for secrets.' },
      { id: 'b', text: 'Reference Secrets Manager ARNs in the ECS task definition secrets section; ECS injects them as environment variables at startup', correct: true, explanation: 'ECS task definition "secrets" field: reference Secrets Manager ARN or SSM Parameter Store path. ECS retrieves the value at task startup and injects as environment variables. The plaintext value is only in memory, not in the task definition.' },
      { id: 'c', text: 'Mount a Secrets Manager volume in the container', correct: false, explanation: 'Secrets Manager does not have a volume mounting interface. ECS native secrets injection or application-level SDK retrieval are the correct approaches.' },
      { id: 'd', text: 'Use Lambda to retrieve secrets and pass to ECS via API calls', correct: false, explanation: 'Lambda → ECS communication for secret injection adds complexity. ECS has native built-in secrets injection from Secrets Manager.' },
    ],
    explanation: { overall: 'ECS secrets injection: task definition "secrets" array references SSM Parameter Store (valueFrom: arn:...parameter/name) or Secrets Manager (valueFrom: arn:...secret:name). ECS IAM task execution role needs secretsmanager:GetSecretValue permission. Secrets injected as environment variables at startup.', examTip: 'ECS task roles vs task execution roles: Task Role = what the container can access (S3, DynamoDB). Task Execution Role = what ECS can do on your behalf (pull image from ECR, retrieve secrets from Secrets Manager/SSM). Both are needed.' },
    tags: ['ecs', 'secrets-manager', 'secrets-injection', 'task-definition', 'security'],
  },
  {
    id: 'ct-005',
    stem: 'A company runs EKS with managed node groups. They want to ensure Kubernetes pods can only communicate with specific AWS services. Pod-level network policies and IAM permissions need to be granular. Which feature provides per-pod IAM permissions?',
    type: 'single', difficulty: 3, topicSlug: 'containers', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Use EC2 instance profile IAM role on the node — all pods on the node share the role', correct: false, explanation: 'Node instance profile gives all pods on the node the same permissions. This violates least privilege for multi-workload nodes.' },
      { id: 'b', text: 'IAM Roles for Service Accounts (IRSA)', correct: true, explanation: 'IRSA uses OIDC federation: EKS creates an OIDC provider, Kubernetes service accounts are annotated with an IAM role ARN. Pods using that service account get temporary credentials for ONLY that specific IAM role. Pod-level least privilege.' },
      { id: 'c', text: 'Use Kubernetes RBAC to restrict pod permissions to specific AWS services', correct: false, explanation: 'Kubernetes RBAC controls Kubernetes API access (pods, deployments) not AWS API access (S3, DynamoDB). IAM controls AWS API access.' },
      { id: 'd', text: 'Mount IAM access keys as Kubernetes secrets', correct: false, explanation: 'Storing IAM access keys in Kubernetes secrets is a security anti-pattern. IRSA provides dynamic, automatically-rotated credentials.' },
    ],
    explanation: { overall: 'IRSA (IAM Roles for Service Accounts): OIDC identity provider on EKS cluster. IAM role trust policy: {"Federated": "oidc...eks..."} with Condition checking service account name/namespace. Kubernetes SA annotated with IAM role ARN. Pods get temporary creds via AWS SDK automatically. EKS Pod Identity (newer): even simpler setup.', examTip: 'IRSA vs EKS Pod Identity: Pod Identity is newer, simpler (no OIDC setup), supports cross-account. IRSA = well-established, widely used. Both provide per-pod IAM permissions. Node instance profile = last resort (shared permissions). Use IRSA/Pod Identity for production workloads.' },
    tags: ['eks', 'irsa', 'iam', 'kubernetes', 'pod-security'],
  },
  {
    id: 'ct-006',
    stem: 'A company runs ECS on EC2. Their application containers need to access sensitive files from a shared EFS file system. The ECS task must mount the EFS volume. Which is the correct way to configure this in ECS?',
    type: 'single', difficulty: 2, topicSlug: 'containers', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Install NFS client in each container and mount EFS using /etc/fstab', correct: false, explanation: 'Manual NFS mount inside containers is fragile and requires privileged containers. ECS has native EFS volume support.' },
      { id: 'b', text: 'Define an EFS volume in the ECS task definition and mount point in the container definition', correct: true, explanation: 'ECS task definition: define EFS volume with file system ID, root directory, and EFS authorization config. Container definition: mount the volume to a container path. ECS handles the EFS mount automatically.' },
      { id: 'c', text: 'Use an S3 sync sidecar container to sync files before the main container starts', correct: false, explanation: 'S3 sync sidecar is a pattern for S3, not EFS. EFS is a shared persistent file system with concurrent access support.' },
      { id: 'd', text: 'Bake the EFS content into the Docker image', correct: false, explanation: 'Baking content into Docker images makes them large and prevents shared access across tasks. EFS provides shared persistent storage.' },
    ],
    explanation: { overall: 'ECS EFS integration: task definition "volumes" section: EFSVolumeConfiguration with fileSystemId, rootDirectory, transitEncryption, authorizationConfig (IAM, accessPointId). ECS Fargate: requires platform version 1.4.0+. EFS access points provide isolated file system namespace and user mapping.', examTip: 'ECS storage options: EFS = persistent shared file system (multiple tasks/containers). EBS = block storage (one container at a time on EC2 launch type). Ephemeral storage (Fargate): /tmp, up to 200 GB. Bind mount: share between containers in same task.' },
    tags: ['ecs', 'efs', 'persistent-storage', 'volume', 'fargate'],
  },
  {
    id: 'ct-007',
    stem: 'A company uses Amazon ECR to store Docker images. They want to ensure that only approved, vulnerability-free images are deployed to ECS production. Images with critical CVEs must be blocked. Which workflow achieves this?',
    type: 'single', difficulty: 2, topicSlug: 'containers', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Enable ECR scan on push; pipeline checks scan results; block deployment if critical CVEs found', correct: true, explanation: 'ECR Enhanced Scanning (Inspector): auto-scans images on push using Inspector against CVE databases. CI/CD pipeline queries scan results via ECR API. If critical vulnerabilities found, pipeline fails and does not update ECS task definition.' },
      { id: 'b', text: 'Use IAM policy denying ECS access to ECR images with vulnerability tags', correct: false, explanation: 'ECR image tags are metadata strings. IAM cannot dynamically deny access based on Inspector scan results.' },
      { id: 'c', text: 'Configure an S3 lifecycle policy to delete vulnerable images from ECR', correct: false, explanation: 'ECR does not use S3 lifecycle policies. ECR has its own lifecycle policies for managing image expiration.' },
      { id: 'd', text: 'Enable ECR immutable tags to prevent overwriting approved images', correct: false, explanation: 'Immutable tags prevent tag overwrites (e.g., "latest" cannot be pushed over). It does not perform vulnerability scanning.' },
    ],
    explanation: { overall: 'Shift-left security pipeline: (1) Developer pushes to ECR. (2) ECR triggers Inspector scan. (3) EventBridge event with scan results → Lambda or pipeline evaluates severity. (4) If no critical CVEs: update ECS task definition / proceed with deployment. (5) If critical CVEs: fail pipeline, notify team. ECR Lifecycle policy: clean up old images.', examTip: 'ECR scan modes: Basic (CVE from NVD/Open Source) vs Enhanced (AWS Inspector, broader coverage). Enhanced scanning continuously rescans when new CVEs are published. Block deployment in CI/CD based on scan results: use describe-image-scan-findings API to check.' },
    tags: ['ecr', 'inspector', 'vulnerability-scanning', 'cicd', 'security'],
  },
  {
    id: 'ct-008',
    stem: 'A company runs 100 microservices on EKS. They want to observe inter-service request latency, error rates, and trace requests across services without modifying application code. Which approach provides this observability?',
    type: 'single', difficulty: 2, topicSlug: 'containers', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Enable CloudWatch Container Insights on the EKS cluster', correct: false, explanation: 'Container Insights collects node and pod-level CPU, memory, network, and disk metrics. It does not provide request-level distributed tracing between services.' },
      { id: 'b', text: 'Deploy AWS App Mesh with AWS X-Ray tracing; X-Ray SDK collects traces from Envoy proxies', correct: true, explanation: 'App Mesh injects Envoy sidecars that intercept all traffic. X-Ray traces are generated by Envoy without application code changes. Latency, error rates, and traces across services visible in X-Ray Service Map.' },
      { id: 'c', text: 'Add CloudWatch Logs to each microservice for request logging', correct: false, explanation: 'Logs provide raw data but require manual correlation across services. Distributed tracing with X-Ray provides automatic end-to-end request correlation.' },
      { id: 'd', text: 'Use Amazon GuardDuty for EKS to monitor inter-pod communication', correct: false, explanation: 'GuardDuty EKS monitors for security threats (privileged containers, anomalous API calls). Not for performance observability.' },
    ],
    explanation: { overall: 'App Mesh + X-Ray: Envoy proxy intercepts all east-west traffic. X-Ray integration: Envoy generates trace segments automatically. X-Ray Service Map shows: request flow, latency percentiles (p50, p95, p99), error rates, dependencies between services. Zero application code changes needed.', examTip: 'Service mesh observability: App Mesh + X-Ray = distributed tracing without code changes. CloudWatch Container Insights = infrastructure metrics (CPU, memory). CloudWatch Application Insights = APM for specific technologies. For microservice communication analysis, App Mesh + X-Ray is the standard.' },
    tags: ['app-mesh', 'x-ray', 'observability', 'distributed-tracing', 'eks'],
  },
  {
    id: 'ct-009',
    stem: 'A company runs EKS clusters in 3 regions. They want centralized management of all clusters, consistent policy enforcement, and the ability to deploy workloads to multiple clusters simultaneously from a single control plane. Which solution provides this?',
    type: 'single', difficulty: 3, topicSlug: 'containers', examDomain: 'organizational-complexity',
    options: [
      { id: 'a', text: 'AWS Control Tower with EKS blueprints', correct: false, explanation: 'Control Tower governs AWS accounts and Landing Zones. It does not provide multi-cluster Kubernetes workload management.' },
      { id: 'b', text: 'Amazon EKS Anywhere for on-premises Kubernetes management', correct: false, explanation: 'EKS Anywhere is for running EKS on on-premises or non-AWS infrastructure. Not for multi-region AWS EKS cluster management.' },
      { id: 'c', text: 'Amazon EKS with AWS GitOps using Flux or ArgoCD across multiple clusters', correct: true, explanation: 'GitOps (Flux/ArgoCD) uses a Git repository as the source of truth. Changes pushed to Git are automatically synced to all registered clusters. Enables consistent multi-cluster deployments from a single control point.' },
      { id: 'd', text: 'Create a single large EKS cluster spanning all 3 regions', correct: false, explanation: 'EKS clusters are regional — a single cluster cannot span multiple AWS regions.' },
    ],
    explanation: { overall: 'Multi-cluster EKS management: (1) GitOps (Flux/ArgoCD): declarative, Git-driven, multi-cluster sync. (2) Amazon EKS Connector: register external clusters with EKS console. (3) AWS Controllers for Kubernetes (ACK): manage AWS resources from Kubernetes. For fleet management: EKS Auto Mode simplifies node management.', examTip: 'EKS multi-cluster: no native AWS fleet management tool. GitOps with FluxCD/ArgoCD is the standard approach. ArgoCD: pull model, central control plane. FluxCD: pull model, per-cluster controllers. Both enable multi-cluster deployment from a single Git repository.' },
    tags: ['eks', 'multi-cluster', 'gitops', 'argocd', 'fleet-management'],
  },
  {
    id: 'ct-010',
    stem: 'A company migrates a legacy application to ECS Fargate. The application requires 4 vCPU and 30 GB memory. Is this Fargate configuration possible?',
    type: 'single', difficulty: 1, topicSlug: 'containers', examDomain: 'migration-modernization',
    options: [
      { id: 'a', text: 'No — Fargate maximum is 4 vCPU / 16 GB memory', correct: false, explanation: 'Fargate maximum is 16 vCPU / 120 GB memory. 4 vCPU / 30 GB is well within limits.' },
      { id: 'b', text: 'Yes — 4 vCPU / 30 GB is a valid Fargate task configuration', correct: true, explanation: 'Fargate supports up to 16 vCPU and 120 GB memory. For 4 vCPU, valid memory ranges are 8 GB to 30 GB. So 4 vCPU / 30 GB is the maximum memory for that CPU configuration and is valid.' },
      { id: 'c', text: 'No — Fargate requires an even ratio of CPU to memory', correct: false, explanation: 'Fargate has specific valid vCPU/memory combinations but not necessarily an even ratio. 4 vCPU supports 8-30 GB memory options.' },
      { id: 'd', text: 'No — Fargate does not support more than 8 GB memory per task', correct: false, explanation: 'Fargate supports up to 120 GB memory. This limit was significantly increased.' },
    ],
    explanation: { overall: 'Fargate task size limits: 0.25-16 vCPU. Memory: depends on CPU selection. 4 vCPU: 8 GB to 30 GB (in 1 GB increments). 8 vCPU: 16 GB to 60 GB. 16 vCPU: 32 GB to 120 GB. For memory-intensive workloads, Fargate can handle large allocations. EC2 launch type provides even more flexibility with bare metal instances.', examTip: 'Fargate task sizing: 0.25 vCPU (0.5-2 GB), 0.5 vCPU (1-4 GB), 1 vCPU (2-8 GB), 2 vCPU (4-16 GB), 4 vCPU (8-30 GB), 8 vCPU (16-60 GB), 16 vCPU (32-120 GB). For SA Pro, know the maximum limits and valid combinations.' },
    tags: ['fargate', 'task-sizing', 'cpu', 'memory', 'limits'],
  },
  {
    id: 'ct-011',
    stem: 'A company uses ECS with EC2 launch type and wants to ensure that ECS tasks cannot access the EC2 instance metadata (to prevent credential theft from the instance profile). How can this be achieved?',
    type: 'single', difficulty: 3, topicSlug: 'containers', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Remove the IAM instance profile from the ECS EC2 instances', correct: false, explanation: 'EC2 instance profile is needed for ECS agent itself to call ECS API. Removing it breaks ECS functionality.' },
      { id: 'b', text: 'Enable IMDSv2 (hop limit = 1) on EC2 instances to prevent containers from accessing instance metadata', correct: true, explanation: 'IMDSv2 with hop limit = 1: containers behind Docker bridge networking (NAT) have TTL decremented to 0 before reaching IMDS, blocking access. The ECS agent runs directly on EC2 (hop limit 1 = accessible) but containers (hop limit 2) cannot reach it.' },
      { id: 'c', text: 'Use ECS task-level network isolation with security groups on each task', correct: false, explanation: 'ECS security groups on tasks control inbound/outbound to/from tasks, not access to the EC2 metadata service endpoint (169.254.169.254).' },
      { id: 'd', text: 'Block 169.254.169.254 in NACLs for the ECS subnets', correct: false, explanation: 'NACLs block based on CIDRs at the subnet level. 169.254.169.254 is a link-local address that NACLs cannot block in the same way as routed traffic.' },
    ],
    explanation: { overall: 'IMDSv2 hop limit protection: AWS EC2 IMDSv2 with hop limit=1. EC2 instance (hop 1): can access IMDS. Docker container (hop 2, behind NAT): TTL decremented to 0, cannot access IMDS. ECS assigns task IAM roles via a separate proxy (169.254.170.2) that containers CAN access, providing per-task credentials.', examTip: 'IMDS security: IMDSv2 (token required, hop limit=1) protects against SSRF-based credential theft. ECS task roles accessed via 169.254.170.2 (separate from EC2 metadata). Always use IMDSv2 and set hop limit=1 on ECS EC2 nodes for container security.' },
    tags: ['ecs', 'imds', 'imdsv2', 'credential-theft', 'security'],
  },
  {
    id: 'ct-012',
    stem: 'A company runs ECS services and wants to implement blue-green deployments with automatic traffic shifting and rollback on CloudWatch alarm. Which service provides this capability for ECS?',
    type: 'single', difficulty: 2, topicSlug: 'containers', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'ECS rolling update deployment type with CloudWatch alarm rollback', correct: false, explanation: 'Rolling update gradually replaces old tasks. There is no built-in blue-green capability or automatic traffic shifting between two versions.' },
      { id: 'b', text: 'AWS CodeDeploy with ECS blue-green deployment type', correct: true, explanation: 'CodeDeploy ECS blue-green: creates a new task set (green) alongside the original (blue). Traffic shifts from blue to green (canary, linear, or all-at-once). CloudWatch alarm triggers automatic rollback if issues detected. Original task set (blue) remains until deployment succeeds.' },
      { id: 'c', text: 'AWS CodePipeline with ECS deployment provider', correct: false, explanation: 'CodePipeline orchestrates the CI/CD pipeline but uses CodeDeploy as the deployment provider for blue-green ECS deployments.' },
      { id: 'd', text: 'ECS Auto Scaling with Step Scaling to gradually scale up new tasks', correct: false, explanation: 'Auto Scaling adjusts capacity based on metrics, not version traffic splitting for deployments.' },
    ],
    explanation: { overall: 'CodeDeploy ECS blue-green: new task set deployed alongside original. ALB listener rule: test traffic (port 8080) → green, production traffic (port 443) → blue. Traffic shifting: Canary10Percent5Minutes (10% → 5 min wait → 90%), Linear, or AllAtOnce. Alarm-triggered rollback: returns all traffic to blue.', examTip: 'ECS deployment types: Rolling Update (gradual task replacement, simple). Blue/Green via CodeDeploy (two versions running, traffic shifting, rollback capability). For zero-downtime with easy rollback: CodeDeploy Blue/Green. For simple updates: Rolling Update.' },
    tags: ['codedeploy', 'ecs', 'blue-green', 'traffic-shifting', 'rollback'],
  },
  {
    id: 'ct-013',
    stem: 'A company stores container images in Amazon ECR. They want to automatically delete images older than 90 days that are not tagged as "stable" to reduce storage costs. Which ECR feature accomplishes this?',
    type: 'single', difficulty: 1, topicSlug: 'containers', examDomain: 'cost-optimization',
    options: [
      { id: 'a', text: 'ECR Lifecycle Policies with rules based on image age and tag patterns', correct: true, explanation: 'ECR Lifecycle Policies: define rules with tagStatus (tagged/untagged/any), tagPrefixList, countType (imageCountMoreThan or sinceImagePushed), and countUnit (days). Rule: tagged images NOT matching "stable" pattern, sinceImagePushed > 90 days → expire.' },
      { id: 'b', text: 'S3 Lifecycle Rules applied to the ECR underlying storage', correct: false, explanation: 'ECR manages its own underlying storage. S3 lifecycle rules cannot be applied to ECR repositories directly.' },
      { id: 'c', text: 'Create a Lambda function triggered by CloudWatch Events to delete old images', correct: false, explanation: 'Custom Lambda for image cleanup is possible but complex. ECR Lifecycle Policies provide this natively without any code.' },
      { id: 'd', text: 'Enable ECR image scanning to automatically delete vulnerable images', correct: false, explanation: 'ECR scanning identifies vulnerabilities but does not automatically delete images. Lifecycle policies handle deletion.' },
    ],
    explanation: { overall: 'ECR Lifecycle Policy: configured per repository. Rules evaluated in priority order. Actions: expire (delete) image. Example rule: {rulePriority: 1, description: "Delete non-stable images >90 days", selection: {tagStatus: "tagged", tagPrefixList: ["stable"], countType: "sinceImagePushed", countUnit: "days", countNumber: 90}, action: {type: "expire"}}.', examTip: 'ECR Lifecycle Policy types: (1) Untagged images after N days (clean up dev builds). (2) Images not matching stable tag prefix after N days. (3) Keep only last N images. Lifecycle policies reduce ECR storage cost significantly in active CI/CD environments.' },
    tags: ['ecr', 'lifecycle-policy', 'cost-optimization', 'image-management'],
  },
  {
    id: 'ct-014',
    stem: 'A company runs a stateful application on EKS that requires guaranteed CPU and memory for production workloads. Dev environment pods should use excess capacity but not starve production. Which Kubernetes feature ensures production pods get guaranteed resources?',
    type: 'single', difficulty: 2, topicSlug: 'containers', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Kubernetes Pod Priority Classes: production pods get high priority, dev pods low', correct: true, explanation: 'PriorityClass: production pods get a high priority value. When the cluster is under resource pressure, the scheduler evicts low-priority pods to make room for high-priority pods. Preemption ensures production pods are always scheduled.' },
      { id: 'b', text: 'Set CPU and memory requests on production pods to ensure guaranteed QoS class', correct: true, explanation: 'Kubernetes QoS classes: Guaranteed (requests = limits), Burstable (requests < limits), BestEffort (no requests/limits). Guaranteed pods are last to be evicted. Set requests = limits for production for Guaranteed QoS.' },
      { id: 'c', text: 'Use Kubernetes Node Affinity to pin production pods to dedicated nodes', correct: false, explanation: 'Node affinity pins pods to specific nodes. If those nodes run out of resources, production pods cannot scale. This is more suitable for hardware-specific requirements.' },
      { id: 'd', text: 'Enable Kubernetes Horizontal Pod Autoscaler for production pods', correct: false, explanation: 'HPA scales pods based on metrics but does not guarantee resource availability on the nodes.' },
    ],
    explanation: { overall: 'Kubernetes resource management for production: (1) Guaranteed QoS: requests = limits (not evicted until node is severely pressured). (2) PriorityClass: preemption for critical pods. (3) Resource Quotas per namespace: limit dev namespace consumption. (4) LimitRange: default requests/limits per namespace.', examTip: 'Kubernetes QoS: Guaranteed (request=limit) → last evicted. Burstable (request<limit) → evicted when node under pressure. BestEffort (no requests) → first evicted. For production: set requests = limits (Guaranteed) + high PriorityClass.' },
    tags: ['kubernetes', 'qos', 'priority-class', 'resource-management', 'eks'],
  },
  {
    id: 'ct-015',
    stem: 'A company uses AWS Fargate for ECS tasks. They want to reduce costs for non-critical background processing workloads. These workloads can be interrupted. Which Fargate feature reduces compute costs for interruptible workloads?',
    type: 'single', difficulty: 1, topicSlug: 'containers', examDomain: 'cost-optimization',
    options: [
      { id: 'a', text: 'Fargate Spot', correct: true, explanation: 'Fargate Spot uses spare Fargate capacity at up to 70% discount. Tasks can be interrupted with 2-minute notice. Suitable for batch processing, data transformation, and other interruptible workloads.' },
      { id: 'b', text: 'Fargate Reserved Capacity', correct: false, explanation: 'Fargate does not have Reserved Capacity pricing. AWS Savings Plans can provide discounts on Fargate usage.' },
      { id: 'c', text: 'Fargate ARM64 Graviton2 instances', correct: false, explanation: 'Graviton2-based Fargate tasks are up to 20% cheaper than x86, but they are not interruptible. Good for cost savings, but not for interruptible workloads specifically.' },
      { id: 'd', text: 'ECS Service Auto Scaling to scale down non-critical services', correct: false, explanation: 'Auto Scaling reduces task count during low demand but does not provide the deep discount of Fargate Spot.' },
    ],
    explanation: { overall: 'Fargate Spot: uses spare Fargate capacity at 50-70% discount. Capacity reclaimed with 2-minute notice. Application must handle interruption gracefully (checkpointing, graceful shutdown). ECS Service with Fargate Spot capacity provider: mix of Fargate (On-Demand) and Fargate Spot (similar to EC2 mixed instances).', examTip: 'Fargate cost options: Fargate On-Demand (full price, no interruptions). Fargate Spot (50-70% cheaper, interruptible). Graviton2 (20% cheaper, not interruptible). Compute Savings Plans (up to 50% savings on Fargate with commitment). For batch/non-critical: Fargate Spot.' },
    tags: ['fargate', 'spot', 'cost-optimization', 'batch'],
  },
  {
    id: 'ct-016',
    stem: 'A company runs containers on ECS and wants to collect container-level CPU, memory, and network metrics to CloudWatch for dashboards and alarms. Which approach enables this with minimal configuration?',
    type: 'single', difficulty: 1, topicSlug: 'containers', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Install CloudWatch Agent in each container', correct: false, explanation: 'Installing agents in each container increases image size and maintenance burden. Container Insights provides metrics without modifying containers.' },
      { id: 'b', text: 'Enable CloudWatch Container Insights for the ECS cluster', correct: true, explanation: 'Container Insights: enable at the cluster level (or account level). Automatically collects CPU, memory, network, and storage metrics per task, service, and cluster. Metrics available in CloudWatch. Logs via awslogs log driver.' },
      { id: 'c', text: 'Use AWS X-Ray to collect container performance metrics', correct: false, explanation: 'X-Ray collects distributed traces (request latency, service map). It does not collect infrastructure-level container CPU/memory metrics.' },
      { id: 'd', text: 'Deploy Prometheus and Grafana on ECS for container metrics', correct: false, explanation: 'Prometheus/Grafana provides powerful monitoring but requires setup and management. Container Insights is the managed AWS solution with less configuration.' },
    ],
    explanation: { overall: 'CloudWatch Container Insights for ECS: enable at cluster or account level. Metrics: CPUUtilization, MemoryUtilization, NetworkRxBytes, NetworkTxBytes, StorageReadBytes per task/service/cluster. Logs: awslogs driver sends container logs to CloudWatch Logs. Enhanced Observability (paid): adds task-level metrics.', examTip: 'Container Insights: ECS and EKS both support it. ECS: enable per cluster or account-level default. EKS: deploy CloudWatch agent as DaemonSet (or use Container Insights add-on). Metrics automatically available in CloudWatch for dashboards and alarms.' },
    tags: ['cloudwatch', 'container-insights', 'ecs', 'metrics', 'monitoring'],
  },
  {
    id: 'ct-017',
    stem: 'A company runs an EKS cluster and wants to automatically scale the number of EC2 nodes based on pending pods that cannot be scheduled due to insufficient resources. Which EKS feature provides this?',
    type: 'single', difficulty: 2, topicSlug: 'containers', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'EKS Horizontal Pod Autoscaler (HPA)', correct: false, explanation: 'HPA scales the number of pods, not the number of EC2 nodes in the cluster.' },
      { id: 'b', text: 'Kubernetes Cluster Autoscaler connected to an EC2 Auto Scaling group', correct: true, explanation: 'Cluster Autoscaler: watches for unschedulable pods (pending due to resource constraints). Triggers scale-out of the ASG to add nodes. Scales in by identifying underutilized nodes and evicting pods to consolidate them.' },
      { id: 'c', text: 'AWS Karpenter node provisioner', correct: true, explanation: 'Karpenter: faster and more flexible than Cluster Autoscaler. Directly launches EC2 instances (without ASG). Consolidates workloads and replaces expensive nodes with cheaper ones. Supports diverse instance types.' },
      { id: 'd', text: 'EKS Managed Node Groups Auto Scaling', correct: false, explanation: 'Managed Node Groups can be scaled but do not automatically react to unschedulable pods. Cluster Autoscaler or Karpenter trigger scaling based on pod demand.' },
    ],
    explanation: { overall: 'EKS node scaling options: Cluster Autoscaler (CA): K8s-native, works with ASGs, widely deployed. Karpenter: AWS-native, directly provisions instances, handles diverse instance types, faster provisioning, better bin-packing. Both watch for pending pods and scale nodes. Karpenter is the newer, more flexible option.', examTip: 'Karpenter vs Cluster Autoscaler: Karpenter = faster (seconds vs minutes), better bin-packing, instance type flexibility, spot diversification. CA = works with any K8s cluster. For EKS on AWS, Karpenter is the recommended modern approach for node provisioning.' },
    tags: ['eks', 'cluster-autoscaler', 'karpenter', 'node-scaling', 'kubernetes'],
  },
  {
    id: 'ct-018',
    stem: 'A company runs ECS services fronted by an Application Load Balancer. Multiple services share the same ALB. They want to route traffic based on URL path (/api → service A, /web → service B). How is this configured?',
    type: 'single', difficulty: 1, topicSlug: 'containers', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'ALB listener rules with path-based conditions routing to separate Target Groups', correct: true, explanation: 'ALB supports path-based routing: listener rule {IF path-pattern = /api/*} → Target Group A, {IF path-pattern = /web/*} → Target Group B. Each ECS service registers its tasks with the appropriate Target Group.' },
      { id: 'b', text: 'Use Route 53 latency-based routing between two separate ALBs', correct: false, explanation: 'Route 53 latency routing routes to different geographic endpoints. It does not provide URL path-based routing to different services behind the same ALB.' },
      { id: 'c', text: 'Configure ECS Service Connect to route based on path', correct: false, explanation: 'Service Connect provides service-to-service private routing, not public ALB path-based routing.' },
      { id: 'd', text: 'Deploy a separate ALB for each ECS service', correct: false, explanation: 'Separate ALBs work but are costly (hourly + LCU charges per ALB). Path-based routing on a single shared ALB is more cost-effective.' },
    ],
    explanation: { overall: 'ALB path-based routing: single ALB, multiple listener rules. Each rule has conditions (path-pattern, host-header, header, query-string) and an action (forward to target group). ECS services register with their respective TG. Maximum 100 rules per listener. Cost-effective consolidation of multiple services behind one ALB.', examTip: 'ALB consolidation: host-based routing (different domains/subdomains to different services), path-based routing (URL paths), and method/header-based routing — all on single ALB. Reduces ALB costs when running many microservices.' },
    tags: ['alb', 'path-routing', 'ecs', 'load-balancing', 'microservices'],
  },
  {
    id: 'ct-019',
    stem: 'A company builds a multi-tenant SaaS application on EKS. Each tenant\'s workload must be isolated. Tenant A pods must not access Tenant B pods\' network traffic. Which Kubernetes feature provides network-level isolation between tenants?',
    type: 'single', difficulty: 2, topicSlug: 'containers', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'Kubernetes RBAC to restrict pod access between namespaces', correct: false, explanation: 'RBAC controls Kubernetes API access (who can create/delete resources) not network traffic between pods.' },
      { id: 'b', text: 'Kubernetes NetworkPolicy with namespace selectors to restrict pod-to-pod communication', correct: true, explanation: 'NetworkPolicy: ingress/egress rules using pod selectors and namespace selectors. Block cross-tenant traffic: deny all traffic except from within the same namespace. Requires a CNI plugin that supports NetworkPolicy (e.g., Amazon VPC CNI with Calico, or AWS Network Policy Controller).' },
      { id: 'c', text: 'Kubernetes ResourceQuota to prevent tenants from consuming other tenants\' resources', correct: false, explanation: 'ResourceQuota limits compute resources per namespace but does not block network traffic between namespaces.' },
      { id: 'd', text: 'EKS Fargate profiles for each tenant to provide isolated underlying infrastructure', correct: false, explanation: 'Fargate profiles provide isolated compute (no node sharing) but do not enforce network policies between pods/namespaces.' },
    ],
    explanation: { overall: 'NetworkPolicy for multi-tenant isolation: per namespace, default deny all (both ingress and egress), then allow only approved traffic. EKS: Amazon VPC CNI supports NetworkPolicy with AWS Network Policy Controller (add-on). Alternative: Calico CNI for advanced NetworkPolicy. Combined with namespaces: strong tenant isolation.', examTip: 'EKS network isolation: NetworkPolicy enforced by CNI (not by Kubernetes API server). Amazon VPC CNI with AWS Network Policy Controller: eBPF-based, high-performance. Default Kubernetes: no network isolation between pods. Always install network policy support for multi-tenant EKS.' },
    tags: ['kubernetes', 'network-policy', 'multi-tenant', 'namespace', 'eks'],
  },
  {
    id: 'ct-020',
    stem: 'A company wants to run containerized batch processing jobs that are triggered by S3 object uploads. Each job processes a single S3 object and terminates. The jobs run for 10-30 minutes. They want minimal operational overhead and pay only for resources used. Which architecture is optimal?',
    type: 'single', difficulty: 2, topicSlug: 'containers', examDomain: 'new-solutions',
    options: [
      { id: 'a', text: 'ECS Fargate tasks triggered by EventBridge Pipes from S3 events', correct: true, explanation: 'EventBridge Pipes: S3 event → directly runs ECS Fargate task (run-task API). Each task processes one object and terminates. Fargate: pay for vCPU/memory only when the task is running. Zero management overhead.' },
      { id: 'b', text: 'Lambda with container image (15-minute timeout)', correct: false, explanation: 'Lambda maximum timeout is 15 minutes. Jobs running 30 minutes exceed this limit.' },
      { id: 'c', text: 'EKS with Kubernetes Jobs triggered by S3 events', correct: false, explanation: 'EKS requires node management (Karpenter reduces this) and is more complex for simple event-triggered batch processing.' },
      { id: 'd', text: 'AWS Batch with Fargate compute environment and S3 job triggers via SQS', correct: true, explanation: 'AWS Batch manages job queuing, scheduling, and retry. Fargate compute: no EC2 management. S3 → SQS → Lambda → SubmitJob to Batch. Batch handles job execution, retry on failure, and cleanup. Ideal for batch processing at scale.' },
    ],
    explanation: { overall: 'Event-triggered container jobs: (1) EventBridge Pipes + ECS RunTask: simple, event-driven, good for simple jobs. (2) AWS Batch + Fargate: better for complex job management (dependencies, retries, priority queues, job arrays). Both use Fargate for pay-per-use pricing.', examTip: 'ECS RunTask vs AWS Batch: RunTask = fire-and-forget single task. Batch = job queuing, dependencies, retries, priority, job arrays, compute environment management. For simple event-triggered jobs: ECS RunTask. For complex job workflows with dependencies: AWS Batch.' },
    tags: ['ecs', 'fargate', 'batch', 'eventbridge', 'event-driven'],
  },
];
